[package]
name = "stab-model"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false

[features]
execution-leak = ["dep:stab-engine"]

[dependencies]
stab-engine = { path = "../stab-engine", optional = true }

[lib]
path = "src/lib.rs"
