Dominant cost: supported `01`, `b8`, `r8`, `hits`, and `dets` readers now emit paired packed and sparse Stab submeasurements that match the pinned Stim dense and sparse reader filters; the `b8` packed path needed word-at-a-time decoding so the dense pair no longer paid byte-by-byte bit assembly.
Next owner action: keep the paired reader submeasurements guarded by schema-version-2 thresholds while final clean evidence remains below 1.25x, and keep `m8-measure-reader-ptb64-contract` as a checked no-ratio regression waiver until pinned Stim exposes a faithful ptb64 reader perf filter.
