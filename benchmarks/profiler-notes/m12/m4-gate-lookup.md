Dominant cost: canonical all-gate lookup is now split from alias, lowercase, and invalid lookup contracts, but the pinned Stim baseline can land around 100 ns and the Stab canonical measurement around 170 ns, making the direct pair beta-safe but too baseline-sensitive for an honest 1.25x threshold.
Next owner action: keep `gate_data_hash_all_gate_names` to `stab_gate_data_hash_all_gate_names` and the alias, lowercase, and invalid lookup contract measurements outside strict Stim-relative thresholds until larger repeated lookup sets or a less timer-dominated paired workload prove stable submeasurement evidence below 1.25x.
