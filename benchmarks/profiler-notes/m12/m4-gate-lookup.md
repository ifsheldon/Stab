Dominant cost: the batched direct-match row is still a sub-microsecond all-gate lookup microbenchmark, so the remaining roughly 1.6x ratio is dominated by benchmark dispatch noise plus alias and case-normalization checks after the canonical exact-name fast path.
Next owner action: keep `m4-gate-lookup` outside the 1.25x regression threshold file until a larger repeated in-process profile proves whether generated lookup tables would beat the current exact-name dispatch instead of only improving measurement noise.
