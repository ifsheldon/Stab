Dominant cost: the direct error-decomposition row is dominated by tiny independent/disjoint XYZ conversion filters, especially the sub-nanosecond pinned Stim `independent_to_disjoint_xyz_errors` and exact disjoint-to-independent cases where timer overhead is still material even after Stab measures case-diverse in-process batches; dirty p100 evidence still lands on both sides of the 1.25x cutoff.
Next owner action: keep the stable `approx_p10` direct pair guarded by a schema-version-2 submeasurement threshold, and keep exact, p100, and independent-to-disjoint filters outside the 1.25x gate until a future source-owned benchmark can compare larger paired case arrays or profiling proves a real arithmetic optimization.
