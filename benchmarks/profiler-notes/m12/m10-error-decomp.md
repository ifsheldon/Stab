Dominant cost: the direct error-decomposition row is dominated by tiny independent/disjoint XYZ conversion filters where wrapper overhead and the exact closed-form branch decide the remaining worst ratio, while `approx_p10` and `approx_p100` previously spent the full Newton fallback on disjoint cases that are impossible by zero-component algebra.
Next owner action: keep all four direct pairs visible in beta evidence, keep the stable `approx_p10` direct pair guarded by the schema-version-2 submeasurement threshold, and add thresholds for the exact, p100, and independent-to-disjoint pairs only after repeated clean primary reports prove enough headroom for those small filters after the exact-branch split.
