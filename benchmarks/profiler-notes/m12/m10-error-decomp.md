Dominant cost: the direct error-decomposition row contains nanosecond-scale conversions. The Stab runner previously placed a compiler black-box boundary around every full `Result`, while pinned Stim accumulates the three numeric outputs and fences only the completed batch witness. That runner mismatch inflated the exact and independent conversion ratios without measuring additional public work; the iterative `approx_p10` and `approx_p100` paths remain dominated by the faithful pinned-Stim Newton fallback.
Next owner action: keep one numeric output accumulator per enlarged Stab batch, preserve all four direct pairs in beta evidence, keep the stable `approx_p10` direct pair guarded by the schema-version-2 submeasurement threshold, and add thresholds for the exact, p100, and independent-to-disjoint pairs only after repeated clean primary reports prove enough headroom for these tiny filters.
