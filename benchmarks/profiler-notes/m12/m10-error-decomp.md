Dominant cost: the partial error-decomposition row contains nanosecond-scale conversions. Successful exact disjoint-to-independent and independent-to-disjoint calls expose the same numeric outputs as pinned Stim and remain faithful pairs. Failed p10 and p100 calls do not: pinned Stim leaves its final Newton iterate in output parameters, while Stab's public API returns `None`, so pairing those timings would compare different observable work.
Next owner action: keep the two successful pairs in beta evidence, keep failed p10 and p100 Stab calls visible only as report-only public-`None` timings, and retain the schema-version-2 `unstable-faithful-pairs` timing-regression waiver until repeated clean evidence gives both successful nanosecond-scale pairs enough headroom for source-owned 1.25x thresholds.
