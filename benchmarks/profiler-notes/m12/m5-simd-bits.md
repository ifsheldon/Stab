Dominant cost: the M5 bit-vector row mixes direct upstream-sized XOR and not-zero checks with Stab contract extras such as masked, range, and copy workloads, so the row median is not a faithful strict threshold surface; pinned Stim v1.16.0 labels the `not_zero` perf filter `simd_bits_not_zero_100K`, but its source sets `n = 10 * 1000` and bit 600, so the guarded Stab pair is named `stab_simd_bits_not_zero_10K` and mirrors the actual upstream work.
Next owner action: keep the direct `simd_bits_xor_10K` and `simd_bits_not_zero_100K` to `stab_simd_bits_not_zero_10K` pairs guarded by schema-version-2 submeasurement thresholds only while their actual work sizes and repeated clean evidence remain aligned, and keep masked, range, and copy contract extras outside Stim-relative timing thresholds until pinned Stim exposes matching filters or the benchmark matrix promotes them to Stab-only memory/throughput contracts.
