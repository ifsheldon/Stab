Dominant cost: the sparse parser workload was dominated by per-instruction target storage and generic plain-target parsing for repeated `H 0`, `CNOT 1 2`, and `M 0` lines; the current SmallVec target storage and simple plain-instruction fast path move the row below the 1.5x profiler-note threshold, but `circuit_parse_sparse` still determines the row ratio against pinned Stim.
Next owner action: keep the stable direct `circuit_parse` to `stab_circuit_parse` pair guarded by a schema-version-2 submeasurement threshold, and leave the slower `circuit_parse_sparse` pair outside the 1.25x gate until a fresh profile shows whether the remaining roughly 1.3x sparse-parser cost is parser dispatch, circuit item allocation, or still target construction.
