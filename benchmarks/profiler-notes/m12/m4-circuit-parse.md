Dominant cost: the sparse parser workload is dominated by repeated exact plain instructions such as `H 0`, `CNOT 1 2`, and `M 0`.
Current fix: the parser streams admitted source lines instead of materializing `Vec<&str>`, bounds capacity estimation by the admitted prefix, and fast-parses exact common plain `H`, `M`, `MZ`, `CX`, and `CNOT` instructions while preserving the generic parser for tags, arguments, comments, unusual whitespace, multi-target instructions, classical targets, and error cases. The benchmark now enters through `Circuit::from_stim_bytes`; valid UTF-8 is borrowed without a model-sized conversion, while opaque comment and tag bytes use the separately tested bounded fallback path.
Historical evidence: pre-A2 local reports measured this row below the unchanged `1.25x` gate, but their mutable `target/` paths and older parser entry point do not qualify as source-current evidence for the byte-oriented implementation.
Next owner action: produce a clean-revision baseline, timing compare, and allocation report at revision-qualified immutable paths; then record their digests here. Keep the stable direct `circuit_parse` to `stab_circuit_parse` pair guarded by its schema-version-2 submeasurement threshold, and add a separate sparse-pair threshold only after repeated clean reports prove enough stability.
