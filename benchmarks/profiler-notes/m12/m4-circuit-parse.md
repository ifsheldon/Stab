Dominant cost: the sparse parser workload is dominated by repeated exact plain instructions such as `H 0`, `CNOT 1 2`, and `M 0`.
Current fix: the parser now streams `input.lines()` instead of materializing `Vec<&str>`, keeps top-level circuit capacity from a newline count, and fast-parses exact common plain `H`, `M`, `MZ`, `CX`, and `CNOT` instructions while preserving the generic parser for tags, arguments, comments, unusual whitespace, multi-target instructions, classical targets, and error cases.
Evidence: `target/benchmarks/m4-watch-focused-final-parser/compare.json` measured `m4-circuit-parse` at `1.0867027027027027x`, and the clean committed-code primary beta report at `target/benchmarks/m12-primary-beta/compare.json` measured it at `1.1185x`.
Next owner action: keep the stable direct `circuit_parse` to `stab_circuit_parse` pair guarded by a schema-version-2 submeasurement threshold, keep the sparse pair covered by clean beta evidence while it remains below 1.25x, and add a separate sparse-pair threshold only after repeated clean reports prove enough stability.
