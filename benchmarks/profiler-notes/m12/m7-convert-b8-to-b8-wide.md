Dominant cost: before the byte-aligned identity path, `stab convert --in_format=b8 --out_format=b8 --bits_per_shot=2048` expanded every packed record into boolean records and repacked the same bytes, so the public CLI path paid materialization and bit-writer costs for a same-format byte-aligned copy.
Next owner action: keep the fast path limited to byte-aligned `b8 -> b8` conversions without observable side output, continue validating the packed record byte width before writing, and leave non-byte-aligned widths on the canonical reader/writer path so unused padding bits are not preserved accidentally.
