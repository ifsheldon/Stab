Dominant cost: WS5's streaming consolidation accidentally removed the earlier B8 identity path, so `stab convert --in_format=b8 --out_format=b8 --bits_per_shot=2048` again expanded every packed frame into dense bits and repacked it. Two shared-host trends measured `1.578x` and `2.473x`, and the focused clean-source baseline plus dirty pre-fix comparison measured `2.616x` on AArch64.
Resolved path: the shared `RecordStreamReader` remains the sole B8 framing and tail-validation owner but can now return one borrowed packed frame. Identity conversion writes byte-aligned frames directly and masks only unused tail bits for non-byte-aligned widths, preserving canonical output without dense expansion; the first dirty paired diagnostic measured `0.044783x`, and clean source revision `17871e75` measured `0.043610x` against its fresh pinned-Stim baseline.
Next owner action: keep representation-preserving access in the shared reader, retain strict incomplete-frame rejection and observable-side-output exclusion, and require fresh clean-revision primary evidence after any codec or convert-streaming refactor.
