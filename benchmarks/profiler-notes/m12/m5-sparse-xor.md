Dominant cost: the sparse XOR row now has separate schema-version-2 thresholds for table row-XOR and item-XOR, after `SparseXorVec::xor_item` was tightened around the sorted-unique small-row path and the benchmark evidence stopped hiding the item path behind the row median.
Post-beta beta-125 follow-up: the stricter gate exposed that `SparseXorTable_SmallRowXor_1000` could still flap around 1.25x when every tiny merge used the 64-item stack buffer path.
The accepted fix adds a 16-item stack buffer path before the existing 64-item path, preserving the same safe sorted symmetric-difference algorithm while reducing fixed per-call work for the small table rows used by the direct benchmark pair.
Next owner action: keep `SparseXorTable_SmallRowXor_1000` to `stab_sparse_table_row_xor_1000` and `SparseXorVec_XorItem` to `stab_sparse_xor_item_7` guarded only while repeated clean evidence remains below 1.25x, and revisit the sorted-unique vector representation before adding new sparse-XOR submeasurements.
