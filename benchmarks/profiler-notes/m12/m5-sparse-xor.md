Dominant cost: the sparse XOR row includes a larger table row-XOR workload and a tiny repeated `xor_item` microbenchmark, so row-median evidence alone is too weak for strict regression ownership; dirty post-beta reports put the table row-XOR pair on both sides of the 1.25x line, and the 7-item `xor_item` pair remains a nanosecond-scale surface above the 1.25x gate on low pinned-Stim baselines.
Next owner action: keep both `SparseXorTable_SmallRowXor_1000` and `SparseXorVec_XorItem` measured but outside strict thresholds until repeated clean evidence or further data-structure optimization reaches stable 1.25x evidence, and continue to report normalized row-XOR and item-XOR rates.
