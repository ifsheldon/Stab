Observed evidence: the clean allocation-tracked d25/r25 direct match at `1ad8108b202e5fdd1395c55666da7d38bd5a20a4` took 183.701 ms versus Stim's 31 ms, allocated 5,743,913 times across nine timed iterations, and reached 12,437,520 live bytes. Kernel CPU sampling was unavailable because this host sets `perf_event_paranoid=4`; path inspection therefore identifies, but does not claim a sampled percentage for, ordered `BTreeMap<SearchState, SearchState>` frontier bookkeeping, cloned `BTreeSet` observable masks on each transition, and repeated sparse detector-map lookup as the primary candidates.
Next owner action: profile on a host with userspace sampling enabled, then replace tree-owned search states with interned compact detector/observable state keys, indexed predecessor storage, hash or generation-marked visited lookup, and pre-indexed adjacency. Preserve deterministic output by canonicalizing only the recovered minimum path, and promote this row only after repeated clean direct-match evidence is at most 1.25x Stim.
