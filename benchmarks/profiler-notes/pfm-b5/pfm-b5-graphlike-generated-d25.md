Dominant cost: post-review allocation evidence and code-path inspection identify ordered `BTreeMap<SearchState, SearchState>` frontier ownership plus observable-mask symmetric-difference allocation on transitions as the remaining dominant candidates. Collision-checked arena indexes removed linear graph-edge scans and comparison-time observable-mask clones without duplicating edge payloads. The clean allocation-tracked d25/r25 direct match at `93b80dafcf50282088d96c68604f84bf0eed94e1` took 146.499 ms versus Stim's 31 ms, allocated 2,076,672 times across nine timed iterations, reached 15,461,688 peak live bytes, and recorded zero resident-byte delta; kernel CPU sampling could not assign percentages because this host sets `perf_event_paranoid=4`.
Next owner action: profile on a host with userspace sampling enabled, then replace tree-owned search states with interned compact detector/observable state keys, indexed predecessor storage, hash or generation-marked visited lookup, and reusable symmetric-difference buffers. Preserve deterministic output by canonicalizing only the recovered minimum path, and promote this row only after repeated clean direct-match evidence is at most 1.25x Stim.
