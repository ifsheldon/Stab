Dominant cost: allocation evidence and code-path inspection identify ordered `BTreeMap<SearchState, SearchState>` frontier bookkeeping, cloned `BTreeSet` observable masks on each transition, and repeated sparse detector-map lookup as the dominant candidates. The clean second-review allocation-tracked d25/r25 direct match at `37cf5860c72456347d1365340735fc7ef126d4de` took 184.769 ms versus Stim's 31 ms, allocated 5,755,680 times across nine timed iterations, reached 12,437,520 peak live bytes, and recorded zero resident-byte delta; kernel CPU sampling could not assign percentages because this host sets `perf_event_paranoid=4`.
Next owner action: profile on a host with userspace sampling enabled, then replace tree-owned search states with interned compact detector/observable state keys, indexed predecessor storage, hash or generation-marked visited lookup, and pre-indexed adjacency. Preserve deterministic output by canonicalizing only the recovered minimum path, and promote this row only after repeated clean direct-match evidence is at most 1.25x Stim.
