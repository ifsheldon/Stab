Dominant cost: final-audit allocation evidence, near-linear detector throughput shared with d25, and code-path inspection point to ordered search-state ownership and transition-time observable-mask construction instead of graph-edge lookup, DEM expansion, or maximum sparse IDs. Collision-checked arena indexes removed linear graph-edge scans and comparison-time mask clones without duplicating edge payloads, and two-phase graph admission now rejects resource-limit violations before reserving vector or index capacity. The clean allocation-tracked d11/r1000 direct match at `4c5901e2eaf03ddf0c8043b5655d943b70b92a70` took 1.070 s versus Stim's 250 ms, allocated 15,719,538 times across nine timed iterations, reached 115,199,520 peak live bytes, and recorded zero resident-byte delta; kernel CPU sampling could not assign percentages because this host sets `perf_event_paranoid=4`.
Next owner action: use this scale row to validate compact interned state storage, indexed predecessors, and reusable observable-mask buffers after a sampled profile is available. Add generated differential tests around equal-length tie handling before changing frontier representation, and keep this faithful row report-only until repeated clean evidence is at most 1.25x Stim with bounded live allocation.
