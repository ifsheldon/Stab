# PERFQ-M5-SIMD-BITS-NOT-ZERO-LATE Profiler Note

Owner: `stab-bits/bit-vector`.

Status: clean A9 revision `f740fd36442e3561b3cb6acf172a24030352fe80` produced passing full reports at all three scales and passing soak reports at small and medium. Their paired median Stab-to-Stim ratios ranged from `0.994593x` through `1.050775x`, and the worst bootstrap confidence upper bound was `1.080483x`. Operator-observed, non-replayable history records that the soak-large qualification command reached a failed or noisy timing outcome before the producer refused publication because this group had no source-owned failure owner. The absent report and unused intended path are verifiable, but the unpublished outcome class and internal attempt sequence are not; no timing ratio is claimed. Earlier environmental load rejections are likewise operator-observed history with verifiable artifact absence, not replayable host evidence.

Dominant cost: the late-hit workload forces `BitVec::not_zero` to inspect the complete logical vector and its terminal tail before finding the final set bit. Fixture construction, allocation, input validation, output construction, and digesting remain outside the `raw-work-v2` timer, so an accepted report measures the full scan and Boolean accumulation. The retained `f740fd36` full-large result has median ratio `1.050775x`, upper bound `1.080483x`, and paired relative MAD `0.014700`; the failed or noisy soak-large command has no publishable statistics. Stack sampling remains unavailable while this host uses `perf_event_paranoid=4`.

Historical evidence at clean revision `60b732c77f1828058fbd65ec6c5c4ad582467fd1` measured the same late-hit intent at approximately `0.51x` through `0.66x` Stim, but it predates the current worker, inventory, timing-boundary, and evidence contracts and is diagnostic context only. It cannot substitute for source-current full and soak reports.

Next owner action: bind this note to the checked runtime group, regenerate and review every affected contract, commit the repair, require exact-head CI, and restart the complete A9 correctness, worker, timing, memory, rollup, and completion chain from that one clean revision at new paths. If the first source-current late-large report is again failed or noisy, retain and inspect the now-publishable failure report, profile the exact full-scan path with available allocation and phase instrumentation, and optimize the implementation if required. Do not add a waiver, change either the `1.25x` Stim parity gate or the `1.15x` Stab self-regression gate, shrink the workload, reuse an artifact path, or rerun in search of a favorable sample.
