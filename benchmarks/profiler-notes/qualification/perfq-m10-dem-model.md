# PERFQ-M10-DEM-MODEL Profiler Note

Owners: `stab-model/dem-parser` for `PERFQ-M10-DEM-PARSE-CONTRACT` and `stab-model/dem-printer` for `PERFQ-M10-DEM-PRINT-CONTRACT`.

Status: clean revisions `f23386bdc12258eab97b9997b3f478841caa050c` and `d8de73d50fbeb4e001ea38c784d1fdcfc76dad76` produced and replayed faithful parse/small/full reports under their exact source inventories. Their stable median failures were respectively `1.773450x` and `1.372971x`; neither was noisy or rerun from the same revision. Clean inventory-bound revision `ca9fd68d3856e9cac9cc6da16433947d056e8848` then passed parse/small/full and parse/medium/full, but its first parse/large/full report failed stably because the bootstrap confidence upper bound was `1.263920x`. Clean revision `61bf222` completed the repaired three-family matrix after the folded maximum was corrected: all print and folded-parse reports passed, while all flat-error and coordinate-sparse parse reports failed stably. Clean A9 revision `92b629faca87347786f9cb1296f32b9ddb1121b4` exposed a later valid-path regression at `1.524360x`, and the fifth optimization reduced but did not close that regression on clean revision `5417fd661a67b5bc181795dde7631218070cf494` at `1.307677x`. The sixth optimization closed Stim parity on clean revision `68db1796ab7a427d5e4a6f7be5679a3debaa89bb`, but source-owned Stab self-regression correctly rejected folded-repeats-small. A seventh production optimization now restores fast structural scanning and cheap shallow destruction while retaining rich-parser and deep-stack safety fallbacks; no source-current formal evidence exists yet. The fixture, semantic-work denominator, comparator, output obligations, representative scales, common-iteration policy, `1.25x` parity threshold, and `1.15x` self-regression threshold remain unchanged and unwaived.

## Representative Families

The repaired release contract replaces the former repeated mixed cycle with three deterministic workload families at 64, 4,096, and 65,536 compact work items. `flat-errors` measures flat error-heavy throughput with varied probabilities, detector and observable targets, and separators. `coordinate-sparse` measures tags, coordinate declarations and shifts, plus sparse high detector and observable identifiers. `folded-repeats` measures nested compact repeat blocks with large repeat counts that must remain folded.

Rust and C++ generate each family independently and must agree on exact input bytes, input digest, canonical output bytes, and semantic output digest before timing is promotable. Scale monotonicity is evaluated within a family, never between unrelated families. The accepted maximum is 524,288 compact items for `flat-errors` and `coordinate-sparse`, and 131,072 compact items for `folded-repeats`; the folded cap keeps its six-line cycle within Stab's public one-million-line DEM parser boundary.

## Initial Diagnosis

Dominant cost: per-instruction allocation and generic string processing are confirmed material costs, although post-fix formal closure is not yet proven. Before optimization, parsing the 4,096-item qualification family performed 14,859 allocation calls, approximately 29 per eight-item cycle. Stab allocated a lowercase instruction name, argument vector, and target vector on common instructions and repeatedly grew the top-level and spill-sized target vectors. Pinned Stim parses bytes directly and commits argument, target, and tag payloads into monotonic buffers. The frozen workload intentionally repeats tags, numeric arguments, separators, detector and observable targets, shifts, and nested blocks, so removing any of those features to improve the ratio would invalidate the comparison.

The print path was not implicated by the original diagnostic probe. Both workers retain owned-string production in timing, return the final owned string from the timed callback, and transfer it to post-timing validation without charging either implementation for that transfer. Only the final known terminal-newline difference is normalized after timing.

A stack-sampling profile is unavailable on this host because `/proc/sys/kernel/perf_event_paranoid` is `4`. This does not permit changing the benchmark contract. Allocation instrumentation, direct worker scaling, and source inspection supplied the evidence used for the first production change.

## Implemented Optimization

Commit `fb089098406892756572ea14439452a1001df57a` keeps up to two DEM arguments and one target inline, pre-sizes spill target and top-level storage, parses instruction names without allocating lowercase strings, parses unsigned targets with checked decimal accumulation, and bypasses character-by-character comment scanning on comment-free lines. A preallocation cap prevents newline-heavy hostile input from causing an unbounded speculative top-level allocation before the parser enforces its line limit.

The new public regressions preserve mixed-case instruction names, Unicode target whitespace, trailing comments, hashes inside tags, escaped closing brackets, payloads larger than the inline capacities, canonical reparsing, and the exact qualification family. The 4,096-item allocation guard admits at most 4,100 calls, down from the measured 14,859-call pre-fix result. All `stab-core` tests, workspace tests, formatting, workspace Clippy, and staged pre-commit checks passed before the implementation commit.

A dirty-tree source-owned adapter probe after the change reported `stim_seconds=0.001916643`, `stab_seconds=0.002525429`, and diagnostic ratio `1.317631x` over 16,384 top-level item operations. A separate dirty-tree Stab worker executed 10,000 owned parses of the 64-item fixture in `0.096216406` seconds. These are optimization guidance only: neither result is clean paired evidence, neither can satisfy the gate, and both are superseded by the required clean note-binding revision.

Clean note-binding revision `3a78eb74ef62d22631709b10618186567a1ece17` passed both sequential source-owned adapter probes with exact output parity. The parse probe reported `stim_seconds=0.003617204`, `stab_seconds=0.002420963`, and diagnostic ratio `0.669291x`; the print probe reported `stim_seconds=0.005289254`, `stab_seconds=0.006054023`, and diagnostic ratio `1.144589x`. Worker reproducibility also passed with pinned-Stim digest `cb484542faaeba73156a1ba5d7a1f35104b697320847ad33994b9bb1f33b67d4` and Stab digest `9a2205c5522144b4a25facc69dd60b2a5cd7bf6f9ef6d574e8513b126d930382`.

The focused CQ producer at that revision then stopped before execution or publication because the parser optimization moved 108 rustdoc source lines and the generated correctness digest no longer matched the frozen inventory. No correctness artifact was created. Regeneration confirmed that only those 108 source-line fields and the derived digest changed: selected parents, owners, selectors, dispositions, case IDs, counts, and the exact DEM prerequisite remained unchanged. The clean probes and worker check prove the pre-refresh implementation contract but cannot be promoted across the inventory change.

Clean inventory-refresh revision `d8de73d50fbeb4e001ea38c784d1fdcfc76dad76` passed both sequential adapter probes, private-worker reproducibility, the exact one-case CQ prerequisite, its replay, and exact preflight. Its parse/small/full report retained 29,527 common iterations and 1,889,728 item operations per sample across nine pairs, then failed stably at median ratio `1.3729706998216054x`, bootstrap 95 percent interval `[1.366241707530459x, 1.3773752216236312x]`, and paired relative MAD `0.0032080231592692974`. Replay passed and regression failed closed on the non-passing outcome. This is permanent evidence that the first optimization was material but insufficient; it must not be rerun or replaced by a favorable sample from the same revision.

## Second Implemented Optimization

Commit `e9b94d524167b2ca7bb953e525abc26e77641629` stores short UTF-8 DEM tags in a private 16-byte inline representation with a heap fallback, fast-paths tags without escapes, parses targets in one pass, reserves spill storage once when a second target is present, and estimates top-level capacity from at most 256 input bytes. These changes preserve arbitrary UTF-8 tags, escaped tags, public `Option<&str>` accessors, constructor inputs, parser limits, canonical output, and the frozen benchmark fixture.

The exact 4,096-item qualification-family allocation measurement fell from the first optimization's guard of 4,100 calls to 2,049 observed calls under a 2,050-call guard, with 963,816 allocated bytes. Long ASCII tags, an 18-byte multi-byte UTF-8 tag, escaped tags, comments, and multi-target instructions exercise the inline and fallback paths. Full `stab-core` tests, workspace tests, formatting, workspace Clippy, and staged pre-commit checks passed for the clean commit.

Final dirty-tree direct-worker diagnostics reported `0.416383174` seconds for the small diagnostic run and `0.377149828` seconds for the large diagnostic run, with observed peak RSS `34701312` bytes in the latter. Two- and three-target inline alternatives were marginally faster but materially enlarged every instruction, so the final implementation retained one inline target and a single spill reservation. These measurements are local optimization guidance only; they have no paired Stim samples, cannot be promoted, and do not prove the gate.

Clean inventory-bound revision `ca9fd68d3856e9cac9cc6da16433947d056e8848` passed both sequential source-owned adapter probes, private-worker reproducibility, the exact one-case CQ prerequisite, its replay, and exact preflight. Parse/small/full passed at median ratio `1.1741490288680279x` with bootstrap 95 percent interval `[1.1520024305094314x, 1.1828408574028013x]`; parse/medium/full passed at median ratio `1.204309243184199x` with interval `[1.1913888243751443x, 1.2143650043774166x]`. The first parse/large/full report retained 31 common iterations and 2,031,616 item operations per sample across nine pairs, then failed stably at median ratio `1.2482205525151264x`, bootstrap 95 percent interval `[1.2297776854679268x, 1.2639196099402108x]`, and paired relative MAD `0.012577150242760585`. Replay passed and regression failed closed on the non-passing large-scale outcome. This report is permanent evidence and must not be rerun or replaced by a favorable sample from the same revision.

## Third Implemented Optimization

Commit `430428ea93a40af25a352746acc2bc517e7ad1fd` reduces the persistent representation of a spilled DEM tag from `String` to `Box<str>`, keeping `DemInstruction` and `DemItem` at or below 96 bytes, and moves the private tag representation into a cohesive module so the parser source remains below the project size threshold. Target tokens now dispatch directly from their leading byte without a preliminary all-digit scan, and unsigned decimal parsing uses one explicit overflow bound per digit. The parser also accepts lowercase `d` and `l` targets like pinned Stim while preserving uppercase canonical output.

The regressions bind the model-layout ceiling, maximum and overflowing unsigned targets, malformed targets, lowercase target canonicalization, mixed ASCII and Unicode boundary whitespace, arbitrary UTF-8 tags, and the existing 4,096-item allocation ceiling. The implementation preserves public API signatures, parser limits, canonical output, and the exact qualification fixture, and uses no unsafe code. Full workspace tests, formatting, workspace Clippy, and staged pre-commit checks passed before the clean implementation commit.

Five dirty-tree direct Stab worker diagnostics over the large 31-iteration shape improved from the preceding revision's `0.458354964` through `0.465637638` seconds and approximately 34.7 MiB peak RSS to `0.426247111` through `0.431028809` seconds and approximately 33.0 MiB peak RSS. Dirty-tree source-owned adapter probes also passed exact parity: parse reported `stim_seconds=0.002125009`, `stab_seconds=0.002416290`, and diagnostic ratio `1.137073x`; print reported `stim_seconds=0.005317445`, `stab_seconds=0.003130915`, and diagnostic ratio `0.588801x`. These figures are diagnostic optimization guidance only. They are not clean paired formal evidence and cannot satisfy the gate.

## Three-Family Evidence And Fourth Optimization

Clean revision `61bf222` first published and preserved a complete 36-report AArch64 matrix under the corrected three-family contract. Both full and soak print reports passed at every scale with medians from `0.424820x` through `0.733220x` and worst confidence upper bound `0.743439x`. Folded-repeat parse also passed all six reports with medians from `0.524082x` through `0.601938x` and worst upper bound `0.609743x`. Flat-error parse failed all six reports with medians from `1.419965x` through `1.548927x`, coordinate-sparse parse failed all six with medians from `1.258467x` through `1.369423x`, and the parse rollup rejected the family. None of these stable reports was rerun.

The retained reports and allocation instrumentation showed that the representative families exceeded the old inline payload shapes: flat errors use up to five targets, while coordinate-sparse uses up to five targets and three arguments. The fourth optimization enables `smallvec`'s maintained union representation, keeps three arguments and five targets inline, dispatches canonical instruction names directly, tokenizes targets with a byte cursor, and accumulates decimal values of at most 18 digits without an impossible overflow branch. Generic mixed-case names, arbitrary target widths, malformed UTF-8 boundaries, overflow, tags, comments, whitespace, and spill-sized public constructors retain their existing fallbacks and validation.

The 4,096-item representative allocation guard now admits at most two fixed parse allocations for both flat-error and coordinate-sparse input; flat errors previously performed 3,585 allocations. Dirty-tree 691-iteration direct adapter probes preserved exact output identity. Flat errors improved from a preceding diagnostic ratio of `1.297874x` to `1.050847x` with `stim_seconds=0.176320391` and `stab_seconds=0.185285773`; coordinate-sparse reported `0.880783x` with `stim_seconds=0.206308428` and `stab_seconds=0.181713033`. These direct probes are optimization guidance only and cannot satisfy the formal gate.

## Required Owner Action

Next owner action: bind the seventh optimization, updated note digest, regenerated correctness and performance inventories, and synchronized A9 status in one reviewed source-contract revision. Require exact-head CI, then regenerate every A9 correctness prerequisite, worker receipt, report, rollup, memory receipt, and completion artifact from that clean revision at new paths. The clean Git revision, private Cargo build, sealed Stab binary digest, and worker identity bind the restored valid-path behavior; the comparator-source list remains reserved for the pinned-Stim comparator implementation.

1. Reproduce both sealed workers and both DEM probes from the clean note-binding revision before formal timing.
2. Publish the first full and soak report for every release scale exactly once unless the controller classifies a result as noisy and permits its single complete rerun.
3. Inspect retained raw samples, calibration decisions, paired-ratio relative MAD, confidence bounds, setup and peak RSS, and Stab allocation behavior before accepting the optimization.
4. If parse or print fails the gate, diagnose the retained report independently; do not average groups, transfer evidence, relax the threshold, add a waiver, or reuse an artifact path.
5. After any parser, printer, worker, adapter, note, schema, or runtime-contract change, regenerate the affected source-owned contracts and restart correctness, reproducibility, probes, full and soak reports, parity checks, rollups, and the revision completion manifest from the replacement revision.

Current acceptance still requires both the median paired ratio and bootstrap 95 percent upper bound to be at most `1.25x` at all three scales, with no waiver path. Failed, noisy, host-rejected, and controller-rejected attempts remain visible in the progress report and cannot be replaced by favorable reruns.

## Independent Review Correction

Independent review on 2026-07-23 found that the C++ DEM callbacks returned into preconstructed result variables by move assignment before the timer stopped, while the Rust worker returned its owned result from the measured closure and retained it afterward. The shared C++ adapter now uses an output-returning measurement helper for DEM parse and canonical print and every older return-valued workload, samples the clock before post-timing transfer, and keeps result construction itself inside timing; mutation-only kernels retain the void helper. The review also found that C++ peak RSS was sampled after canonicalization and digest work while Rust sampled immediately after timed dispatch; C++ now captures peak RSS at the same pre-digest phase boundary. These source-bound corrections invalidate the `9497df0` timing and accepted-maximum memory chain as current evidence even though its artifacts remain valid historical records under their exact source contract. Fresh correctness, probe, reproducibility, timing, memory, rollup, and completion evidence is required from a clean corrected revision.

## A9 Valid-Path Regression And Fifth Optimization

Exact-head GitHub Actions passed for clean revision `92b629faca87347786f9cb1296f32b9ddb1121b4`, and its exact correctness prerequisites, oracle checks, worker reproducibility, and the 17 non-DEM full-tier groups passed before the first DEM report. The immutable report at `target/benchmarks/qualification/a9-92b629fa-formal-r1-full-PERFQ-M10-DEM-PARSE-CONTRACT--flat-errors-small` then failed on its first attempt with median ratio `1.5243595551801157x`, bootstrap 95 percent interval `[1.5124992260232082x, 1.5374062462114146x]`, and paired relative MAD `0.003644796192198851`. The host was verified, work and output identities matched, swap was restored, and the stable result was not rerun.

Comparison with the retained `68d107a` report showed that Stim's median time per 64-item parse changed by approximately `0.6` percent while Stab's changed from approximately `9.385` microseconds to `13.129` microseconds, a diagnostic Stab slowdown of approximately `39.9` percent. The later A2 diagnostic work had replaced the comment-free line shortcut and canonical instruction dispatch with a stateful source-command scan over every byte plus generic name scanning on every valid instruction. The architecture extraction preserved that implementation and therefore preserved the regression; the separate crate boundary and formal private build were not the cause.

The fifth optimization returns a complete line immediately when it contains no tag, comment, or block delimiter, while all context-sensitive lines continue through the existing `SourceCommands` state machine. It also recognizes canonical lowercase instruction names before falling back to the existing mixed-case scanner and `DemInstructionKind` lookup. The same source slices, limits, target and argument parsers, semantic validation, diagnostic precedence, error spans, and fallback behavior remain authoritative. No second parser or benchmark-only grammar is introduced.

All `stab-model` tests and warnings-denied Clippy pass, including mixed case, comments, tags, blocks, malformed syntax, diagnostics, allocation ceilings, and all three qualification families. A source-current ordinary release probe over 691 parses of the 4,096-item flat family improved from `1.388462x` before the change to `1.165144x` after it. The exact failed work shape, 41,139 parses of 64 items, reported `stim_seconds=0.196601174`, `stab_seconds=0.211692130`, and diagnostic ratio `1.076759x`. These one-pair dirty-tree probes are optimization guidance only and cannot satisfy the formal gate.

The next clean revision must regenerate every affected source-owned inventory, pass exact-head CI, reproduce correctness and both workers, and start the complete A9 correctness and performance cycle at new artifact paths. The accepted `92b629fa` non-DEM reports and rollups become historical with this source change and cannot be combined with replacement DEM evidence.

## Fifth Optimization Rejection And Sixth Optimization

Clean revision `5417fd661a67b5bc181795dde7631218070cf494` bound the fifth optimization and passed exact-head GitHub Actions run `30766263266`. Its PR correctness report passed 700 of 700 selected parents, its broad full and soak reports each passed 933 of 933, all 11 exact full prerequisites passed and replayed, the live 62-case result-format corpus and implemented oracle matrix passed, and worker reproducibility produced the expected 46 canonical receipts. These artifacts remain valid historical evidence under that exact revision, but no later revision may promote them.

The immutable report at `target/benchmarks/qualification/a9-5417fd66-formal-r2-full-PERFQ-M10-DEM-PARSE-CONTRACT--flat-errors-small` then failed stably on its first attempt. Its paired median ratio was `1.3076769795904326x`, bootstrap 95 percent interval `[1.298935399719563x, 1.3537053081225807x]`, and paired relative MAD `0.006684815904312651`. Both workers parsed the same 64-item fixture for 2,621,056 exact work items with identical semantic output. The host passed load, affinity, governor, temperature, throttling, memory, and swap checks; replay passed, parity failed closed, swap was restored, and the report was not rerun.

The retained profile attributed `54.78` percent of Stab cycles to the generic `parse_dem_instruction` body, including repeated movement of large inline parser state, while `SourceCommands::next` accounted for another `11.91` percent. A separate coordinate-sparse profile after the first direct-path prototype attributed `23.63` percent to the stateful source scanner and showed generic Unicode-aware string searchers consuming material time on ASCII delimiters. Float parsing and semantic validation were real but secondary costs, so replacing numeric semantics or weakening validation was not justified.

The sixth optimization adds one production canonical-instruction path for all four DEM instruction kinds. It parses exact lowercase names, finite canonical argument lists, unescaped tags, and typed targets with byte cursors; runs the model's authoritative semantic validator before direct construction; and returns `None` on mixed case, escaped tags, unusual spacing, malformed values, overflow, or any uncertain form. Those cases continue through the same source-sliced diagnostic parser, preserving grammar, limits, error precedence, spans, and public errors. `SourceCommands` also returns a complete tagged line directly when no comment or block boundary exists, because tag state is irrelevant without those delimiters.

Focused equivalence tests compare every selected canonical instruction kind, arguments, tags, target forms, separator form, and integer boundary against the rich parser. Fallback tests cover mixed case, escaped tags, alternate spacing, lowercase targets, empty and out-of-range arguments, malformed separators, target-count errors, wrong target kinds, and integer overflow. Existing model tests continue to cover comments and hashes inside tags, braces, escaped and long UTF-8 tags, CRLF, diagnostics, allocation ceilings, repeat blocks, and all three qualification families.

Dirty-tree paired diagnostics preserved exact work and output identity. Flat-errors at 1,024 iterations by 4,096 items reported `stim_seconds=0.267209918`, `stab_seconds=0.281043032`, and ratio `1.051769x`; coordinate-sparse at the same shape reported `stim_seconds=0.284948495`, `stab_seconds=0.300613105`, and ratio `1.054973x`; and the failed small-scale calibration shape at 40,954 iterations by 64 items reported `stim_seconds=0.179649918`, `stab_seconds=0.174177328`, and ratio `0.969537x`. These are diagnostic guidance only. The sixth optimization still requires a clean source-contract commit, exact-head CI, regenerated correctness and worker evidence, and a first-attempt formal report at a new path before any parity claim can be restored.

## Sixth Optimization Evidence And Seventh Optimization

Clean revision `68db1796ab7a427d5e4a6f7be5679a3debaa89bb` bound the sixth optimization and passed exact-head GitHub Actions run `30774247780`. PR correctness passed 700 of 700 selected parents, broad full and soak each passed 933 of 933, all 11 exact full prerequisites passed and replayed, the live result-format corpus and implemented oracle matrix passed, and worker reproducibility produced 46 canonical receipts.

The complete 36-report DEM matrix then passed the unchanged Stim parity policy. Across parse and print, full and soak, three families, and three scales, the worst paired median was `1.1217977447638299x`, the worst bootstrap confidence upper bound was `1.1486983094725967x`, the best median was `0.4167423861390582x`, and the hottest accepted reading was 88800 millidegrees Celsius under the source-owned 100000-millidegree ceiling. Both parse rollups were produced and replayed.

The first source-owned Stab self-regression check rejected folded-repeats-small instead of allowing the broad parity margin to hide a product slowdown. Relative to the accepted architecture-specific baseline, its paired median deteriorated by `1.320574x` and its upper bound by `1.331252x`, both above the unchanged `1.15x` limit. The run stopped without a waiver, threshold change, or favorable rerun. All 36 reports and the two parse rollups remain immutable historical evidence under `68db1796`; they cannot be promoted by a later revision or reused as replacement paths.

Normalized Stim throughput remained stable while Stab's folded-small parse rose from approximately 38.3 to 51.0 microseconds per 64-item parse. Source inspection and exact-shape diagnostics isolated two architecture-extraction costs. Every folded cycle paid the richer stateful `SourceCommands` scanner for repeat headers and structural lines, and every parsed nested model paid an always-iterative destructor even at the fixture's shallow depth. The seventh optimization directly parses only exact lowercase canonical repeat headers, returns complete terminal block and closing-brace lines without a full state scan when their bytes prove that shortcut safe, and falls back to the authoritative parser for mixed case, escaped tags, alternate spacing, malformed values, overflow, comments, or inline structures. Model destruction now uses native shallow vector destruction through depth 31 and switches to the existing iterative algorithm at depth 32, preserving stack safety for arbitrarily deep programmatically constructed models.

Focused tests bind valid and fallback repeat-header forms, structural source-command admission, parser diagnostics, and a 4,096-level DEM dropped on a 64 KiB stack. The source-current code review found that the first shortcut returned a line-ending diagnostic span instead of the original terminal-brace span. The implementation now retains the brace span, and public parser regressions cover circuit and DEM missing-count diagnostics under LF, CRLF, and EOF termination while preserving their distinct established span conventions. CPU-0-pinned dirty guidance over the exact formal shapes improved folded small, medium, and large Stab elapsed medians by approximately 21, 20, and 16 percent versus the rejected clean revision. Flat-error and coordinate-sparse diagnostic shapes also improved modestly, and exact input and output digests remained unchanged. These measurements guide the fix but are not promotable evidence. The seventh optimization requires regenerated source-owned contracts, both audits, a focused clean commit, exact-head CI, and a complete evidence restart at unique paths.
