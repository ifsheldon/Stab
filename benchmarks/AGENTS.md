# Instructions for Benchmark Contracts

- Treat `manifest.csv` as the source-owned benchmark contract manifest for M3 and later performance work.
- Keep benchmark rows explicit about owning milestone, threshold class, runner, upstream source, phase, measurement family, and description.
- Use `contract-only` only when there is no direct pinned C++ executable runner yet, and keep those rows tied to an upstream source or future compatibility-matrix anchor.
- Keep compare-note prefixes aligned with the benchmark comparability taxonomy in `README.md`; every primary row must resolve to a machine-readable class before it can count as M12 evidence.
- Do not write generated benchmark run outputs in this directory; machine-specific artifacts belong under `target/benchmarks/`.
- Keep `stim-qualification-suite.json` checked in as the deterministic PQ0 source-owned contract, and update its frozen semantic digest only with reviewed source ownership changes.
- Keep benchmark source and output operations fail-closed on non-Unix until an equivalent handle-relative, no-reparse-point implementation exists; do not restore path-check-then-open fallbacks.
- When changing the manifest schema, runner meanings, generated artifact locations, or benchmark workflow, update `README.md`, the root operational docs, and the roadmap or spec-gap log as appropriate.

## Performance Qualification Contracts

- Use `just bench::qualification-list`, `just bench::qualification-check`, and `just bench::qualification-regenerate --check` for the PQ0 performance disposition ledger; update the frozen digest and checked inventory together when reviewed checklist, API, manifest, threshold, waiver, or upstream perf ownership changes.
- Record every reviewed legacy timing retirement in schema-version-2 `qualification-threshold-migrations.json`. Use `legacy_guard_kind: timing-threshold` only with both exact legacy measurements, use `legacy_guard_kind: no-ratio-waiver` only with both legacy measurements null, and use a null replacement scale only when one completion authorizes the complete checked scale family. Bind the exact authorization and migration revisions, inventory digests, completion report and preflight digests, replacement group and measurement, and retained memory baseline; do not reopen or weaken the source row after migration.
- Use `just bench::qualification-clifford-vectors --check` to verify the frozen schema-version-1 Clifford-string descriptors, gate order, exact outputs, and hostile-request matrix; regenerate and review the checked fixture whenever that source contract intentionally changes.
- Use `just bench::qualification-probe --group pq1-process-contract-smoke` and `just bench::qualification-probe --group pq1-adapter-protocol-smoke` to reproduce the bounded process and pinned-Stim adapter contracts independently. Product adapter probes include the circuit, gate, bit-kernel, sparse-XOR, transpose, Pauli, Clifford, and direct DEM groups documented in `README.md`; the DEM selectors are `pq2-dem-parse-adapter-smoke` and `pq2-dem-canonical-print-adapter-smoke`.
- Use `just bench::qualification-run --tier pr`, `--tier full`, or `--tier soak` to execute PQ1 paired evidence below `target/benchmarks/qualification/`, then use `just bench::qualification-report --input <report-directory>` and `just bench::qualification-parity --input <report-directory>` to revalidate derived evidence and source-owned Stim parity thresholds. The hidden `qualification-regression` command is a temporary deprecated alias for `qualification-parity`, not a Stab self-regression check.
- Use `just bench::qualification-self-regression --full <rollup> --soak <rollup>` only with accepted full and soak rollups from one group and identity. Missing architecture-specific baselines are `unseeded`, never passing. Generate a candidate with `just bench::qualification-baseline-candidate --full <rollup> --soak <rollup> --out <new-directory>` and review the candidate before changing the checked baseline file.
- The PQ1 `pq1-adapter-protocol-smoke` group is diagnostic infrastructure, never a product performance ratio: it does not accept correctness evidence, remains report-only, and cannot be promoted even from a clean verified full or soak run.
- PQ1 offline validation must compare reports to the currently checked correctness and performance inventory digests, replay the current pinned Rust toolchain, bind the report output directory, regenerate the exact preflight bytes, and use compare-and-swap publication so a stale refresh cannot overwrite newer evidence.
- Formal performance producer and replay artifacts must be direct children of their declared roots before any lock, private Git work, artifact read, or filesystem mutation occurs. Open and retain the repository descriptor before the first artifact, Git, source, build, or subprocess access; represent descriptor roots with typed ownership; perform repository-relative source and Git metadata access from duplicated handles without reopening the procfs file-descriptor magic link through a generic `O_NOFOLLOW` absolute-path walk; resolve all qualification source, Git, build, worker, probe, regression, report, rollup, and completion work through that retained root; and reserve the admitted absolute path for repeated proof that the same descriptor remains the live repository root. Publication must retain descriptor-backed identity, length, and digest bindings for the exact staged, target, source, and correctness artifact trees; retain every displaced child descriptor before exchange; reject same-inode mutation, substituted directories, or unexpected entries before and after exchange; propagate cleanup failures; revalidate sources and the repository after descriptor-checked displaced-tree cleanup; and durably synchronize the complete output hierarchy before success.
- Future product groups must reconstruct canonical CQ request, report, completion, preflight, and execution receipts. Cargo and property selector digests are hashes of their displayed selectors; oracle-fixture and ops-check digests are resolved source-contract digests frozen into the controller-approved request and must not be replaced by hashes of weaker display selectors.
- Performance correctness preflight must reject planned or deferred prerequisites, incomplete passing stream receipts, retained artifacts, missing exit status, invalid Cargo exact-test counts, and report-to-receipt attempt disagreement. It must globally reconstruct statistical ownership, unique `(case_id, seed)` attempts, complete planned and executed seed panels, and exact derived shot and seed aggregates before timing.
- PQ1 must recompute the pinned-Stim adapter build fingerprint from the recorded source, library, tools, and canonical compiler arguments; derive expected work in the parent; perform semantic preflight at the exact common calibrated batch shape; bind every subsequent validation, warmup, sample, and memory digest to that preflight; and audit tracked, staged, and untracked repository state through a private Git index reconstructed from `HEAD`. Calibration probes select the batch size and are work-bound but do not produce ratio evidence.
- Worker contract-preflight acceptance and rejection probes must use and bind the distinct `contract` evidence mode. Contract rows may report finite zero elapsed time because elapsed time is not statistical evidence; timing and memory rows must remain finite and strictly positive, and no statistic, formal report, regression, or threshold path may consume a contract row.
- Promotable future performance groups must bind exact clean CQ preflight evidence, equal semantic work units and semantic output, a clean unchanged revision, verified host policy, symmetric worker identities, full or soak samples, and a source-owned regression rule. Common-iteration groups must retain equal total work. The only current unequal-total-work exception is source-owned `independent-throughput` timing for `PERFQ-M6-CLIFFORD-STRING`, which requires independent stable batches, a common exact semantic preflight, normalized seconds-per-work ratios, and report-bound counts, work, and output digests. `--allow-unverified-host` is diagnostic only.
- The controlled PQ1 host profiles require stable thermal-zone identity and readings at or below 85000 millidegrees Celsius before and after a run in addition to affinity, load, memory, swap, and frequency-governor checks.
- PQ1 calibration targets at least 350 milliseconds so a separately executed common batch can jitter without silently crossing the contractual 250-millisecond acceptance floor; reports must retain both values and derive the validation mode from raw calibration decisions and common receipts. Standard mode rejects either common validation outside 250 milliseconds through 2 seconds. Wide-ratio mode may permit only the implementation that selected fewer independent iterations to exceed 2 seconds, while the common-iteration owner stays at or below 2 seconds, both implementations stay at or above 250 milliseconds, and neither exceeds the hard 20-second ceiling. Source-owned `independent-throughput` mode independently retains both implementations between 350 milliseconds and 2 seconds, validates exact common semantics at the smaller selected count, and normalizes timing by each implementation's exact declared work; it is valid only for the exact runtime group named by the checked inventory and runtime contract.
- File-writing qualification workers must wait on the start barrier before writing so the controller installs the child regular-file limit before releasing measured work; do not replace this with post-run file-size inspection.

## Benchmark Commands

- Use `just bench::list` to inspect M3 benchmark contracts, `just bench::smoke` to validate them without long runs, `just bench::baseline --stim vendor/stim` to record pinned C++ Stim baselines under `target/benchmarks/baseline/latest`, and `just bench::compare --milestone Mx` to inspect planned Stab-vs-Stim comparison rows.
- After committing changes to private worker sources, build inputs, or receipt policy, use `just bench::qualification-worker-reproducibility` from the clean unchanged commit. Each receipt must hash the ordered framed worker sources and frozen fixtures from the materialized source, the Stim adapter receipt must bind CMake's resolved `libstim` compile flags, and each sealed binary must confirm its source and build identity through the worker protocol. The canonical preflight derives one accepted small request per active runtime group and implementation plus three shared rejection classes per implementation. It currently contains 46 ordered receipts and must never exceed 128. Group-specific maxima, odd/even behavior, malformed requests, and overflow remain focused unit-test and adapter-probe obligations. Two isolated release builds must produce identical Stim and Stab source, build-fingerprint, binary-digest, and canonical-contract-preflight identities.
- Use `just bench::qualification-rollup --group <group> --tier <full-or-soak> --input <scale-report> ... --out <rollup-directory>` from the same clean committed revision recorded by the source reports to bind exactly one current promotable report per source-owned scale into an architecture-scoped family. A family must also share exact Stim and Stab source, build-fingerprint, binary-digest, and canonical-contract-preflight identities. Use `just bench::qualification-rollup-report --input <rollup-directory>` to replay the checked contract and source report and preflight digests, reject altered derived evidence, and atomically regenerate the rollup Markdown and preflight. Keep full and soak rollups separate, and never mix AArch64 and x86-64 reports.
- For the active DEM R6 scope, use `just bench::qualification-completion --scope dem-r6 --rollup <parse-full> --rollup <parse-soak> --rollup <print-full> --rollup <print-soak> --out <completion-directory>` from the clean source revision. The command replays the four rollups, validates all 36 source reports and their Stim parity outcomes, evaluates Stab self-regression as passed or unseeded, and binds repository, host, worker, inventory, policy, correctness, report, rollup, and memory identities into one schema-version-2 completion manifest. Use `just bench::qualification-completion-report --input <completion-directory>` for deterministic offline reconstruction. Schema-version-1 completion receipts remain read-only historical evidence. Milestone audit and independent code review remain human evidence and must not be represented as mechanically self-certified steps.
- Benchmark baseline `--out` paths must be repository-relative paths under `target/benchmarks/`, and `--only` filters must exactly match benchmark row ids or milestone names such as `M7`.
