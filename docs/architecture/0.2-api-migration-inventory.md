# Stab 0.2 API Migration Inventory

This ledger accounts for public Rust API changes made between the frozen pre-0.2 baseline and the coordinated Stab 0.2 release.

It is append-only during the migration.

The old path, replacement, rationale, and verification must be recorded before a public item is removed, renamed, or moved.

The complete discovered API remains owned by `oracle/qualification-manifest.json`; this document records intentional migration decisions instead of duplicating unchanged items.

## Status Values

- `migrated`: the old API has been removed or moved and its replacement is implemented and tested.
- `adapter`: a compatibility adapter remains temporarily while callers migrate.
- `pending`: the target replacement is specified but the old API remains current.
- `removed`: the old API had no useful supported replacement.

## Packed Bit Storage

| Pre-0.2 API | Stab 0.2 replacement | Status | Rationale | Verification |
| --- | --- | --- | --- | --- |
| `stab_core::bits::*` and root packed-storage types | Canonical `stab_bits::*` APIs and `stab_core::advanced::storage::*` facade paths | `migrated` | Records and algebra need a Stable leaf storage package without pulling in the core facade, CLI, or Nightly quantum kernels. Explicit storage belongs in the supported advanced tier instead of making implementation containers look like common facade values. | Stable Rust 1.97.1 check, tests, warnings-denied rustdoc, CQ2 bit contracts through the advanced facade, external consumer fixtures, architecture tier checks, and generated ownership |
| Crate-private `BitVec::words_mut() -> &mut [u64]` | Public guarded `BitVec::words_mut() -> BitWordsMut<'_>` | `migrated` | Cross-crate consumers need bounded mutable word access, but a raw slice could leave logical padding bits set. The guard masks the unused tail on drop and preserves the packed-vector invariant across early returns and errors. | `a3-bitvec-owned-storage-contract`, streaming result tests, stabilizer tests, and complete workspace tests |
| Generic `std::simd` packed-bit kernels in `stab-core` | Stable scalar kernels in `stab-bits` and optional fixed-block acceleration in dependency-free `stab-kernels-simd` | `migrated` | The storage crate must compile on Stable and remain usable by codecs without enabling Nightly. A narrow optional edge restores portable SIMD only for source-current measured dense XOR, while scalar tails and logical padding remain storage-owned. | Stable default and Nightly feature matrices, dense-XOR scalar equivalence, tail and allocation tests, architecture checks, and source-current scalar-versus-SIMD evidence |
| Crate-private Clifford portable-SIMD implementation in `stab-core` | Stable scalar Clifford composition in `stab-algebra` plus optional raw fixed-block composition in `stab-kernels-simd` | `migrated` | Algebra remains Stable by default while Nightly callers can select one dependency-free raw kernel. The raw result includes before-and-after non-identity masks so metadata remains exact without a second vector reload; engine backend policy remains separate. | Every 24-by-24 valid product, complete blocks and scalar tails, unequal widths, exact metadata counts, zero-allocation mutation, and source-current non-identity Clifford evidence |

## Diagnostics

| Pre-0.2 API | Stab 0.2 replacement | Status | Rationale | Verification |
| --- | --- | --- | --- | --- |
| `CircuitError::InvalidResultFormat { message }` | `CircuitError::InvalidResultFormat(FormatError)` plus `CircuitError::format_error` | `migrated` | Result-format failures need stable machine codes and exact source bytes without changing the established human error prefix. The typed payload retains the message and adds severity, an optional byte span, and operation-specific context. | Exact value, conversion, display, 01, HITS, and DETS span tests; complete result-format corpus |
| Unstructured result-format error strings | `ByteSpan`, `DiagnosticSeverity`, `FormatErrorCode`, `FormatErrorContext`, and `FormatError` | `migrated` | Agents and the JSON CLI renderer need a non-lossy diagnostic contract. Private fields prevent callers from constructing internally inconsistent payloads while stable accessors expose the public contract. | `result_format_diagnostic_value_contract_is_stable` and exact grammar-span tests |
| Circuit and DEM parser failures split across strings and line-only variants | `CircuitError::Parse(ParseError)`, `ParseErrorCode`, `ParseErrorContext`, and `CircuitError::parse_error` | `migrated` | Agents need exact source locations and stable classifications while humans retain established line-prefixed messages for behavior that was already compatible. The byte-oriented command cursor owns LF, CRLF, inline block boundaries, malformed source bytes, numeric, tag, target, repeat, and EOF locations. It preserves source-order failure precedence, treats Stim comment and tag payloads as opaque bytes, and exposes exact byte serializers and tag accessors instead of applying whole-input UTF-8 normalization. Pinned Stim v1.16.0 takes precedence where old Stab accepted an incompatible spelling or rejected a legal inline block transition. | Exact circuit and DEM spans, stable codes, malformed UTF-8 byte ranges outside opaque metadata, source-order failures, opaque comment and tag bytes, legacy message precedence, pinned inline-block and numeric boundaries, parser compatibility suites, and schema-version-1 CLI JSON |
| Facade-owned `ByteSpan`, `DiagnosticSeverity`, `ParseError`, `ParseErrorCode`, and `ParseErrorContext` definitions | Canonical `stab_model` definitions with `stab-core` compatibility reexports and lossless `ModelError::Parse` conversion | `adapter` | Circuit and DEM parsers report typed diagnostics without importing the Nightly facade. Private representation and bounded attacker-controlled excerpts are model-owned beside the parsers; format-specific diagnostics remain records-owned instead of being folded into the model error family. | Stable `stab-model` tests for byte arithmetic, UTF-8 spans, typed context, severity, and bounded text; complete parser diagnostics; facade conversion tests; warnings-denied rustdoc |
| Human-only CLI failures and deprecation warnings | Global `--error-format=human|json` with private schema-version-1 JSON Lines rendering | `migrated` | Agents need structured failures, but serialization dependencies and CLI argument policy do not belong in domain crates. The conservative raw-argument probe selects JSON only for one unambiguous request and leaves malformed arguments to Clap. | Exact human output, JSON schema and typed context, warning/error ordering, Clap, help/version, streaming absolute-span, writer-failure, and alias-preservation tests |

Packed `b8`, `r8`, and `ptb64` diagnostics use shared byte-length validation and one inlined zero-allocation `r8` record decoder across materialized and streaming consumers. Their existing human messages and acceptance boundaries remain unchanged while stable codes, exact offending-byte or EOF spans, and typed length or run contexts are exposed.

## Resource Policies And Estimates

| Pre-0.2 API | Stab 0.2 replacement | Status | Rationale | Verification |
| --- | --- | --- | --- | --- |
| Fixed circuit and DEM parser line and repeat-depth constants | `ParseLimits`, `SourceLineLimit`, `RepeatNestingLimit`, `Circuit::from_stim_str_with_limits`, and `DetectorErrorModel::from_dem_str_with_limits` | `migrated` | Line admission is configurable, while parsed models retain the non-overridable 256-level default envelope. Named quantities prevent swapped constructor arguments. Existing entry points delegate through the exact old defaults, source-line admission may be tightened or relaxed, and parser repeat admission may only be tightened until every required downstream consumer supports a larger parsed model. Existing programmatic-model APIs with deeper contracts remain consumer-specific and must use iterative traversal or reject before recursion. | Exact defaults, accepted maximum, first rejection, hard-ceiling rejection, circuit/DEM parity, unchanged compatible human messages, preallocation bounded by the admitted source prefix, and constrained-stack folded-summary construction for deeper programmatic DEMs |
| Untyped parser resource rejection strings | `ResourceLimitError`, `ResourceOperation`, `ResourceKind`, and `CircuitError::resource_limit_error` | `migrated` | Agents need the operation, resource dimension, actual amount, and limit without parsing English. One private typed cause now derives both the compatibility-preserving human message and schema-version-1 JSON context, preventing the two views from drifting. | Core value/context assertions and exact schema-version-1 CLI JSON |
| Fixed circuit and DEM flattening limits | Canonical `stab_analysis::CircuitFlattenLimits` and `stab_analysis::DemFlattenLimits`, policy-aware analysis entry points, and `stab-core` compatibility aliases | `migrated` | Explicitly materializing transforms need caller-selected output and traversal budgets without coupling the circuit and DEM operations. Both flatten policies and their typed resource causes now live beside their extracted Stable transforms, while `stab-core` preserves its aggregate error and public aliases. Existing entry points delegate through the canonical limits. | Default equivalence, exact maximum, first rejection, overflow, admission-before-output, typed resource context, and lossless facade conversion |
| Fixed detection conversion limits | `DetectionConversionLimits` plus policy-aware compile, materialized, sampling, streaming, width, count, and validation entry points | `migrated` | Per-record width, returned in-memory data, repeat unroll, expanded instruction work, aggregate repeat work, retained terms, and compiled-plan bytes grow independently. Record width, materialized data, and repeat unroll preserve their historical defaults. Audit replaced unsafe representational aggregate defaults with finite operation-safety defaults because compact repeated input could otherwise drive unbounded traversal or retention. One operation-owned policy keeps frame and reference conversion paths aligned. | Existing-entry-point equivalence, independent dimensions, real practical default boundaries, nested repeats, streaming behavior, allocation boundaries, compiled-plan admission, and typed resource context |
| Fixed DEM sampler work and buffer limits | `DemSamplerLimits` plus policy-aware materialized, replay, and streaming methods | `migrated` | Stochastic error applications, replay traversal, returned in-memory units, and the active byte footprint are distinct costs. Replay callers do not charge borrowed input as returned output, while a separate work budget and the existing active-footprint boundary preserve resource safety and default admission. Streaming callers avoid total output limits without bypassing execution-work admission. | Seeded equivalence, replay, exact maxima, first rejection, RNG-before-admission checks, bounded streaming, and typed resource context |
| Test-substituted and fixed logical-error search budgets | Canonical `stab_analysis::LogicalErrorSearchLimits`, graphlike and hypergraph policy entry points, and `stab-core` compatibility aliases | `migrated` | Traversal, graph construction, hyperedge retention, and search frontier state have separate growth dimensions. The policy, folded traversal, graph construction, and search algorithms now live together in the Stable analysis owner, while production defaults apply in tests and releases alike and callers can choose bounded research searches explicitly. | Default semantic equivalence, every budget boundary, preflight-before-retention, graph and hypergraph searches, precise typed resource kinds, and lossless facade conversion |
| Fixed SAT traversal, CNF, and output limits | Canonical `stab_analysis::SatMaterializationLimits`, policy-aware shortest and likeliest SAT entry points, and `stab-core` compatibility aliases | `migrated` | SAT generation needs independent traversal, mechanism, target, variable, clause, literal, and serialized-output admission before large allocations. The policy and typed failures live beside the Stable SAT implementation while the facade preserves existing root paths and aggregate errors. | Exact defaults, every dimension's accepted maximum and first rejection, two-pass traversal admission, output sizing, typed resource context, and lossless facade conversion |
| No pre-execution parse estimate | `Estimate`, `EstimateClass`, `ResourceEstimate`, `ResourceEstimate::builder`, `ResourceEstimateBuilder`, `ParseLimits::estimate`, and `ParseLimits::estimate_bytes` | `migrated` | Inspection must distinguish facts from guesses. Text and byte estimates report original input bytes and physical lines exactly, including opaque metadata; semantic expansion, memory, output, and work remain `Unknown` until their owning operation can estimate them honestly. The builder lets an owning operation name only defensible quantities without adding operation-specific constructors to the model crate. The upper-bound vocabulary is reserved for the first operation that can produce a defensible bound and is not independently promoted by a manufactured test value. | LF, CRLF, opaque bytes, empty, unterminated-line, exact, and unknown classification assertions |
| Facade-owned parser-limit and estimate definitions plus `From<stab_records::EncodedSizeEstimate<T>> for Estimate<T>` | Canonical `stab_model::{ParseLimits, SourceLineLimit, RepeatNestingLimit, Estimate, EstimateClass, ResourceEstimate}` definitions with facade reexports; explicit conversion at records/model composition sites | `adapter` | Stable model parsing and downstream planning need one dependency-safe vocabulary. A model-to-records dependency would invert the product graph, and Rust's orphan rules prohibit recreating the generic `From` implementation once both types are owned by independent crates. Explicit two-variant conversion keeps the boundary visible and preserves unknown-size semantics without coupling either leaf package. | Stable model boundary and estimate tests; facade policy-equivalence tests; sampling plan JSON tests; complete core and CLI suites |
| Facade-only circuit and DEM parser resource causes | Canonical `stab_model::{ResourceLimitError, ResourceLimitContext, ResourceOperation, ResourceKind}` for parse admission plus exhaustive conversion into the broader facade `ResourceLimitError` | `adapter` | The Stable parsers must reject source-line and repeat-depth excess without importing analysis and execution resource vocabulary. Four model causes cover the only current parse budgets; the closed context enum forces a coordinated facade mapping whenever that set changes, while preserving exact existing error strings and typed core fields. | Direct four-cause Stable model contract; circuit and DEM accepted-boundary and first-rejection tests; exact core operation, dimension, values, span, and human-display assertions |
| Flat model-construction variants directly on `ModelError` | Canonical closed `stab_model::ValidationError` with stable codes and severity, aggregated by `ModelError::Validation`, with exhaustive conversion into established facade `CircuitError` variants | `adapter` | Parse, resource admission, and structural validation are different caller actions. A dedicated owner-domain family lets Stable consumers branch without parsing messages, while keeping the variant set closed makes every future validation cause a compile-time facade-conversion obligation. Circuit count, coordinate, and detector-query failures are model validation even where the compatibility facade historically grouped them under result-format or DEM errors. | Real oversized-ID and unknown-gate failures; all gate argument and target validation classes; count, coordinate, and detector-query regressions; exact facade conversion and complete model/core suites |

Hard limits for target representation, DEM numeric values, detector identifiers, PTB64 shot grouping, generator domains, stabilizer algebra, fixed CLI I/O, and still-partial analysis fallbacks are deliberately absent from these policies. Parser preallocation samples at most a fixed prefix and is capped by admitted lines, so rejected trailing input cannot inflate allocation before admission. [The A2 resource policy inventory](a2-resource-policy-inventory.md) records every reviewed family and the rationale for keeping each boundary configurable or fixed.

## Fingerprints And Capabilities

| Pre-0.2 API | Stab 0.2 replacement | Status | Rationale | Verification |
| --- | --- | --- | --- | --- |
| No canonical model identity | Canonical `stab_model::{ModelFingerprint, ModelDialect}`, `Circuit::fingerprint`, and `DetectorErrorModel::fingerprint`, with facade reexports | `adapter` | Agents need a reproducible content identity that is independent of input comments, whitespace, case, and compatibility-printer precision while remaining domain-separated and cryptographically collision-resistant across circuit and DEM dialects. Schema one hashes an explicit domain, schema, dialect, and streaming structural encoding with architecture-independent lengths, fixed-width values, exact finite float bits, and normalized signed zero. Printer text is deliberately excluded because its Stim-compatible rounding can merge distinct probabilities and its formatting is not an identity contract. | Independently reconstructed rich circuit and DEM digests, canonical-input equivalence, high-precision mutation separation, signed-zero normalization, deterministic repetition, cross-dialect separation, zero allocation through the admitted repeat envelope, bounded depth-only spill beyond it, and direct Stable model tests |
| No backend-neutral compilation identity | Canonical `stab_engine::{CompilationRequestFingerprint, CompilationOperation}` with `stab-core` compatibility reexports | `migrated` | Agents need to distinguish canonical source identity from the semantic request presented to a compiler. Sampling schema one binds the model fingerprint, operation, and explicit compiler schema while truthfully encoding zero caller options and zero configurable compile limits. Shots, seed, reference mode, result format, paths, and backend identity remain outside this layer. | Independently reconstructed rich request digest, canonical source equivalence, model mutation separation, exact metadata, zero-allocation generation, generated alias ownership, and direct benchmark caller migration |
| Facade-owned biased bit randomization helper | Canonical `stab_engine::biased_randomize_bits` with a `stab-core` compatibility reexport | `migrated` | Probability-driven execution belongs with the mutable engine rather than the facade or model. The utility accepts a model-owned validated probability, caller-owned words, and caller-owned RNG without importing codecs, filesystems, CLI types, or operational policy. | Deterministic boundary cases, seeded probability sensitivity, canonical ownership checks, and the direct-match pinned Stim `m8-probability-util` benchmark |
| No backend-bearing executable identity | `PlanFingerprint` and `SamplingBackend` | `migrated` | A backend-neutral request does not identify the executable variant selected after lowering. Plan schema one binds the request fingerprint, selected backend, executable-contract schema, and private direct-Z, small-frame, or general-frame identity without claiming serialization or mutable-session identity. | Independently reconstructed scalar direct-Z vector, backend and executable-schema separation, deterministic compilation, and unavailable-backend rejection |
| Scattered gate, codec, and compiler discovery | `CapabilitySet`, `CodecCapability`, `CompilationCapability`, `RecordFormat`, and `RecordEncoding` | `migrated` | Runtime discovery must follow product descriptors instead of qualification inventories or a second status manifest. Gates come from the closed gate table, codec facts come from the records-owned registry, and all four compiler registrations are colocated with engine ownership. A nullable request-fingerprint schema distinguishes implemented compilers that do not yet expose that identity from absent compilers. Scalar remains the only sampling backend because A6's build-time leaf acceleration does not create a second executable sampling plan. | Exact descriptor-source equality, executable compiler registrations, unique codec names, encoding and grouping facts, typed-layout requirements, compiler and optional request-fingerprint schemas, parse defaults, and exact scalar backend registration |
| Parse-only estimates | `RecordFormat::estimate_output_bytes` and `estimate_sampling_request` using `ResourceEstimate` | `migrated` | Records own fixed-width encoded-size formulas, while the facade assembles the sampling estimate through the domain-neutral model builder using exact structural and expanded-operation counts without compiling or executing. Sparse output size, runtime work, scratch, and resident memory remain unknown because current code cannot estimate them defensibly. | Folded repeat counts, expanded counts, fixed-width and sparse formats, PTB64 grouping, overflow-to-unknown behavior, and zero-allocation estimation |
| Raw `MeasureRecordWriter` and `MeasureRecordBatchWriter` output plumbing | `MeasurementSink`, `DetectionSink`, `DemSampleSink`, and their typed codec implementations; legacy writers under `stab_core::advanced::records` | `adapter` | The Stable component boundary needs semantic widths, separate detector and observable planes, sink-owned finalization, and preserved sink errors. Execution emits typed bit-plane batches directly, while byte-oriented writers remain available for CLI and migration code without occupying the common facade root. | Exact bytes for every format, semantic width rejection, detector/observable separation, PTB64 finalization, first-error cancellation, duplicate-heavy allocation bounds, advanced-facade consumer compilation, and generated ownership |
| Decoder output through owned batches or unrestricted mutable storage | `ObservablePredictionBatch::view_prefix_mut` and `ObservablePredictionBatchViewMut` | `migrated` | Reusable decoder and pipeline sessions need caller-owned prediction storage sized for a maximum engine batch while exposing only the active shot prefix. The correction-typed view checks each write against that prefix and cannot address reusable suffix records. | Prefix width and shot metadata, admitted writes, complete-record replacement, out-of-prefix and width rejection, zero-width and zero-shot prefixes, and unchanged suffix sentinels |
| No agent-facing discovery or dry-run commands | Stab-native `capabilities` schema version 3 plus `inspect` and `plan sample` schema version 2 with private CLI serializers | `migrated` | Agents need runtime discovery and request validation without reading qualification prose or executing probabilistic work. Successful output is descriptor-driven, inspection stays parse-only, and planning compiles only for validation while keeping run configuration separate from compiler identity. Schema two added selected scalar backend and backend-bearing plan identity instead of silently reinterpreting schema one's reserved null field. Capabilities schema three adds the complete closed compiler-family set and explicit absence of a request-fingerprint schema where none is public. | Exact descriptor generation, circuit and DEM inspection, type inference and admission, request and plan identity, deterministic no-execution planning, compile rejection, PTB64 grouping, one-shot herald visibility, and schema-selecting help output |

Model fingerprints identify canonical source models only. Compilation request fingerprints add operation and compiler identity before backend selection. Plan fingerprints add selected backend and executable-contract identity. None describes mutable sessions or serialized plans.

## Decoder Interoperability

| Pre-0.2 API | Stab 0.2 replacement | Status | Rationale | Verification |
| --- | --- | --- | --- | --- |
| No stable decoder composition boundary | Canonical `stab_decoder::{DecoderModelView, DecoderLayout, DecoderInputBatchView, DecodeCancellation, DecoderSession, ValidatedDecodeBatch, decode_batch}` contracts with curated `stab-core` root reexports | `migrated` | Independent decoder crates need exact DEM identity, truth-hidden detector input, caller-owned prediction storage, preflight, cancellation, and progress without importing the simulator facade or sharing one speculative compiler abstraction. The generic session stays statically dispatched, while each implementation owns compilation and retained representation. | Dimension rejection before dispatch or mutation, prediction-prefix isolation, pre-cancellation, committed-prefix cancellation and failure, malformed summaries, fixed session layout, zero shots, Stable external consumer compilation, bounded exact-ML conformance, generated repetition circuits, and a public-only sample-to-detect-to-decode experiment |

## Sampling Plans And Sessions

| Pre-0.2 API | Stab 0.2 replacement | Status | Rationale | Verification |
| --- | --- | --- | --- | --- |
| `CompiledSampler` as compiler, state owner, materializer, and encoder | `stab_engine::SamplingCompiler` -> immutable `SamplingPlan` -> mutable `SamplingSession` -> `MeasurementSink`; legacy adapter at `stab_core::advanced::compat::CompiledSampler` | `adapter` | Agents and library callers need a shareable compilation product, isolated reusable random state, explicit cancellation and failure progress, and composition through typed batches. Combining those roles in one convenience type hid allocation, backend, and sink lifecycle boundaries. The canonical implementation and typed composition API are physically owned by `stab-engine`; the materialized adapter remains supported without occupying the common root. | Typed invalid-circuit and unavailable-backend diagnostics, plan sharing, session isolation, seeded chunking, zero-shot behavior, batch-boundary cancellation, exact sink failure progress, poison semantics, reference modes, frozen and differential direct/small/general streams, warmed session and codec allocation bounds, advanced-facade consumer checks, generated ownership checks, and source-current M8 evidence |
| Sampler byte encoders inside execution | `advanced::compat::CompiledSampler` methods over sessions and records-owned sinks | `adapter` | Existing byte and materialized methods remain useful for migration, but result encoding must not be an engine dependency, a second execution path, or the architectural center of the common root. | Exact legacy sampling bytes for all supported formats, PTB64 grouping, CLI sample parity, and no codec or filesystem import in the execution path |
| CLI `sample` callback and per-format output paths | One `SamplingSession` run into a CLI-owned typed `MeasurementSink` | `migrated` | The command should exercise the public composition seam, retain file-role safety, and bound output scratch to one 64-shot batch or one PTB64 group. A zero-shot request performs alias identity checks without opening, reading, creating, or truncating explicit paths, matching Stim's no-I/O shortcut while preserving Stab's stronger explicit-alias rejection. | Existing CLI sample matrix, seeded exact output, write and flush failures, skip-reference behavior, zero-shot invalid input and path identity, allocation-stable wide formats, and format-specific output |
| `CompiledDetectionConverter` as conversion compiler, reusable state, materializer, and visitor | `stab_engine::MeasurementToDetectionCompiler` -> immutable `MeasurementToDetectionPlan` -> mutable `MeasurementToDetectionSession` -> sink-bound `MeasurementToDetectionSinkAdapter` -> `DetectionSink`; legacy adapter at `stab_core::advanced::compat::CompiledDetectionConverter` | `adapter` | Conversion plans are shareable, while reference samples, sweep records, conversion scratch, progress, cancellation, and poison state belong to isolated sessions. The canonical compiler, plan, session, converter kernel, and typed adapter are physically owned by `stab-engine`; the materialized adapter remains discoverable without presenting itself as the primary root API. | Exact materialized equivalence, sweep and reference modes, typed adapter routing, record-at-a-time valid-prefix delivery, double-finish and post-finish rejection, exact finish-failure progress, abandoned-delivery poisoning, whole-batch cancellation, sink failures, fallible reference scratch, zero post-warmup allocation, advanced-facade consumer checks, generated ownership checks, and source-current M9 evidence |
| Circuit detection sampling through materialized records or command-specific visitors | `stab_engine::DetectionSamplingCompiler` -> immutable `DetectionSamplingPlan` -> mutable `DetectionSamplingSession` -> `DetectionSink` | `migrated` | Detection sampling is physically engine-owned through one public composition seam while preserving direct detector-frame execution and fused sample-convert execution as distinct private variants. `stab_core::execution` directly reexports these canonical compiler, plan, session, progress, summary, and error identities; only the legacy materializer and callback API remain facade adapters. Variant choice, random state, conversion state, progress, cancellation, and poisoning remain operation-owned. The direct detector-frame plan strips nonsemantic tags and charges its complete retained conversion and executable-circuit representation before fallible materialization. Core and CLI materializers delegate to this owner. | Exact facade-to-engine type identity, exact legacy seeded output, private-variant equivalence, 64-shot partitioning, same-session chunking, cancellation, failure progress, width metadata, aggregate fused-session storage admission, exact direct-frame compiled-byte admission, bounded allocation, generated ownership checks, and source-current M9 evidence |
| `CompiledDemSampler` as compiler, RNG owner, materializer, replay engine, and visitor | `stab_engine::DemSamplingCompiler` -> immutable `DemSamplingPlan` -> mutable `DemSamplingSession` or incremental `DemReplaySession` -> `DemSampleSink`; legacy adapter at `stab_core::advanced::compat::CompiledDemSampler` | `adapter` | Detector-only sampling, sampled-error sampling, and replay have different work and random-consumption contracts. They share one engine-owned lowered immutable program and typed detector, observable, and optional sampled-error planes without being collapsed into one opaque algorithm. `stab_core::execution` directly reexports the canonical engine identities, and `CompiledDemSampler::plan` and `session` expose those same values while preserving facade error conversion only in materialized and callback conveniences. | Exact facade-to-engine type identity, exact materialized equivalence across DEM families, seeded partitioning, replay without RNG advance, replay admission before record traversal, replay abandonment, cancellation before delivery and at finish, exact malformed-prefix progress, active-byte batch admission, sink failure progress, poisoning, zero post-warmup allocation, advanced-facade consumer checks, generated ownership checks, and source-current M11 evidence |
| CLI-specific `detect`, `m2d`, and `sample_dem` streaming visitors and output routing | CLI-owned typed `DetectionSink` and `DemSampleSink` encoders fed by the public A5 sessions | `migrated` | Commands retain filesystem preflight and byte-format policy, while execution owns only semantic batches. `m2d` deliberately submits one initial record at a time so malformed later input preserves a valid output prefix; `sample_dem` validates and rewinds retained replay input before any output activation. | Existing command parity matrices, detector and observable side outputs, PTB64 groups, sampled-error output, replay, writer failures, path aliases, malformed later records, and output-before-replay safety |

## Algebra And Gate Semantics

| Pre-0.2 API | Stab 0.2 replacement | Status | Rationale | Verification |
| --- | --- | --- | --- | --- |
| Root typed IDs, probabilities, and targets constructed through facade-owned `CircuitError` | Canonical `stab_model::{QubitId, ObservableId, MeasureRecordOffset, CircuitDetectorId, RepeatCount, DemRepeatCount, Probability, Pauli, Target}` values returning `ModelError`; `stab-core` retains type reexports and lossless conversion into `CircuitError` | `adapter` | Stable model consumers need foundational Stim values without pulling in records, execution, CLI, or the Nightly facade. Direct model constructors report their owning domain error, while facade operations convert every current variant back into the established aggregate error shape. | Stable `stab-model` check, exact representational boundaries, parsed `rec[-0]` preservation, typed target-family parsing, facade CQ2 boundaries, and complete workspace compile |
| Facade-owned `Gate`, gate metadata enums, `GateDecomposition`, and closed raw descriptors | Canonical `stab_model::{Gate, GateCategory, GateArgumentRule, GateTargetRule, GateTargetGroupKind, GateDecomposition}` roots plus parser and raw-descriptor operations under `stab_model::advanced`; `stab-core` retains compatibility reexports | `adapter` | Gate syntax is Stable model data. Keeping the registry in the Nightly facade prevented model parsing and external inspection without the simulator, while moving semantic tableau or flow interpretation into model would reverse the algebra and analysis dependencies. Model construction now reports `ModelError`; facade parsing maps it losslessly into `CircuitError`. | Exact 82-name Stim hash corpus in `stab-model`, canonical aliases and metadata, model-owned validation errors, raw descriptor checks, all core gate semantic contracts, parser fast-path equivalence, and Gate lookup/circuit parse benchmarks |
| `stab_core::stabilizers::*` and root algebra values | Canonical `stab_algebra::*`; common owned algebra values remain at the facade root, while iterators and admitted constructors use `stab_core::advanced::algebra` | `migrated` | Stable model, analysis, and decoder consumers need Pauli, Clifford, tableau, and flow mathematics without compiling the Nightly facade. The owner-shaped module is removed, while common values remain ergonomic and lower-level traversal is visibly advanced. | Stable Rust 1.97.1 check, focused algebra tests through canonical and facade paths, external consumer fixtures, warnings-denied Clippy and rustdoc, and architecture enforcement |
| `SingleQubitClifford::from_gate(Gate)` | `analysis::single_qubit_clifford_for_gate(Gate)` and facade reexport | `migrated` | A closed-dialect gate-to-algebra conversion depends on both the model and algebra. Keeping it on the algebra value made the algebra layer import the Stim gate model. | Exhaustive mapping of all 24 single-qubit Cliffords plus rejection of `CX`; complete `stab-core` tests |
| `TryFrom<Gate> for SingleQubitClifford` | `analysis::single_qubit_clifford_for_gate(Gate)` | `migrated` | The trait implementation created the same reversed model dependency while hiding that conversion is a semantic lowering operation. The named adapter makes the boundary explicit. | Same exhaustive semantic-adapter test and all existing sampler, analyzer, tracker, simplifier, inversion, and tableau tests |
| `Gate::tableau` | `analysis::gate_tableau` | `migrated` | Tableau lookup combines the closed gate model with algebra and therefore belongs to semantic analysis rather than gate syntax metadata. | Gate-table tableau fixtures and all gate metadata tests |
| `Gate::has_tableau` | `analysis::gate_has_tableau` | `migrated` | Capability discovery must follow the same semantic owner as tableau construction. | Exhaustive named-gate capability assertions |
| `Gate::flows` | `analysis::gate_flows` | `migrated` | Flow derivation consumes gate metadata and algebra values. | Exact Stim flow metadata and derived-tableau flow tests |
| `Gate::has_flows` | `analysis::gate_has_flows` | `migrated` | Capability discovery must not make the model own flow algebra. | Exhaustive named-gate capability assertions |
| Root `GateUnitaryMatrix` definition | `analysis::GateUnitaryMatrix`, with a facade reexport | `adapter` | The complex-valued matrix is the result of a semantic projection, while the model owns only closed scalar gate descriptors. | Exact matrix entries, dimensions, conversion, and amplitude tests |
| `Gate::unitary_matrix` | `analysis::gate_unitary_matrix` | `migrated` | Fixed-shape matrix semantics are an analysis projection of a closed gate. | Exact unitary metadata tests |
| `Gate::has_unitary_matrix` | `analysis::gate_has_unitary_matrix` | `migrated` | Capability discovery follows the unitary semantic adapter. | Exhaustive named-gate capability assertions |
| `Gate::h_s_cx_m_r_decomposition` | `analysis::gate_h_s_cx_m_r_decomposition` | `migrated` | Decomposition is an explicit lowering, not gate syntax data. | Exact decomposition metadata and circuit decomposition tests |
| `Gate::has_h_s_cx_m_r_decomposition` | `analysis::gate_has_h_s_cx_m_r_decomposition` | `migrated` | Capability discovery follows the decomposition semantic adapter. | Exhaustive named-gate capability assertions |
| `GateDecomposition::to_circuit` | `analysis::gate_decomposition_to_circuit` | `migrated` | Parsing static decomposition metadata into a validated circuit crosses from gate descriptors into model parsing and belongs to semantic analysis. | Exact decomposition text, parsed circuit, and simplifier tests |

The A6 extraction removed the `Gate` and `GateDecomposition` method adapters after every product, test, and benchmark caller migrated to the named analysis functions. The facade continues to reexport the owning functions during the coordinated pre-1.0 migration.

The physical analysis slices make `stab_analysis::{single_qubit_clifford_for_gate, gate_tableau, gate_flows, gate_unitary_matrix, gate_h_s_cx_m_r_decomposition, gate_decomposition_to_circuit, circuit_without_tags, circuit_to_tableau, simplified_circuit, decomposed_circuit, GateUnitaryMatrix, AnalysisError}` the canonical component API. Existing `stab_core` root and `stab_core::analysis` paths remain compatibility facades, while public `stab_analysis::gate` and `stab_analysis::circuit` module paths are qualification-tracked aliases of the curated crate root.

## Model And Filesystem Boundaries

| Pre-0.2 API | Stab 0.2 replacement | Status | Rationale | Verification |
| --- | --- | --- | --- | --- |
| Facade-owned `Circuit`, `CircuitInstruction`, `CircuitItem`, `RepeatBlock`, `DetectorErrorModel`, `DemInstruction`, `DemItem`, `DemRepeatBlock`, and related iterators and identifiers | Canonical `stab_model` values with `stab-core` compatibility reexports and lossless aggregate-error conversion at facade call sites | `adapter` | Stable analysis, engine, decoder, and external tooling need exact Stim models without importing execution or Nightly. Moving storage together with parsing, printing, tags, compact traversal, structural queries, and fingerprints prevents parallel model implementations; filesystem policy and operation-specific algorithms stay outside the model. | Stable model tests for exact Stim and DEM syntax, parser fast paths and diagnostics, mutation and iteration, structural counts and coordinates, opaque metadata, compact traversal, frozen fingerprints, resource boundaries, complete core compatibility tests, and regenerated alias ownership |
| Decoder-specific flattening or private folded traversal of DEM error instructions | `DetectorErrorModel::try_visit_error_mechanisms` with `DemErrorMechanismTraversalLimits`, `DemErrorMechanismView`, and typed absolute `DemErrorTarget` values | `migrated` | Repeats, detector shifts, separators, and target identity are model semantics and must not be reimplemented independently by each decoder. The narrow Stable visitor preflights represented mechanism count, skips error-free repeats, bounds represented instruction work, and avoids allocating shifted target vectors; consumers retain ownership of parity, graph, probability, and compilation policy. | Flat and nested repeats, zero-count and error-free repeat pruning, shifted absolute detectors, duplicate and separator preservation, logical observables, mechanism preflight before callbacks, represented-work rejection, and immediate visitor stop |
| `Circuit::from_stim_file` and `Circuit::write_stim_file` | Facade-owned `read_stim_circuit_file` and `write_stim_circuit_file`; model-safe `Circuit::{from_stim_bytes, write_stim_io}` | `migrated` | Stable model crates should parse bytes and serialize into caller-owned writers without importing filesystem paths, opening files, choosing truncation behavior, or owning path security policy. The facade retains the established 64 MiB read cap, canonical streaming output, and aggregate I/O diagnostics. | Exact canonical bytes including opaque tags and repeats, missing-input and missing-parent errors, parse diagnostics, first over-cap rejection, and allocation-stable writer behavior |

## Circuit Analysis And Execution

| Pre-0.2 API | Stab 0.2 replacement | Status | Rationale | Verification |
| --- | --- | --- | --- | --- |
| `Circuit::to_tableau` | `analysis::circuit_to_tableau` | `migrated` | Tableau conversion is pure analysis over a circuit model. | Circuit tableau and gate-semantics suites |
| `Circuit::inverse_unitary` | `analysis::circuit_inverse_unitary` | `migrated` | Circuit inversion is a pure transform. | Unitary inverse exact and round-trip tests |
| `Circuit::inverse_qec` | `analysis::circuit_inverse_qec` | `migrated` | QEC inversion is a pure transform with an explicit unsupported-surface contract. | QEC inverse compatibility tests |
| `Circuit::inverse_qec_with_options` | `analysis::circuit_inverse_qec_with_options` | `migrated` | Options belong to the transform call instead of mutable model behavior. | Option-specific inverse tests |
| `Circuit::time_reversed_for_flows` | `analysis::circuit_time_reversed_for_flows` | `migrated` | Flow-driven reversal combines model and algebra without executing shots. | Flow time-reversal tests |
| `Circuit::time_reversed_for_flows_with_options` | `analysis::circuit_time_reversed_for_flows_with_options` | `migrated` | Explicit transform options belong to analysis. | Option and failure-boundary tests |
| `Circuit::simplified` | `analysis::simplified_circuit` | `migrated` | Simplification is a pure circuit pass. | Exact simplification fixtures |
| `Circuit::without_tags` | `analysis::circuit_without_tags` | `migrated` | Recursive tag removal is an explicit structure-preserving transform, not model state. | Recursive tag, instruction-boundary, and non-mutation tests |
| `Circuit::flattened` | `analysis::flattened_circuit` | `migrated` | Repeat expansion and coordinate application are explicit transformations. | Flattening, overflow, and resource-bound tests |
| `Circuit::flattened_operations` | `analysis::flattened_circuit_operations` | `migrated` | The owned flattened instruction stream is an analysis result, not model state. | Instruction-order and fusion-boundary tests |
| `Circuit::without_noise` | `analysis::circuit_without_noise` | `migrated` | Noise stripping is an explicit transform used by analysis and execution lowering. | Exact record-preservation and recursive-transform tests |
| `Circuit::decomposed` | `analysis::decomposed_circuit` | `migrated` | Gate decomposition is an explicit lowering pass. | Exact base-gate decomposition tests |
| `Circuit::with_inlined_feedback` | `analysis::circuit_with_inlined_feedback` | `migrated` | Feedback rewriting is an explicit transform. | Exact rewrite, unsupported-input, and detection-conversion tests |
| Raw `u64` detecting-region tick inputs and map keys | Canonical `stab_model::CircuitTick` values in `stab_analysis::{DetectingRegionOptions, DetectingRegionTargetOptions, DetectingRegionMap, DetectingRegionTargetMap}`; `stab-core` retains raw-tick compatibility DTOs and converts at the facade boundary | `adapter` | Tick indices are a stable circuit domain and should not be interchangeable with detector IDs, repeat counts, or arbitrary counters in component APIs. The facade preserves the established aggregate API while canonical Stable analysis becomes type-safe. | Direct typed analysis tests for selection, bounds, map lookup, generated helpers, and exact regions plus unchanged facade tests using raw ticks |
| `Circuit::reference_sample` | `execution::circuit_reference_sample` | `migrated` | A reference sample compiles and executes simulator state. | Deterministic reference-sample and sweep-aware tests |
| `Circuit::reference_sample_tree` | `stab_engine::ReferenceSampleTree::from_circuit_reference_sample`, with `execution::circuit_reference_sample_tree` as the facade adapter | `migrated` | Reference-tree construction is engine-owned because it derives from simulator-backed reference sampling; the facade method preserves aggregate-error compatibility. | Canonical construction equivalence, tree lookup, prefix, logical-size, storage-admission, and facade error-conversion tests |
| `Circuit::count_determined_measurements` | `execution::count_determined_measurements` | `migrated` | Determinism counting runs the frame simulator and is execution behavior. | Determined-measurement semantics and resource-bound tests |
| Root `CompiledSampler` type and methods | `stab_engine::{SamplingCompiler, SamplingPlan, SamplingSession}` and root plan/session reexports; legacy adapter at `stab_core::advanced::compat::CompiledSampler` | `adapter` | Compilation, immutable executable identity, mutable random state, and typed output delivery are distinct execution concerns. The root now leads callers toward the canonical architecture, while the pre-0.2 materialized and callback adapter remains available in an explicit migration tier. Additive `try_sample_*` and `try_for_each_*` methods preserve execution and allocation failures; source-compatible infallible adapters retain their historical signatures with explicit panic contracts. | Sampler semantics, seeded fallible/infallible equivalence, pre-execution overflow rejection with zero progress, cancellation and poisoning, bounded warmed allocation, typed sink composition, advanced-facade external consumer compilation, generated public-API ownership, and source-current M8 evidence |
| Root `ReferenceSampleTree` type and methods | `stab_engine::{ReferenceSampleTree, ReferenceSampleTreeError}`, with facade reexports and aggregate-error adapters | `adapter` | The tree is derived from simulator-backed reference sampling, so its definition and operations are physically engine-owned. Checked construction owns exact logical-size and nesting admission, tree fields are private, random access is iterative, and materialization is fallible instead of saturating size or panicking during allocation. The current compatibility implementation bounds and materializes admitted reference bits; optimized folded construction for enormous compact repeats remains future work and is not represented as parity with Stim's folded-tree benchmark. | Checked construction, simplification, iterative random access and decompression, exact logical-size overflow, maximum nesting and first rejection, allocation failure before destination mutation, construction equivalence, 256 MiB storage admission, facade conversion, and generated public-API alias validation |
| Root `count_determined_measurements` | `execution::count_determined_measurements`, with a facade reexport | `adapter` | The free function runs frame-simulator work and shares the execution owner with the compatibility method. | Determined-measurement semantics, resource boundaries, and exact namespace/root alias ownership |
| Root `sample_if_circuit_has_stabilizer_flows` | `stab_engine::sample_if_circuit_has_stabilizer_flows` with typed `ShotCount`, `RandomPolicy`, and `SampledFlowError`; `stab_core::execution::sample_if_circuit_has_stabilizer_flows` preserves the former facade signature | `adapter` | Sampled flow checking compiles and samples a noiseless circuit and therefore is execution-backed rather than pure flow analysis. The engine owns flow ordering, 256-shot batching, per-flow seeded streams, storage admission, and typed failures; the facade owns only legacy input and aggregate-error conversion. | Unitary/record, observable, inverted, unsupported, storage, rounding, seeded-equivalence, facade-error, and exact namespace/root alias tests |

These method adapters were removed during A6 after all source-owned callers migrated to the owning namespaces. Remaining root entries in this table are explicit facade reexports or compatibility types, not methods implemented on model-owned values.

## Detector Error Model Analysis

| Pre-0.2 API | Stab 0.2 replacement | Status | Rationale | Verification |
| --- | --- | --- | --- | --- |
| `DetectorErrorModel::without_tags` | `analysis::detector_error_model_without_tags` | `migrated` | Recursive tag stripping is a compact model transform and does not belong to syntax storage. | Nested-repeat preservation, non-mutation, and exact printing tests |
| `DetectorErrorModel::flattened` | `analysis::flattened_detector_error_model` | `migrated` | Repeat expansion and detector-shift application are materializing analysis with explicit admission limits. | Exact shifted output, overflow, and first-rejection tests |
| `DetectorErrorModel::rounded` | `analysis::rounded_detector_error_model` | `migrated` | Probability rounding is an explicit semantic transform over error instructions. | Pinned Stim rounding, zero retention, tag preservation, and folded-repeat tests |

The model-owned folded traversal exposes only structural summaries and deterministic compact block identities. Search eligibility, ErrorMatcher folding, and SAT nonzero-probability policies are owned by their analysis consumers so the model does not accumulate algorithm-specific caches.

## Error Matching

| Pre-0.2 API | Stab 0.2 replacement | Status | Rationale | Verification |
| --- | --- | --- | --- | --- |
| Root `explain_errors_from_circuit` | `stab_analysis::explain_errors_from_circuit`; the root function remains as a facade adapter | `adapter` | Matching is pure circuit and DEM analysis. The facade maps `AnalysisError` losslessly into the established `CircuitError`. | Pinned matcher corpus, generated-code provenance, filter traversal, resource admission, and facade exact-output tests |
| Root `ExplainedError`, `CircuitErrorLocation`, and `CircuitTargetsInsideInstruction` | Canonical `stab_analysis` values; explicit root wrappers remain | `adapter` | Their historical mutating methods return `CircuitResult`, so direct aliases would silently change public error signatures. The wrappers retain that error boundary, but read-only formatting and comparison use analysis-owned borrowed views instead of cloning model-sized vectors into temporary canonical DTOs. | Canonical ordering and formatting tests, focused facade success and error conversion, exact facade comparison behavior, and zero-allocation borrowed formatting and comparison |
| Root `CircuitErrorLocationStackFrame`, `DemTargetWithCoords`, `FlippedMeasurement`, and `GateTargetWithCoords` | Canonical `stab_analysis` values with facade reexports | `migrated` | These simple provenance values have no facade-specific error signature and need one type identity. | Generated public-API alias ownership and matched-error value tests |
