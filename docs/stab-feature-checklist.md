# Stab Feature Checklist Against Stim v1.16.0

> Historical assessment: this checklist is retained as a detailed P0 input, not the current scope or status source. Use the generated [Stim parity view](stim-parity.md), its source ledger at `../oracle/stim-v1.16-parity.toml`, and the active [GOAL](plans/GOAL.md).

This checklist maps the Stim v1.16.0 inventory in [stim-feature-list.md](stim-feature-list.md) onto the current Stab codebase.
It is a feature-availability document, not a new roadmap.
The former generated counts, qualification contracts, and implementation roadmap remain linked for historical research only. They do not override the active ledger, plan, or GOAL.

Historical status vocabulary used by this retained assessment:

- `Done`: implemented for the current Rust core or CLI surface and backed by source, tests, oracle rows, reports, or benchmark evidence.
- `Reopened`: implemented, but a prior qualification claim has been withdrawn until the named remediation and fresh evidence are complete.
- `Partial`: implemented for an explicit subset, with remaining Stim parity gaps or public API gaps named.
- `Deferred`: intentionally outside the current beta surface or future-plan scope.
- `Missing`: no implementation found in the current Stab tree and not clearly documented as deferred.

## 1. Top-Level Product Surface

| Stim feature area | Stab status | Evidence and notes |
| --- | --- | --- |
| Rust core library equivalent for core Stim semantics | Done for selected Rust API scope | The `stab-model`, `stab-records`, `stab-algebra`, `stab-analysis`, `stab-engine`, and `stab-decoder` component crates own the selected product APIs; [../crates/stab-core/src/lib.rs](../crates/stab-core/src/lib.rs) is a curated facade over those owners. PFM-B1 through PFM-B5 close every non-deferred Rust blocker in the historical source-owned ledger. Python object shape, public interactive simulator products, diagrams, full ErrorMatcher provenance, and other explicitly deferred products are not part of this selected Rust status. |
| CLI binary | Done for selected Stab CLI surface | `stab-cli` implements `stab gen`, `stab convert`, `stab sample`, `stab detect`, `stab m2d`, `stab analyze_errors`, `stab sample_dem`, Stab-native `help`, agent-facing `capabilities`, `inspect`, and `plan sample`, and the additive global `--error-format=human|json` diagnostic mode. Human output remains the default; the agent commands and JSON modes are Stab extensions and do not claim Stim output parity. `capabilities` is generated from source-owned descriptors, `inspect` never executes, and `plan sample` compiles the sole scalar plan for validation without sampling. `portable-simd` remains build-time leaf acceleration and is not advertised as a selectable backend. Section 11 records each selected Stim-compatible command as done or explicitly deferred. The binary is named `stab`; drop-in `stim` packaging or aliasing remains outside this checklist unless the release plan adds it. |
| `.stim`, `.dem`, and result-format compatibility | Partial | Selected `.stim`, `.dem`, and result-format contracts are implemented. Clean revision `68d107a42f655254f31628f0cbedc55479f6c0f3` passed PR, full, and soak correctness qualification plus the live 62-case pinned-Stim corpus after the strict shared text grammar, typed DETS namespace, duplicate semantics, CLI propagation, and path-role repairs. Explicitly partial and deferred semantics remain named in their own rows. |
| Python package `stim` | Deferred | Python bindings are explicitly future work in [plans/rust-stim-drop-in-rewrite.md](plans/rust-stim-drop-in-rewrite.md). No `pyo3`, `maturin`, Python package, or Python API compatibility layer is present. |
| JavaScript/WASM API | Deferred | JS/WASM is explicitly future work in [plans/rust-stim-drop-in-rewrite.md](plans/rust-stim-drop-in-rewrite.md). |
| Ecosystem integrations | Deferred | `stimcirq`, `sinter`, Crumble, `stimflow`, ZX, lattice-surgery helpers, QASM, and Quirk are future or out-of-scope ecosystem surfaces. |
| GPU backend | Deferred | GPU acceleration is explicitly future work, and the current project focuses on CPU portable SIMD first. |

## 2. File Formats And IO Contracts

### 2.1 `.stim` Circuit Format

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| Line-based `.stim` parsing, comments, tags, args, targets, and repeat blocks | Done | WS3 of [plans/post-review-remediation-plan.md](plans/post-review-remediation-plan.md) restored Stim-legal `REPEAT(args)` headers (arguments lex through the shared parser and are discarded like pinned Stim) with the `m4-parser-repeat-args-accept` oracle row, after the earlier bare braceless `REPEAT` repair with the missing-brace diagnostic, pre-fix-failing regressions, and a constructor backstop; formal qualification rerun remains pending. Implemented in [../crates/stab-model/src/circuit.rs](../crates/stab-model/src/circuit.rs), [../crates/stab-model/src/target.rs](../crates/stab-model/src/target.rs), and [../crates/stab-model/src/gate/mod.rs](../crates/stab-model/src/gate/mod.rs). M4 completion evidence is in [plans/m4-completion-report.md](plans/m4-completion-report.md). |
| Canonical `.stim` printing | Done | `Circuit::to_stim_string` is implemented in [../crates/stab-model/src/circuit.rs](../crates/stab-model/src/circuit.rs). M4 reports canonical printer oracle and benchmark evidence. |
| Target kinds | Partial | WS3 of [plans/post-review-remediation-plan.md](plans/post-review-remediation-plan.md) repaired combiner tokenization: attached and Stim-legal spaced combiner forms all parse to the same circuit, misplaced combiners reject at gate validation like pinned Stim, and pre-fix-failing regressions pin both sets; formal qualification rerun remains pending. Qubit, inverted qubit, measurement record, sweep bit, Pauli, inverted Pauli, and combiner parsing are implemented in [../crates/stab-model/src/target.rs](../crates/stab-model/src/target.rs). Sweep-conditioned `m2d` conversion, non-frame and selected frame-path `detect` sampling with omitted all-false sweep bits, and selected analyzer sweep-control no-op plus invalid target-position rejection behavior are implemented for current execution subsets. Sampler-backed execution now rejects parsed `CX q sweep[k]` and `CY q sweep[k]` target order while preserving accepted sweep-first `CX` or `CY` and both-order `CZ` sweep/qubit cases. The selected analyzer subset includes `CZ` sweep/sweep, record/sweep, sweep/record, and record/record classical-only no-op groups, with the boundary locked in [plans/pfm3-analyzer-sweep-boundary-scope.md](plans/pfm3-analyzer-sweep-boundary-scope.md). The selected frame-path `detect` subset includes sweep-controlled `CX`, `CY`, `CZ`, qubit/sweep `XCZ` or `YCZ` no-op groups, and measurement-record `XCZ` or `YCZ` feedback equivalent to `CX` or `CY` feedback, while the analyzer sweep boundary is evidence-closed by `pfm3-analyzer-sweep-matrix`; future shapes require a new failing pinned oracle or an explicit public-surface plan revision; broader sweep target shapes remain partial. Pinned Stim v1.16.0 does not expose `stim detect --sweep`; typed detector-sampler sweep inputs belong to deferred Python or future explicit Rust API work, not current CLI parity. |
| Broadcast and validation behavior | Done | WS3 of [plans/post-review-remediation-plan.md](plans/post-review-remediation-plan.md) widened `E`/`ELSE_CORRELATED_ERROR` to accept combiner and inverted Pauli targets as ignored decoration like pinned Stim (every consumer skips combiners and drops inversion, matching the vendor frame simulator), pinned by decorated-target regressions, the fold-path DEM-equality suite, and the `m4-parser-correlated-error-decorations` oracle row; block-only gates already refuse instruction construction after the earlier WS3 repair; formal qualification rerun remains pending. Gate arity, target rules, Pauli product grouping, disjoint segments, probabilities, and typed arguments are covered by [../crates/stab-model/src/gate/mod.rs](../crates/stab-model/src/gate/mod.rs), [../crates/stab-model/src/circuit.rs](../crates/stab-model/src/circuit.rs), and M4 tests. |
| Full semantic execution of every legal circuit operation | Partial | Implemented where owned by sampler, detection, analyzer, generation, and algebra milestones. Unsupported or deferred semantics are documented in the relevant sections below. |

### 2.2 `.dem` Detector Error Model Format

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| DEM parser and canonical printer | Done | Implemented in [../crates/stab-model/src/dem.rs](../crates/stab-model/src/dem.rs) through `DetectorErrorModel::from_dem_str` and `DetectorErrorModel::to_dem_string`. Text parsing follows Stim's ASCII lexical boundaries, treats empty argument lists as one zero argument where the instruction permits it, accepts zero-count DEM repeat blocks, and rejects textual integers at or above $2^{60}$. Exact oracle coverage lives in `m10-dem-parse-print-exact`, structural coverage in `m10-dem-parse-print-basic` and `coverage-py-dem`, and PF4 malformed-input and public-constructor validation coverage. Broader DEM transform, traversal, introspection, and full API parity gaps remain tracked by their own rows. |
| DEM instruction types | Done for core types | `error`, `detector`, `logical_observable`, `shift_detectors`, separators, and repeat blocks are implemented in [../crates/stab-model/src/dem.rs](../crates/stab-model/src/dem.rs), including Stim-compatible exact-one-target validation for `detector` and `logical_observable`. |
| DEM detector shifts, observables, coordinates, and counts | Done for selected Rust API scope | PFM-B3 routes detector, observable, error, final-shift, and coordinate queries through one checked compact traversal with exact nested-repeat, deterministic differential, overflow, sparse selected-lookup, and first-declaration evidence. Selected coordinates use declaration bounds, algebraic flat and bounded-nested scans, and selected repeat iterations without flattening preceding repeats. The unfiltered map retains its one-million-entry cap because Stim returns every detector index below `count_detectors()`, including empty coordinate vectors; remaining Python API shape is deferred. See [plans/pfm-b3-folded-dem-traversal-progress-report.md](plans/pfm-b3-folded-dem-traversal-progress-report.md). |
| DEM flattening and large repeat traversal | Done for selected bounded-result Rust consumers | PFM-B3 provides one checked folded tree and visitor for counts, coordinates, sampler compilation, graphlike and hypergraph collection, SAT/WCNF collection, and ErrorMatcher filter keys. Coordinate vectors are opt-in, neutral repeats skip before caps, eligible zero-shift repeats fold, and selected coordinate iterations are chosen algebraically. Compact `rounded` and `without_tags` transforms intentionally recurse directly so they allocate only their output body. `DetectorErrorModel::flattened`, full coordinate maps, sampled-error records and replay, shifted active search mechanisms, SAT clauses, and full ErrorMatcher provenance remain capped or deferred because their output or search state is inherently expanded; those caps are tested separately from compact-input traversal. See [plans/pfm-b3-folded-dem-traversal-progress-report.md](plans/pfm-b3-folded-dem-traversal-progress-report.md). |

### 2.3 Shot And Result Data Formats

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| `01` dense text | Done for selected Rust and CLI scope | The shared byte lexer requires LF or CRLF after every record across materialized, streaming, convert, replay, and `m2d` paths. Clean PR, full, and soak qualification plus the live checked corpus cover exact records, CR/LF boundaries, malformed termination, and consumer propagation. |
| `b8` dense binary | Done for selected Rust and CLI scope | Exact CQ2 qualification covers byte packing, non-byte-aligned padding, 64/128/256/504/2048-bit records, large tables, conversion, and width rejection. |
| `r8` sparse binary | Done for selected Rust and CLI scope | Exact CQ2 qualification covers sparse run encoding, long gaps, malformed terminators and overflows, conversion, and large-table round trips. |
| `hits` sparse text index format | Done for selected Rust and CLI scope | The shared byte lexer rejects malformed separators and unterminated records while preserving selected HITS duplicate contracts across materialized and streaming consumers. Clean PR, full, and soak qualification plus the live pinned corpus cover the grammar and propagation. |
| `dets` sparse text | Done for selected Rust and CLI scope | Layout-aware readers preserve independent M/D/L namespaces; dense and packed consumers set duplicate bits, typed visitors preserve source order and duplicates, `SparseShot` applies parity only to observables, and width-only APIs reject D/L tokens. Clean PR, full, and soak qualification plus the live pinned corpus cover strict grammar, namespace bounds, duplicates, and consumer propagation. |
| `ptb64` transposed packed format | Done for selected Rust and CLI scope | Exact CQ2 qualification covers 64-shot grouping, 64-bit word layout, non-word-aligned widths, validation, large tables, conversion, and relevant CLI paths. `m2d --out_format=ptb64` and `--obs_out_format=ptb64` intentionally reject like pinned Stim v1.16.0. |
| Format conversion command coverage | Done for selected CLI scope | WS3 of [plans/post-review-remediation-plan.md](plans/post-review-remediation-plan.md) made `--in_format` required like pinned Stim (the silent `01` default is gone), with a regression asserting exit status, stderr class, and that no output path is touched; formal qualification rerun remains pending. The command surface covers `.stim -> .stim` canonicalization and result conversion across `01`, `b8`, `r8`, HITS, DETS, and `ptb64`. Conversion uses the shared strict lexer and typed DETS layout, explicit input/output aliases fail before truncation, and clean PR, full, and soak qualification covers the repaired path. |
| Streaming IO for large implemented CLI paths | Done for current post-beta surfaces | `stab sample`, `stab sample_dem`, `stab detect`, and implemented `stab m2d` paths stream output or input in bounded chunks. See [plans/post-beta-fix-report.md](plans/post-beta-fix-report.md). |

## 3. Gate And Instruction Surface

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| 81 canonical Stim v1.16.0 gates | Done for registry and validation | `GATES` in [../crates/stab-model/src/gate/mod.rs](../crates/stab-model/src/gate/mod.rs) contains the canonical table excluding `NOT_A_GATE`, with categories, argument rules, target rules, and inverse names. |
| Parser aliases | Done | Aliases such as `MZ`, `MRZ`, `RZ`, `CNOT`, `ZCX`, `H_XZ`, `CORRELATED_ERROR`, `SQRT_Z`, and `SWAPCZ` are handled in [../crates/stab-model/src/gate/mod.rs](../crates/stab-model/src/gate/mod.rs). |
| Gate validation flags and categories | Done for current Rust metadata surface | Stab has typed validation for argument counts, target categories, grouping, probabilities, and selected metadata. Rust `Gate` exposes aliases, argument rules, target rules, target grouping, fusing, noisy/reset/measurement/unitary/single-qubit/two-qubit/target-capability/symmetry flags, unitary inverse, generalized inverse, local Clifford tableau metadata, Stim v1.16.0 `GateData.flows` metadata for tableau-backed unitary gates plus representative measurement-rich and variable-target gates, fixed-shape one- or two-qubit `GateUnitaryMatrix` metadata, and Stim v1.16.0 H/S/CX/M/R `GateDecomposition` metadata for the owned PF1 subset, with `pf1-gate-metadata-api` evidence. Full Python `GateData` object parity remains deferred, and execution support stays tracked separately in the gate semantic row. |
| Gate semantic execution | Done for selected Rust/CLI scope | WS1 of [plans/post-review-remediation-plan.md](plans/post-review-remediation-plan.md) repaired the detector-frame product-measurement collapse (whole-product frame multiplication) and the noiseless reference contract, with pre-fix-failing regressions and pinned-Stim verification; formal qualification rerun remains pending the plan's end-of-plan evidence. PFM-B2 classifies all 81 canonical gates across parser, measurement sampler, reference sampler, detection converter, detector frame, detection sampler, error analyzer, and flow-generator surfaces, with 22 typed target patterns and no unknown state. Thirty-seven independently selectable `pfm3-contract-*` cases cover all nineteen semantic families through exact, structural, rejection, state-equivalence, semantic-invariant, or source-owned statistical comparators; `just oracle::run --milestone PF3` and `just oracle::blockers --check-selectors` pass. Review remediation strengthens fixed-tableau inverse equivalence, grouped pair-measurement inversion, pinned four-body MPP invariants, controlled-Pauli directionality and sweep behavior, independent noisy-gate effects, heralded records, and anti-Hermitian rejection. Public interactive simulator products and exact random-stream parity remain deferred and do not keep this selected execution surface partial. See [plans/pfm-b2-gate-surface-progress-report.md](plans/pfm-b2-gate-surface-progress-report.md). |

PFM-B2 contract groundwork classifies all 81 canonical gates across parser, measurement sampler, reference sampler, detection converter, detector frame, detection sampler, error analyzer, and flow-generator surfaces, maps accepted target groups back to typed roles including Hermitian or anti-Hermitian products, and gives each ledger case a machine-readable eight-surface coverage set.
The final 37-case execution matrix turns that contract integrity into completion evidence for all nineteen semantic families; the groundwork report remains historical context and [plans/pfm-b2-gate-surface-progress-report.md](plans/pfm-b2-gate-surface-progress-report.md) is authoritative for closure.

## 4. Core Circuit Features

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| Circuit construction from parsed text | Done | `Circuit::from_stim_str`, item storage, repeat blocks, instructions, tags, and canonical printing are implemented in [../crates/stab-model/src/circuit.rs](../crates/stab-model/src/circuit.rs). |
| Programmatic mutation | Done for selected Rust API scope | WS6 item 7 of [plans/post-review-remediation-plan.md](plans/post-review-remediation-plan.md) hardened deeply nested API-built circuits: `Circuit` now mirrors the detector error model's iterative `Drop`/`Clone`/`PartialEq` (with a bounded shallow fast path) and a depth-elided `Debug`, so a 4,096-level nest constructs, clones, compares, and drops on a 64 KiB stack where the derived recursion aborted the process (pre-fix SIGABRT reproduced); formal qualification rerun remains pending. `append_instruction`, `append_repeat_block`, `append_from_stim_text`, `append_from_stim_program_text`, `from_stim_bytes`, `write_stim_io`, `append_circuit`, `concatenated`, `repeated`, `repeat_in_place`, `insert_item`, `insert_instruction`, `insert_repeat_block`, `insert_circuit`, `pop_item`, `pop_last_item`, iterative `Clone`, and `clear` exist and are covered by `pf1-circuit-rust-api`. Filesystem opening and truncation remain caller policy rather than a model or facade API. Python operator ergonomics remain deferred. |
| Core introspection | Done for current Rust API surface | `Circuit::count_qubits` preserves pinned Stim's target-based behavior, including its treatment of `MPAD` values, while execution owners use a separate semantic simulated width that excludes pads. Standalone, repeat-nested, depth, and breadth regressions own both concepts; formal qualification rerun remains pending. `count_qubits`, top-level `len`/`is_empty`, `iter_items`, `item_range`, `instruction_range`, lazy flattened forward and reverse instruction iterators, `count_measurements`, `count_detectors`, `count_observables`, `count_ticks`, `count_sweep_bits`, `measurement_record_count`, `detection_record_width`, and related helpers have exact CQ2 Circuit API ownership. Aggregate count methods return checked `Result` errors on repeat-count overflow; this is an intentional Rust API divergence from pinned C++ helpers that saturate at `UINT64_MAX`, not an unimplemented parity case. Python binding-style indexing and full Python property parity are deferred. |
| Circuit coordinate queries | Done for current Rust API surface | Coordinates are parsed and used in analyzer and detection workflows. Rust `Circuit` exposes folded `final_coordinate_shift`, `final_qubit_coordinates`, `detector_coordinates`, `detector_coordinates_for`, and `coordinates_of_detector` using `CircuitDetectorId`, with `pf1-circuit-rust-api` coverage. Rust coordinate queries reject non-finite folded coordinate results as documented hardening; exact Python-style API shape and exact C++ infinity behavior remain deferred or logged. |
| Repeat handling | Partial | Repeat blocks parse, print, sample, analyze, convert, and participate in the Rust `Circuit::flattened`, `Circuit::flattened_operations`, and `Circuit::without_noise` transform subset. Materialized flattening is capped at one million output operations while shift-only repeats are folded; fully folded traversal across every transform remains partial. |
| Circuit transforms | Done for the nondeferred Rust scope | `decomposed_circuit` is the sole public base-gate lowering route and covers product measurements, Pauli products, all metadata-backed Clifford templates, and legal classical target orientations. Strict unitary inversion covers SPP, annotations, coordinate shifts, and compact repeats. `inverse_qec` and flow time reversal share one sparse reverse-flow engine across measurement, reset, detector, observable, noise, herald, generated-code, and bounded repeat semantics. Duplicate reset targets are segmented and return self-validating flows instead of Stim's out-of-range record indexes; `rec[-0]` is rejected instead of rewritten. Feedback inlining owns Stim's accepted record controls and compact repeats, with XCZ/YCZ support recorded as a Stab extension. Exact current contracts and divergences are generated in [stim-parity.md](stim-parity.md). Deferred exports remain listed separately. |
| Typed circuit-pass extension seam | Done and source-current qualified for the implemented A8 surface | Canonical `stab_analysis::{CircuitPass, CircuitPassContext, CircuitPassLimits, CircuitPassInput, CircuitPassResources, CircuitPassStage, CircuitPassOutput, CircuitPassProjectionError, CircuitPassError, run_circuit_pass}` admits represented items, targets, arguments, projected logical payload bytes, and repeat nesting before dispatch, admits each implementation's checked output projection before lowering, and validates the actual closed-dialect result against both caller limits and the projection before release. Payload bytes deliberately exclude allocator metadata and spare collection capacity; they bound proportional model payload rather than exact resident memory. `without_noise` is the built-in adapter, while the unpublished Stable `stab-reference-noise-pass` crate proves deterministic external X-noise insertion without changing the gate table, execution IR, or unrelated model code. Exact PR, full, and soak correctness, the controlled external-pass diagnostic, built-in continuity, final audits, and GitHub Actions run `30732340918` close A8. The extension is owned only by `stab-analysis`; the removed `stab_core::experimental` facade path has no compatibility replacement. Dynamic plugins, runtime gate registration, and placeholder GPU APIs remain absent. |
| Reference samples and determined measurements | Done for current Rust API surface | WS1 of [plans/post-review-remediation-plan.md](plans/post-review-remediation-plan.md) aligned this surface with pinned Stim: references are strictly noiseless, `count_determined_measurements` ignores flip arguments and rejects `MPAD`/heralded records with a typed error, and panicking wrappers are absent (see [MIGRATING-0.2.md](MIGRATING-0.2.md)); formal qualification rerun remains pending. `SamplingPlan::try_reference_sample`, the all-zero-sweep `circuit_reference_sample` operation, `ReferenceSampleTree::from_circuit_reference_sample`, `SamplingPlan::try_count_determined_measurements`, and the free `count_determined_measurements` helper expose the current Rust subset. Python bit-packed return shapes and full Python API parity are deferred. |
| Circuit exports | Deferred | QASM, Quirk, Crumble, and diagram exports are explicitly future work. |
| Built-in circuit generation | Done for current CLI and core generator surface | Repetition-code, rotated/unrotated surface-code, and color-code generation are implemented in [../crates/stab-analysis/src/circuit_generation.rs](../crates/stab-analysis/src/circuit_generation.rs) and `stab gen`. Materializing entry points preflight a 131,072 projected-physical-qubit cap before allocation: repetition accepts all distances through 2047, rotated surface accepts through 256, unrotated surface through 181, and color accepts valid odd distances through 341. Pinned Stim's nominal distance domain extends through 2047, so larger surface and color rejection is a deliberate Stab resource boundary. See [plans/m7-completion-report.md](plans/m7-completion-report.md). |

## 5. Detector Error Model Features

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| DEM construction and mutation | Done for current Rust API surface | `DetectorErrorModel::new`, `len`, `is_empty`, `clear`, `append_from_dem_text`, `push_instruction`, `push_repeat_block`, `DemInstruction` constructors, `DemRepeatBlock::new`, typed `Probability`, typed zero-capable `DemRepeatCount`, and derived `Clone` exist and are covered by `pf1-dem-rust-api` plus the exact DEM qualification case. `DemRepeatCount` is intentionally distinct from the nonzero circuit `RepeatCount` because Stim accepts `repeat 0` in detector error models. Python-style list operations, concatenation operators, repetition operators, and exact Python API shape remain deferred. |
| DEM introspection | Done for selected Rust API scope | Detector, observable, error, and final-shift summaries; full and selected coordinate maps; typed item and range iteration; and lazy adjusted flattened iteration are implemented. PFM-B3 supplies shared folded traversal and resource evidence for the bounded-result queries. Python binding shape and deferred diagram products are not active Rust blockers. |
| DEM transforms | Done for selected Rust API scope | `without_tags` and `rounded` use direct compact recursive transforms, preserve repeat structure, allocate only the transformed body plus recursion state, and retain programmatic depth-257 behavior; rounding changes only error probabilities and retains rounded-to-zero mechanisms. `flattened` and `iter_flattened_instructions` preserve adjusted detector and coordinate semantics, with the materialized `flattened` result retaining its explicit expansion cap. Python operators, diagrams, and other deferred product surfaces remain outside this Rust closure. |
| DEM sampling | Done for current Rust and CLI surface | `DemSamplingCompiler`, its immutable plan, mutable sampling or replay session, typed `DemSampleSink`, and `stab sample_dem` support deterministic, noisy statistical, observable side output, sampled-error output, replay, and current result formats. PFM-B3 compiles through the shared folded traversal; direct detector sampling keeps compact repeat structure and parity-folds eligible zero-shift stochastic bodies. Sampled-error delivery and replay keep Stim-compatible flat error-bit order and therefore retain application and replay work caps for inherently expanded shapes. See [../crates/stab-engine/src/dem_sampling](../crates/stab-engine/src/dem_sampling), [../crates/stab-cli/src/sample_dem.rs](../crates/stab-cli/src/sample_dem.rs), [plans/m11-progress-report.md](plans/m11-progress-report.md), [plans/rpf4-dem-sampler-progress-report.md](plans/rpf4-dem-sampler-progress-report.md), and [plans/pfm-b3-folded-dem-traversal-progress-report.md](plans/pfm-b3-folded-dem-traversal-progress-report.md). |
| Streaming DEM sampling | Done for current Rust and CLI surface | `DemSamplingPlan` creates owned `DemSamplingSession` and `DemReplaySession` values. Sampling calls borrow a sink for one run; incremental replay binds exactly one sink for the lexical lifetime of a short-lived `DemReplayTransaction`, and reusable sessions retain no caller borrow afterward. Both paths deliver bounded typed batches and stop on sink errors or cancellation. `stab sample_dem` streams detector output, observable side output, sampled-error output, and replayed-error copying through bounded writers. The removed pre-0.2 materializer no longer creates a second execution route or total-output budget. Exact Python `CompiledDemSampler.sample_write` API shape remains deferred under the sampler and converter classes row. See [plans/post-beta-fix-report.md](plans/post-beta-fix-report.md) and [plans/pfm0-sampling-streaming-evidence-lock.md](plans/pfm0-sampling-streaming-evidence-lock.md). |
| DEM analysis and shortest graphlike error | Done for the nondeferred Rust scope | Graphlike and hypergraph search, shortest-error and likeliest-error SAT/WCNF production, and ErrorMatcher filtering share the compact DEM traversal owner. Likeliest-error WCNF repeat semantics are cross-checked exhaustively against direct optimization over small nested and shifted models instead of relying on encoder round trips. Sparse detector ids and typed graph, transition, incidence, and materialization limits remain explicit. Full ErrorMatcher provenance, diagrams, and Python product shapes remain deferred. |
| DEM diagrams | Deferred | Match-graph SVG, 3D, and HTML diagrams remain future work. |

## 6. Sampling, Conversion, And Simulation Features

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| Compiled measurement sampling | Done for current Rust and CLI scope | WS3 of [plans/post-review-remediation-plan.md](plans/post-review-remediation-plan.md) repaired broken-pipe handling and `--skip_reference_sample` behavior. `SamplingCompiler`, immutable `SamplingPlan`, reusable `SamplingSession`, typed `MeasurementSink`, and `stab sample` form the current architecture. Nested repeats are retained in one validated flat operation tape with fixed-depth compilation and execution. `ReferenceSampleLoopPolicy::Fold` reuses repeat output only after measured reuse admission, exact stabilizer-state recurrence, and bounded snapshot admission; a storage-ineligible candidate falls back to iteration without changing circuit admission or seeded output. `Iterate` and `--skip_loop_folding` always disable reuse. Plans report their fixed scalar backend, loop policy, and executable identity; sessions preserve seeded chunking, cancellation, exact progress, poison semantics, and bounded 64-shot delivery. P2 removed caller-selectable unavailable backends and the pre-0.2 `CompiledSampler` materialization, byte, and callback adapters, leaving one compiler-plan-session-sink route. |
| Measurement-sampler Python API parity | Deferred | No Python `CompiledMeasurementSampler` binding is implemented. |
| Detector sampling | Done for selected Rust and CLI surface | WS1 of [plans/post-review-remediation-plan.md](plans/post-review-remediation-plan.md) repaired whole-product collapse and the noiseless reference for this surface with pre-fix-failing regressions verified against pinned Stim; formal qualification rerun remains pending. `DetectionSamplingCompiler`, its immutable plan, mutable session, typed `DetectionSink`, and `stab detect` form the current route in `stab-engine` and [../crates/stab-cli/src/detection.rs](../crates/stab-cli/src/detection.rs). Non-frame and selected frame-path sweep-conditioned circuits use omitted all-false sweep bits for the current execution subset, including frame-path Pauli-observable circuits with sweep-controlled `CX` and `CY` qubit targets, sweep-controlled `CZ`, `CZ` bit/bit no-op groups, qubit/sweep `XCZ` or `YCZ` no-op groups, and measurement-record `XCZ` or `YCZ` feedback equivalent to `CX` or `CY` feedback. Non-frame detection sampling validation rejects Stim-parsed `CX q sweep[k]` and `CY q sweep[k]` target order like pinned Stim execution. Pinned Stim v1.16.0 has no `stim detect --sweep` flag, so the current CLI surface is closed for sweep input. Exact Python `CompiledDetectorSampler` class shape, sweep-bit API parity, and any future explicit Rust compiled detector-sampler product remain deferred under the sampler and converter classes row. See [plans/m9-completion-report.md](plans/m9-completion-report.md), [plans/rpf3-sweep-gate-progress-report.md](plans/rpf3-sweep-gate-progress-report.md), and [plans/pfm0-sampling-streaming-evidence-lock.md](plans/pfm0-sampling-streaming-evidence-lock.md). |
| Measurement-to-detection conversion | Done for current Rust and CLI scope | `MeasurementToDetectionCompiler`, its immutable plan, mutable session, sink-bound transaction, and `stab m2d` use one engine-owned path. The transaction binds exactly one sink across incremental writes and finalization; the pre-0.2 whole-output and callback converter is removed. The conversion plan retains folded bodies once, resolves relative record offsets at execution, preserves detector order and observable XOR, and admits expanded work, aggregate iterations, compact terms, and compact bytes without a fixed repeat-count syntax cap. Sweep-conditioned conversion combines one static all-zero-sweep reference with frame-derived per-record corrections, so skip-reference mode removes only the static reference and preserves detector, record-observable, and Pauli-observable sweep effects. One semantic owner covers every legal sweep-controlled and measurement-record-controlled Pauli orientation, classical `CZ` no-ops, omitted and explicit sweep records, both reference modes, and feedback crossing compact nested-repeat iterations. CLI folded-versus-unrolled evidence covers `m2d` primary and observable side output, appended and side-output Pauli corrections, plus `detect` PTB64 grouping. `m2d --sweep`, `--sweep_format`, and scoped `--ran_without_feedback` remain supported for the documented formats. Python-shaped converter APIs and broader public simulator products remain deferred in their own rows. |
| Detector-analysis utility APIs | Done for the nondeferred Rust scope | `circuit_detecting_regions` covers the full fixed Clifford tableau set, products, legal classical controls, gauges, anticommutation modes, filters, annotations, and compact repeat tick selection. `missing_detectors` now derives deterministic measurement invariants from canonical flow generators and eliminates them with declared detector and record-only observable rows, removing the parallel forward tableau engine and its dedicated folded-repeat implementation. Feedback, heralds, Pauli products, generated circuits, sparse ids, nested repeats, and explicit resource boundaries have semantic owners. Current contracts live in [stim-parity.md](stim-parity.md). |
| DEM sampling | Done for current Rust and CLI surface | See section 5 and M11 evidence. |
| Single-shot interactive tableau simulator | Deferred | Core tableau algebra and some simulator semantics exist for implemented workflows, but a public Python-style `TableauSimulator` API or new public Rust simulator product is outside the active non-deferred scope. Scoped simulator cross-checks exist in M12 for evidence, not as a public simulator product. |
| Batched flip-frame simulator | Deferred | Internal frame-simulator and packed-frame paths support sampling, detection, and Pauli-target observable cases, but a full public Python-style `FlipSimulator` API or new public Rust simulator product is outside the active non-deferred scope. |
| Exact random-stream parity with Stim | Deferred | The project explicitly requires statistical and semantic equivalence, not exact random-stream reproduction. |

## 7. Error Analysis, Search, And Decoder Configuration

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| Circuit-to-DEM analysis | Done for the nondeferred Rust scope | The sparse reverse tracker is the sole sensitivity-propagation engine for folded and unfolded analysis. Exact generated-code, decomposition, gauge, approximation, coordinate, recurrence, and error-merging owners prove selected values. A live metadata-driven differential compares canonical detector-error-model output against pinned Stim for every declared legal gate and target shape. Analyzer admission uses typed expanded-operation, repeat-iteration, and nesting limits, including an explicit one-million-step divergence for non-recurrent loops. Full ErrorMatcher provenance remains deferred. |
| Decoder interoperability and bounded reference decoder | Done for the selected A7 Rust scope | Stable `stab-decoder` exposes truth-hidden detector batches, caller-owned observable predictions, canonical dimensional preflight, static session dispatch, cancellation, and exact progress. The unpublished `stab-reference-decoder` proves the seam with a bounded exact maximum-likelihood implementation and a public-only sample-to-detect-to-decode experiment. Its directed-interval comparisons are certified by a mutually exclusive exact-dyadic fallback, strict representable majorities are not collapsed into ties, retained state is allocator-measured, reused decoding allocates nothing, and byte, pair-update, and limb-weighted work excesses return typed errors. Clean PR, full, and soak qualification, source-current Stab-only diagnostics, independent milestone audit, full code review, and exact-revision CI close this selected scope at measured revision `38160da59e6a55b1e4efd753d2aee8b8eb18f2b0`; no Stim parity or current self-regression result is claimed for decoder workloads. |
| `analyze_errors --decompose_errors` and related flags | Done for selected M10 and PF7 scope | WS2a of [plans/post-review-remediation-plan.md](plans/post-review-remediation-plan.md) ported pinned Stim's within-problem decomposition (exact search over the component's own terms under used-term and observable masks with the 64-term cap and Stim's error class, the greedy pair-single-remnant path, and zero-probability exclusion, with witness and structural-invariant regressions failing against the old search), and WS2b's consolidation closed the remaining blocker: the reverse tracker owns gauge symptoms directly and the non-decompose baseline DEM byte-matches pinned Stim across the sixty-three-entry matrix in both fold modes, so decomposition now runs on a byte-exact baseline pinned by the `m10-analyze-errors-decompose-surface-{d3,d3-fold,d5,d5-fold}` exact-output oracle rows; formal qualification rerun remains pending the freeze-lift change set. Implemented for the scoped M10 and PF7 cases, including `--block_decompose_from_introducing_remnant_edges`, `--ignore_decomposition_failures`, approximate disjoint options (the disjoint-to-independent converter is the vendor's, so `PAULI_CHANNEL_1` accept/reject and approximate output byte-match pinned Stim), gauge handling, and generic `--fold_loops` plus `--decompose_errors` repeated composite-error and remnant-edge behavior. Additional unselected analyzer families require a new exact plan rather than keeping this selected surface partial. |
| Error explanation value objects | Done for current active Rust value-object subset | Matched-error value objects and `explain_errors_from_circuit` are owned by `stab-analysis`, and PF6 covers `ExplainedError` and `CircuitErrorLocation` canonicalization of DEM terms, circuit locations, flipped Pauli products, and flipped measured observables through `pf6-matched-error-canonicalize-rust`. Future analyzer or search slices may add more value-object hardening if those outputs require it, but full ErrorMatcher provenance, generated surface-code repeat matching, heralded matching, repeat-contained noise stack frames, and `stim explain_errors` CLI UX remain deferred. |
| `stim explain_errors` CLI | Deferred | Explicitly deferred in [plans/rust-stim-drop-in-rewrite.md](plans/rust-stim-drop-in-rewrite.md) and [plans/post-beta-fix-report.md](plans/post-beta-fix-report.md). |
| Shortest graphlike and hypergraph logical-error search | Done for selected Rust API semantics | PFM-B5 covers ten graphlike, eleven hypergraph, and one shared traversal-resource case spanning no-error, exact zero-probability diagnostics, exact distance one through three, bounded hyperedges, generated rotated-surface and repetition codes, sparse high detector IDs, sparse high observable IDs, source-mechanism membership, variable-sized state admission, canonical target signatures, and fail-closed ungraphlike behavior. Graph construction allocates only touched detector nodes and compressed observable slots; expanded nonzero error mechanisms, per-error and aggregate target occurrences, effective detector nodes, unique graph edges, persistent graph terms, unique search states, attempted transitions, per-state terms, aggregate stored state terms, hyperedge degree, and total edge incidences have separate tested limits; two-phase admission checks graph caps before persistent edge or index reservation; collision-checked arena indexing avoids linear edge scans without duplicating edge payloads; and hypergraph adjacency stores each edge once. The clean d25/r25 and d11/r1000 benchmark rows remain report-only at 4.647x and 4.279x Stim. See [plans/pfm-b5-analyzer-search-progress-report.md](plans/pfm-b5-analyzer-search-progress-report.md). |
| SAT or WCNF encoding | Done for the nondeferred Rust semantics | `shortest_error_sat_problem` and `likeliest_error_sat_problem` own direct, sparse-id, nested-repeat, shifted-repeat, and unsatisfiable models. The likeliest encoder is checked against direct exhaustive optimization over 360 small semantic cases. Typed limits independently bound mechanisms, target occurrences, variables, clauses, literals, and output bytes without a repeat-count shortcut. |
| Sparse reverse detector-frame tracking | Done for selected internal and Rust-consumer scope | The shared sparse reverse tracker covers the selected Clifford, feedback, `SPP` or `SPP_DAG`, supported-unitary repeat, shifted measurement or detector repeat, analyzer, unsigned-flow, and matched-error consumers. PFM-B5 adds generated folded-versus-unrolled analyzer loops and closes the two independently selected tracker cases without fixture-specific periods. Full ErrorMatcher provenance and any new public simulator product remain deferred. See [plans/pfm-b5-analyzer-search-progress-report.md](plans/pfm-b5-analyzer-search-progress-report.md). |

## 8. Stabilizer Algebra Features

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| Owned Pauli strings | Done for scoped Rust algebra surface | `PauliString`, parsing, multiplication, commutation, iteration, and text round trips are implemented in [../crates/stab-algebra/src](../crates/stab-algebra/src). Canonical semantic and resource tests execute `stab-algebra` directly; `stab-core` retains only facade and cross-component integration ownership. Caller-sized construction and reusable iterator state are fallible and capped at 1,048,576 qubits, with exact accepted and first-rejected CQ2 resource evidence. See [plans/m6-completion-report.md](plans/m6-completion-report.md). |
| Flexible Pauli strings | Done | WS3 of [plans/post-review-remediation-plan.md](plans/post-review-remediation-plan.md) repaired the double sign-prefix consumption: the dense body now parses through a sign-rejecting variant, so `+-X`, `--X`, `-+X`, `i-X`, and `-i+X` reject with typed invalid-character errors mirroring pinned Stim while every single-sign form stays accepted, pinned by a pre-fix-failing regression; formal qualification rerun remains pending. `FlexPauliString` is exposed and covered by M6. Caller-sized construction shares the typed 1,048,576-qubit Pauli limit. |
| Clifford strings | Done for scoped Rust algebra surface | `CliffordString` is exposed and covered by M6, with M12 direct benchmark optimizations. Its packed representation maintains an exact non-identity count so point mutation does not rescan every bit plane, and repeated randomization reuses the allocated planes. Construction and projected growth are fallible and capped at 1,048,576 entries; repetition checks overflow and empty repetition is constant-work. |
| Tableaux and tableau iterators | Done for the nondeferred Rust algebra surface | `Tableau` supports checked conjugated-generator construction, signed powers, direct sums, noncontiguous embedding, canonical stabilizers, composition, inversion, named gates, and exhaustive iteration. `tableau_to_circuit` synthesizes H/S/CX circuits and round-trips signed Clifford actions; `pauli_before_circuit` and `pauli_after_circuit` own circuit conjugation. Compact circuit repeats compose logarithmically, while typed limits bound dense tableau size, random construction, unitary conversion, iteration, and nonunitary Pauli traversal. Python/numpy and public interactive simulator products remain deferred. |
| Algebra resource admission | Done for current Rust algebra surface | Public size-driven constructors return typed `StabilizerError` rejections through `StabilizerResource`. Explicit sizes reject before allocation, iterable construction stops at the first excess item without growing beyond the cap or returning partial output, and random construction rejects before RNG consumption. Flow construction and parsing admit at most 65,536 aggregate measurement plus observable terms, multiplication uses a bounded symmetric-difference merge so cancellation can keep a result within that contract, and compact circuit-to-Tableau repeats share a 16,777,216-unit aggregate work budget. [../crates/stab-algebra/tests/cq2_algebra_resources.rs](../crates/stab-algebra/tests/cq2_algebra_resources.rs) owns the direct algebra boundaries; [../crates/stab-analysis/tests/circuit_tableau.rs](../crates/stab-analysis/tests/circuit_tableau.rs) owns only circuit-derived admission and compact-repeat integration. These Stab limits are resource contracts, not exact Stim size-limit parity. |
| Flows | Done for the nondeferred Rust scope | `Flow` owns typed parsing, canonical text, multiplication, measurement indexes, observables, and bounded construction. Canonical generators cover every declared legal in-scope gate shape and compact repeats without first flattening the circuit. Signed checking uses the existing execution-backed sampled owner; unsigned checking retains typed diagnostics, sparse reverse-tracker execution, and batch helpers. GF(2) measurement solving, inverse QEC, time reversal, detecting regions, and missing-detector discovery consume the same flow and reverse-tracker semantics instead of parallel selected-subset engines. Supplied flow-observable dependencies are rejected during time reversal instead of silently dropped. Exact owners and Stim-bug divergences are generated in [stim-parity.md](stim-parity.md). Python binding shapes remain deferred. |
| Public borrowed Pauli string refs | Deferred | M6 explicitly uses owned Rust APIs instead of cloning Stim's borrowed binding-oriented shape. |
| Conversion to or from numpy, state vectors, and arbitrary unitaries | Deferred with scoped Rust subset | The M6-owned `unitary_to_tableau` subset remains implemented Rust semantic evidence. Python/numpy conversion APIs, state-vector APIs, arbitrary unitary conversion APIs, `tableau_to_unitary`, and public graph/vector simulator products remain deferred. P2 deleted the old test-only graph/vector cross-check because it duplicated owner tests without implementing these products. See [plans/m6-completion-report.md](plans/m6-completion-report.md), [plans/m12-progress-report.md](plans/m12-progress-report.md), and [plans/pfm0-matrix-simulator-deferral-evidence-lock.md](plans/pfm0-matrix-simulator-deferral-evidence-lock.md). |
| Graph simulator and vector simulator public APIs | Deferred | There is no direct Stab product evidence for these APIs. Owned tableau and unitary tests do not claim graph or vector simulator parity. |

## 9. Diagrams And Visualization

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| Circuit timeline diagrams | Deferred | `stim diagram` is intentionally deferred. |
| Detector-slice diagrams | Deferred | Diagram surfaces are future work. |
| DEM match-graph diagrams | Deferred | DEM diagram surfaces are future work. |
| Interactive HTML and Crumble diagram surfaces | Deferred | Crumble and interactive diagrams are future ecosystem or visualization work. |

## 10. Built-In Circuit Generation

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| `repetition_code:memory` | Done | Implemented in core generation and `stab gen`, with complete pinned exact-output coverage and a no-noise detector matrix. Every distance through the CLI maximum 2047 remains below the materialization cap. |
| `surface_code:rotated_memory_x` and `surface_code:rotated_memory_z` | Done with an explicit materialization bound | Implemented and covered by complete pinned exact cases, larger exact layouts, the upstream no-noise matrix, and first-rejection resource evidence. Distances through 256 are accepted; 257 and above reject because the projected physical-qubit count exceeds 131,072. |
| `surface_code:unrotated_memory_x` and `surface_code:unrotated_memory_z` | Done with an explicit materialization bound | Implemented and covered by complete pinned exact cases, the upstream no-noise matrix, and first-rejection resource evidence. Distances through 181 are accepted; 182 and above reject because the projected physical-qubit count exceeds 131,072. |
| `color_code:memory_xyz` | Done with an explicit materialization bound | Implemented and covered by complete pinned exact cases, exact d5 layout, the valid upstream no-noise matrix, family-domain rejection, and first-rejection resource evidence. Valid odd distances through 341 are accepted; 343 and above reject because the projected physical-qubit count exceeds 131,072. |
| Generator parameters and noise knobs | Done for current Rust surface | `CodeDistance`, `RoundCount`, all task enums, repetition/surface/color parameter objects, immutable noise builders, generated-circuit accessors, maximum folded rounds, and invalid color-family combinations have focused exact qualification ownership in [../crates/stab-analysis/src/circuit_generation.rs](../crates/stab-analysis/src/circuit_generation.rs). The typed nominal distance domain remains 2 through 2047; resource admission occurs at the materializing generator boundary. |
| Python `stim.Circuit.generated` | Deferred | No Python bindings yet. |

## 11. Command-Line API

| Stim command | Stab status | Evidence and notes |
| --- | --- | --- |
| `stim gen` | Done for selected CLI scope | `stab gen` owns the named generator route and supports every nondeprecated in-scope geometry, task, noise, input, and output option. Exact generated-code owners cover canonical probability text and family layouts. The CLI rejects projected generated circuits above 131,072 physical qubits before opening `--out`; this preserves all repetition distances through 2047 but intentionally narrows materialized surface and color acceptance relative to pinned Stim. Deprecated top-level `--gen` dispatch is intentionally absent. |
| `stim convert` | Done for selected CLI scope | WS3 of [plans/post-review-remediation-plan.md](plans/post-review-remediation-plan.md) made `--in_format` required like pinned Stim, removing the silent `01` default; formal qualification rerun remains pending. WS5 converted result-data conversion to the shared per-record streaming reader per decision D5: the whole-input 64 MiB cap is dropped, memory stays bounded by one record frame (each framed text record is bounded at 64 MiB), an over-cap streaming success fixture and a peak-allocation bound are pinned in `crates/stab-cli/src/tests/resource.rs`, and the `--in_format=stim` circuit route keeps the 64 MiB circuit-input cap. `stab convert` supports `01`, `b8`, `r8`, HITS, DETS, and `ptb64` conversions with explicit counts, `--dem`, `--circuit`, unique `--types` letters, `--obs_out`, and `--obs_out_format`. The Stab-specific `.stim -> .stim` canonical conversion remains supported only for `--in_format=stim --out_format=stim`. Strict text grammar, typed DETS semantics, path-alias safety, and exact conversion behavior passed clean PR, full, and soak qualification. |
| `stim sample` | Done for selected CLI scope | WS3 of [plans/post-review-remediation-plan.md](plans/post-review-remediation-plan.md) repaired both reopened causes: broken-pipe-rooted failures exit 141 with empty stderr like pinned Stim (decision D2), and the WS1 `--skip_reference_sample` divergence was repaired earlier with the committed oracle row `m8-sample-skip-reference-flip`; formal qualification rerun remains pending. `stab sample` supports current flags, deterministic and noisy statistical behavior, output formats, seed handling, skip-reference, skip-loop-folding, and streaming output. |
| `stim detect` | Done for the nondeprecated CLI scope | `stab detect` supports shots, input/output paths, detector or observable routing, relevant formats including supported `ptb64`, seeds, reference correction, and streaming output. Broken-pipe-rooted failures exit 141 with empty stderr like pinned Stim. Observable ordering uses `--append_observables`, implicit DETS ordering, or `--obs_out`; deprecated `--prepend_observables` is intentionally absent. |
| `stim m2d` | Done for selected PF7 CLI surface | WS1 of [plans/post-review-remediation-plan.md](plans/post-review-remediation-plan.md) repaired the noiseless reference contract so converted detector events match pinned Stim for `p == 1` result flips, verified against the pinned binary; formal qualification rerun remains pending. `stab m2d` supports text, `b8`, `r8`, `hits`, `dets`, and `ptb64` input where accepted, plus detector and observable outputs where accepted. `--sweep` and `--sweep_format` stream sweep records for the current sweep-conditioned conversion subset, with CLI evidence for `01`, `b8`, `r8`, `hits`, `dets`, and input-only `ptb64` sweep records; scoped `--ran_without_feedback` applies feedback inlining before conversion, and `ptb64` output is rejected like pinned Stim v1.16.0. PF7 selected CLI evidence covers `--circuit`, `--in`, `--out`, `--sweep`, and `--obs_out` path IO, path-error precedence before converter setup, selected default detector-only `dets` output, `--append_observables`, `--skip_reference_sample`, observable side-output widths, Pauli-target observable annotations, selected format and width failures, writer failure propagation, stdout behavior, stderr class, exit status, and existing M9 sweep, feedback, format, and resource behavior. Broader detector-converter API parity remains tracked by the measurement-to-detection conversion row. |
| `stim analyze_errors` | Done for selected PF7 CLI surface | `stab analyze_errors` implements the staged M10 and PF7 flag and path-IO surface, including default analysis, generic `--fold_loops`, gauge handling, approximate disjoint errors, decomposition controls, input and output paths, malformed input, stdout and stderr classes, and exit status. PFM-B5 closes the selected generated and giant-loop analyzer semantics behind this command. Full ErrorMatcher provenance and `stim explain_errors` remain deferred, while deprecated `--detector_hypergraph` is intentionally unsupported. |
| `stim sample_dem` | Done for the nondeprecated CLI scope | `stab sample_dem` supports detector output, `--obs_out`, sampled-error output, replay input, every accepted result format including `ptb64`, and bounded streaming writers. Hidden `--append_observables` and `--prepend_observables` conveniences were removed; the observable side stream is the sole explicit routing contract. |
| `stim diagram` | Deferred | No Stab command. |
| `stim explain_errors` | Deferred | No Stab command, despite scoped core matched-error support. |
| `stim repl` | Deferred | No Stab command. |
| `stim help` command | Done as a Stab-native structural divergence | `stab help [topic]` owns implemented command, result-format, and gate discovery; `stab --help` and each named command's `--help` remain parser-owned overviews. Exact byte-for-byte Stim documentation and deprecated help spellings are intentionally out of scope. |
| Legacy top-level command flags | Done by intentional omission | Deprecated `--gen`, `--convert`, `--sample`, `--detect`, `--m2d`, `--analyze_errors`, and `--detector_hypergraph` modes are rejected instead of routed through a second parser. Deprecated `sample --frame0`, `detect --prepend_observables`, and hidden `sample_dem` observable-order aliases are also absent. One real-binary owner checks every removed spelling, relevant nonzero status and flag diagnostics, and both parser-owned and Stab-native help surfaces. |

## 12. Python API

| Python feature area | Stab status | Evidence and notes |
| --- | --- | --- |
| Top-level `stim` functions | Deferred | No Python module exists. Rust equivalents exist for some target constructors and helpers but are not exposed as Python. |
| `stim.Circuit` class | Deferred with Rust subset | `Circuit` exists as a Rust type, but Python class operators, binding-style mutation API, file-like object helpers, transforms, diagram/export methods, and properties are not bound. |
| `stim.DetectorErrorModel` class | Deferred with Rust subset | `DetectorErrorModel` exists as a Rust type with current Rust construction, introspection, coordinate, tag-stripping, materialized `flattened`, and `rounded` APIs, but Python class operators, diagrams, Python binding shape, and full method parity are not bound. |
| Sampler and converter classes | Deferred with Rust subset | Rust measurement sampling, measurement-to-detection conversion, and DEM sampling use compiler-plan-session-sink APIs instead of Python-shaped compiled classes. The pre-0.2 whole-output and callback adapters are removed. Python `CompiledMeasurementSampler`, `CompiledDetectorSampler`, `CompiledMeasurementsToDetectionEventsConverter`, and `CompiledDemSampler` classes do not exist. Exact Python `CompiledDetectorSampler` sweep-bit API shape, Python `CompiledDemSampler.sample_write` ergonomics, and any future explicit Rust `CompiledDetectorSampler` product are deferred. |
| Error explanation classes | Deferred with Rust subset | Rust matched-error value objects exist for scoped M10 support, but Python `ExplainedError` and related classes are not exposed. |
| Stabilizer, gate metadata, and simulator classes | Deferred with Rust subset | Rust Pauli, Clifford, Tableau, Flow, and bounded `Gate` metadata APIs exist. Python `GateData`, `TableauSimulator`, `FlipSimulator`, numpy conversions, and full class operator parity are not exposed. |

## 13. JavaScript And WASM API

| JS/WASM feature area | Stab status | Evidence and notes |
| --- | --- | --- |
| WASM build and JS package | Deferred | No WASM build or JS package exists. |
| JS `Circuit`, `Tableau`, `PauliString`, and `TableauSimulator` | Deferred | Rust core APIs can inform future bindings, but no JS bindings are present. |
| Crumble browser editor | Deferred | Future ecosystem work. |

## 14. Ecosystem And Glue Packages

| Ecosystem surface | Stab status | Evidence and notes |
| --- | --- | --- |
| `stimcirq` | Deferred | No Cirq integration exists. |
| `sinter` | Deferred | No decoding-statistics package exists. |
| Crumble | Deferred | No browser editor integration exists. |
| `stimflow` | Deferred | No separate flow toolkit exists. |
| ZX and lattice-surgery glue | Deferred | No ZX or lattice-surgery integration exists. |
| QASM and Quirk exports | Deferred | Explicitly future work unless drop-in scope expands. |

## 15. Packaging, Build, And Documentation Surfaces

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| Cargo workspace | Done | The repo is a Cargo workspace with independently consumable model, records, bits, algebra, analysis, engine, decoder, kernel, facade, CLI, test-support, and Rust operations crates. |
| Operational command surface | Done | The repo uses `just` and modular `justfiles`, with complex logic in Rust ops crates. |
| Python packaging | Deferred | No `pyproject.toml` or Python package exists for Stab bindings. |
| JS packaging | Deferred | No JS package exists. |
| Generated API docs | Done for Rust API reference workflow | `just docs::api` generates workspace Rust API documentation under `target/doc/`, and `just docs::api-check` runs rustdoc with warnings denied. Python stubs remain deferred with Python bindings, and generated feature/status matrix tooling remains a separate future documentation surface. |
| Feature and roadmap docs | Done for current planning surface | Roadmap, test-porting plan, completion reports, lessons learned, GOAL, and this checklist live under [plans](plans) and [docs](.). |

## 16. Test And Benchmark Surface

The rows below preserve the historical CQ and PQ evidence assessment. They do not describe current feature status; use the generated [Stim parity ledger](stim-parity.md) for that.

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| Upstream test inventory and porting plan | Done for its historical selected qualification scope | [plans/stim-test-porting-plan.md](plans/stim-test-porting-plan.md) groups upstream files and historical Rust port priorities. [qualification-status.md](qualification-status.md) preserves generated transitional inventory counts. Clean revision `68d107a42f655254f31628f0cbedc55479f6c0f3` passed the then-current PR, full, and soak selections after the corrected result-format oracle landed. |
| Oracle fixture matrix | Done for historical selected surfaces and CQ1 harness acceptance | `oracle/fixtures/manifest.csv`, `ops/oracle`, and the checked correctness inventory preserve exact, statistical, structural, and semantic-mining ownership from the old program. The generated [transitional qualification dashboard](qualification-status.md) owns those inventory and completion counts only. [plans/cq2-deterministic-qualification-progress-report.md](plans/cq2-deterministic-qualification-progress-report.md) preserves checkpoints with their exact revisions, digests, replay status, and current-versus-historical classification. |
| Benchmark manifest and primary beta gate | Done for the retained historical performance infrastructure | `benchmarks/manifest.csv`, `benchmarks/m12-primary-thresholds.json`, and `benchmarks/m12-primary-beta-waivers.json` preserve the legacy M12 matrix. The PQ0 overlay, runtime-group ledger, and [transitional qualification dashboard](qualification-status.md) preserve old relationships, counts, and completion checkpoints; they do not define current feature status. |
| Historical M12 beta performance gate | Done for its recorded report state | The unchanged `1.25x` M12 gate and its source-owned no-ratio waivers remain diagnostic compatibility evidence. The generated [Stim parity ledger](stim-parity.md) reports current feature status; active performance claims resume under P7 through P9 of [GOAL](plans/GOAL.md). |
| Tests for deferred Python, JS, diagrams, and ecosystem packages | Deferred | These are intentionally future work and should not be used as blockers for the current Rust/CLI beta surface. |

## 17. Historical Remaining Gaps At The Last Assessment

This section preserves the triage view from the checklist's last active assessment. It is not a current work queue; current gaps and milestone ownership come only from the generated [Stim parity view](stim-parity.md).

| Gap | Status | Why it matters |
| --- | --- | --- |
| Full circuit transform API parity | Done for selected Rust API scope | Rust `flattened`, `flattened_operations`, `without_noise`, `decomposed`, scoped `inverse_qec` no-flow reset-measure-detector, exact two-to-one detector-flow, exact `m_det` two-detector packet, exact noiseless MPP all-record identity-parity detector-flow, selected MPAD record-tail rewrite, exact noisy MZZ detector-flow, selected observable Pauli include packet, selected noisy `M`/`MX`/`MY` measurement-only reversal, selected noisy `MR`/`MRX`/`MRY` measure-reset-only reversal, exact noisy measure-reset detector-flow, and measure-reset pass-through target-list rewrites for one detector, folded unitary and tracker-driven measurement-rich `time_reversed_for_flows`, and scoped `with_inlined_feedback` are implemented for the owned RPF2 subset, including selected MPP or pair-measurement decomposition flow-generator preservation, selected measurement-ordering, selected empty-flow plus Pauli-only, measurement-record, and observable MPAD record-tail reversal, selected duplicate MPAD observable-id record parity, selected plain reset-to-measurement over one or more unique qubit targets, selected measurement-to-reset including the selected `dont_turn_measurements_into_resets` single-measurement option, selected measure-reset flow reversal over one or more unique qubit targets including inverted result targets, selected `MZZ` plus plain-qubit unitary suffix reversal, exact pinned `flow_flip` packet, pinned demolition feedback and interleaved-ordering exact-output cases, selected `XCZ`/`YCZ` measurement-record feedback, selected bounded feedback loop refolding, and selected nested bounded-repeat feedback detector-parity preservation. PFM-B1 is complete for the selected Rust transform scope: all nineteen reverse-flow and QEC-transform cases have distinct selectors, clean committed-HEAD allocation evidence, final audit, and GPT-5.6/max review closure. Unselected `inverse_qec`, feedback, heralded-record, and export surfaces require a new exact plan instead of reopening the finite PFM-B1 ledger. Broader repeat-contained feedback remains deferred and requires an explicit plan revision to reopen; QASM, Quirk, and Crumble export also remain deferred. |
| Full DEM public API parity | Done for selected Rust API scope | Rust count, coordinate, iterator, compact transform, sampler, graphlike, hypergraph, SAT/WCNF, and ErrorMatcher filter surfaces have source-owned PFM-B3 traversal evidence. Remaining differences are deferred Python ergonomics and operators, diagram products, and full ErrorMatcher provenance, not an active shared-traversal blocker. |
| Broader sweep-conditioned simulator and analysis parity | Partial | Public `m2d --sweep` covers accepted sweep-record formats `01`, `b8`, `r8`, `hits`, `dets`, and input-only `ptb64`, non-frame and selected frame-path `detect` omitted-sweep default-false sampling plus measurement-record `XCZ` or `YCZ` feedback are implemented, sampler-backed execution rejects `CX q sweep[k]` and `CY q sweep[k]` while accepting neighboring sweep-first `CX` or `CY` and both-order `CZ` sweep/qubit cases, and selected analyzer sweep-control plus `CZ` classical-only no-op behavior and invalid target-position rejection behavior are implemented for current subsets under [plans/pfm3-analyzer-sweep-boundary-scope.md](plans/pfm3-analyzer-sweep-boundary-scope.md), but the analyzer sweep boundary is evidence-closed by `pfm3-analyzer-sweep-matrix`; future shapes require an explicit plan revision, while Python detector-sampler sweep APIs and every remaining sweep target shape remain broader work. Pinned Stim v1.16.0 does not expose `stim detect --sweep`, so that flag is not a current CLI parity gap. |
| Full feedback-inlining transform parity | Partial | `m2d --ran_without_feedback`, `circuit_with_inlined_feedback`, and scoped Rust `Circuit::with_inlined_feedback` cover the supported top-level Pauli and MPP feedback subset, pinned `demolition_feedback` and `interleaved_feedback_does_not_reorder_operations` exact-output cases, selected `XCZ`/`YCZ` measurement-record feedback equivalent to `CX`/`CY`, selected pinned bounded repeat-loop refolding, and nested bounded-repeat `CY`/`CZ` detector-parity cases while rejecting excessive repeat work and unsupported classical controlled gates. Broader repeat-block feedback beyond these selected cases remains deferred and requires an explicit plan revision to reopen. |
| Full ErrorMatcher provenance and `explain_errors` CLI | Deferred | Core value objects exist, but full stack-frame provenance and CLI UX remain future work. |
| Public interactive `TableauSimulator` and `FlipSimulator` APIs | Deferred | Current internals support implemented workflows, but public simulator API parity is not exposed. |
| Diagrams and visualization | Deferred | Stim's diagram command and Python diagram APIs are a large independent rendering surface. |
| Python bindings | Deferred | Needed for eventual Stim drop-in package parity, but intentionally after Rust and CLI surfaces mature. |
| JS/WASM and ecosystem packages | Deferred | Separate product and integration projects, not part of current core Rust/CLI beta. |
| Generated feature/status matrix tooling | Deferred | Rust API documentation is already covered by the section 15 `Generated API docs` row. Generated feature/status matrix tooling remains outside the current Rust and CLI beta closure; plan it separately if it becomes a product requirement. |

## 18. Historical Maintenance Notes

- Do not update this checklist as current work advances. Update `../oracle/stim-v1.16-parity.toml`, regenerate [stim-parity.md](stim-parity.md), and update the active [GOAL](plans/GOAL.md) when milestone state changes.
- When a deferred surface becomes in scope, revise the active parity plan and ledger before implementation or evidence claims.
- Preserve this file only as historical research until P9 extracts any durable rationale and deletes superseded status documents.
