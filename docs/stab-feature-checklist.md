# Stab Feature Checklist Against Stim v1.16.0

This checklist maps the Stim v1.16.0 inventory in [stim-feature-list.md](stim-feature-list.md) onto the current Stab codebase.
It is a feature-availability document, not a new roadmap.
Use [plans/comprehensive-correctness-qualification-plan.md](plans/comprehensive-correctness-qualification-plan.md), [plans/comprehensive-stim-performance-qualification-plan.md](plans/comprehensive-stim-performance-qualification-plan.md), and [plans/GOAL.md](plans/GOAL.md) for the active qualification goals; the historical implementation roadmap remains [plans/rust-stim-drop-in-rewrite.md](plans/rust-stim-drop-in-rewrite.md).

Status key:

- `Done`: implemented for the current Rust core or CLI surface and backed by source, tests, oracle rows, reports, or benchmark evidence.
- `Partial`: implemented for an explicit subset, with remaining Stim parity gaps or public API gaps named.
- `Deferred`: intentionally outside the current beta surface or future-plan scope.
- `Missing`: no implementation found in the current Stab tree and not clearly documented as deferred.

## 1. Top-Level Product Surface

| Stim feature area | Stab status | Evidence and notes |
| --- | --- | --- |
| Rust core library equivalent for core Stim semantics | Done for selected Rust API scope | `stab-core` exposes the selected circuit, gate, target, result-format, sampler, detector-conversion, DEM, analyzer, search, flow, and stabilizer APIs through [../crates/stab-core/src/lib.rs](../crates/stab-core/src/lib.rs). PFM-B1 through PFM-B5 close every non-deferred Rust blocker in the source-owned ledger. Python object shape, public interactive simulator products, diagrams, full ErrorMatcher provenance, and other explicitly deferred products are not part of this selected Rust status. |
| CLI binary | Done for selected Stab CLI surface | `stab-cli` implements `stab gen`, `stab convert`, `stab sample`, `stab detect`, `stab m2d`, `stab analyze_errors`, `stab sample_dem`, Stab-native `help`, and the selected legacy aliases in [../crates/stab-cli/src/lib.rs](../crates/stab-cli/src/lib.rs). Section 11 records each selected command as done or explicitly deferred, with PF7 source-owned evidence for selected `m2d`, `analyze_errors`, and legacy-dispatch closure. The binary is named `stab`; drop-in `stim` packaging or aliasing remains outside this checklist unless the release plan adds it. |
| `.stim`, `.dem`, and result-format compatibility | Partial | `.stim` core, implemented result-format paths, the PFM-B2 gate matrix, and the selected Rust `.dem` count, coordinate, transform, analyzer, sampler, search, SAT/WCNF, and filter-key surfaces are done. This remains a literal product-level rollup only because named deferred command, Python, diagram, and provenance products prevent full Stim parity; there is no remaining shared Rust traversal or gate-execution blocker. See sections 2, 5, 6, and 11. |
| Python package `stim` | Deferred | Python bindings are explicitly future work in [plans/rust-stim-drop-in-rewrite.md](plans/rust-stim-drop-in-rewrite.md). No `pyo3`, `maturin`, Python package, or Python API compatibility layer is present. |
| JavaScript/WASM API | Deferred | JS/WASM is explicitly future work in [plans/rust-stim-drop-in-rewrite.md](plans/rust-stim-drop-in-rewrite.md). |
| Ecosystem integrations | Deferred | `stimcirq`, `sinter`, Crumble, `stimflow`, ZX, lattice-surgery helpers, QASM, and Quirk are future or out-of-scope ecosystem surfaces. |
| GPU backend | Deferred | GPU acceleration is explicitly future work, and the current project focuses on CPU portable SIMD first. |

## 2. File Formats And IO Contracts

### 2.1 `.stim` Circuit Format

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| Line-based `.stim` parsing, comments, tags, args, targets, and repeat blocks | Done | Implemented in [../crates/stab-core/src/circuit.rs](../crates/stab-core/src/circuit.rs), [../crates/stab-core/src/target.rs](../crates/stab-core/src/target.rs), and [../crates/stab-core/src/gate.rs](../crates/stab-core/src/gate.rs). M4 completion evidence is in [plans/m4-completion-report.md](plans/m4-completion-report.md). |
| Canonical `.stim` printing | Done | `Circuit::to_stim_string` is implemented in [../crates/stab-core/src/circuit.rs](../crates/stab-core/src/circuit.rs). M4 reports canonical printer oracle and benchmark evidence. |
| Target kinds | Partial | Qubit, inverted qubit, measurement record, sweep bit, Pauli, inverted Pauli, and combiner parsing are implemented in [../crates/stab-core/src/target.rs](../crates/stab-core/src/target.rs). Sweep-conditioned `m2d` conversion, non-frame and selected frame-path `detect` sampling with omitted all-false sweep bits, and selected analyzer sweep-control no-op plus invalid target-position rejection behavior are implemented for current execution subsets. Sampler-backed execution now rejects parsed `CX q sweep[k]` and `CY q sweep[k]` target order while preserving accepted sweep-first `CX` or `CY` and both-order `CZ` sweep/qubit cases. The selected analyzer subset includes `CZ` sweep/sweep, record/sweep, sweep/record, and record/record classical-only no-op groups, with the boundary locked in [plans/pfm3-analyzer-sweep-boundary-scope.md](plans/pfm3-analyzer-sweep-boundary-scope.md). The selected frame-path `detect` subset includes sweep-controlled `CX`, `CY`, `CZ`, qubit/sweep `XCZ` or `YCZ` no-op groups, and measurement-record `XCZ` or `YCZ` feedback equivalent to `CX` or `CY` feedback, while the analyzer sweep boundary is evidence-closed by `pfm3-analyzer-sweep-matrix`; future shapes require a new failing pinned oracle or an explicit public-surface plan revision; broader sweep target shapes remain partial. Pinned Stim v1.16.0 does not expose `stim detect --sweep`; typed detector-sampler sweep inputs belong to deferred Python or future explicit Rust API work, not current CLI parity. |
| Broadcast and validation behavior | Done for parser and gate validation | Gate arity, target rules, Pauli product grouping, disjoint segments, probabilities, and typed arguments are covered by [../crates/stab-core/src/gate.rs](../crates/stab-core/src/gate.rs), [../crates/stab-core/src/circuit.rs](../crates/stab-core/src/circuit.rs), and M4 tests. |
| Full semantic execution of every legal circuit operation | Partial | Implemented where owned by sampler, detection, analyzer, generation, and algebra milestones. Unsupported or deferred semantics are documented in the relevant sections below. |

### 2.2 `.dem` Detector Error Model Format

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| DEM parser and canonical printer | Done | Implemented in [../crates/stab-core/src/dem.rs](../crates/stab-core/src/dem.rs) through `DetectorErrorModel::from_dem_str` and `DetectorErrorModel::to_dem_string`, with exact oracle coverage in `m10-dem-parse-print-exact`, structural coverage in `m10-dem-parse-print-basic` and `coverage-py-dem`, and PF4 malformed-input and public-constructor validation coverage. Broader DEM transform, traversal, introspection, and full API parity gaps remain tracked by their own rows. |
| DEM instruction types | Done for core types | `error`, `detector`, `logical_observable`, `shift_detectors`, separators, and repeat blocks are implemented in [../crates/stab-core/src/dem.rs](../crates/stab-core/src/dem.rs), including Stim-compatible exact-one-target validation for `detector` and `logical_observable`. |
| DEM detector shifts, observables, coordinates, and counts | Done for selected Rust API scope | PFM-B3 routes detector, observable, error, final-shift, and coordinate queries through one checked compact traversal with exact nested-repeat, deterministic differential, overflow, sparse selected-lookup, and first-declaration evidence. Selected coordinates use declaration bounds, algebraic flat and bounded-nested scans, and selected repeat iterations without flattening preceding repeats. The unfiltered map retains its one-million-entry cap because Stim returns every detector index below `count_detectors()`, including empty coordinate vectors; remaining Python API shape is deferred. See [plans/pfm-b3-folded-dem-traversal-progress-report.md](plans/pfm-b3-folded-dem-traversal-progress-report.md). |
| DEM flattening and large repeat traversal | Done for selected bounded-result Rust consumers | PFM-B3 provides one checked folded tree and visitor for counts, coordinates, sampler compilation, graphlike and hypergraph collection, SAT/WCNF collection, and ErrorMatcher filter keys. Coordinate vectors are opt-in, neutral repeats skip before caps, eligible zero-shift repeats fold, and selected coordinate iterations are chosen algebraically. Compact `rounded` and `without_tags` transforms intentionally recurse directly so they allocate only their output body. `DetectorErrorModel::flattened`, full coordinate maps, sampled-error records and replay, shifted active search mechanisms, SAT clauses, and full ErrorMatcher provenance remain capped or deferred because their output or search state is inherently expanded; those caps are tested separately from compact-input traversal. See [plans/pfm-b3-folded-dem-traversal-progress-report.md](plans/pfm-b3-folded-dem-traversal-progress-report.md). |

### 2.3 Shot And Result Data Formats

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| `01` dense text | Done for implemented paths | Implemented in [../crates/stab-core/src/result_formats.rs](../crates/stab-core/src/result_formats.rs) and streaming readers in [../crates/stab-core/src/result_streaming.rs](../crates/stab-core/src/result_streaming.rs). |
| `b8` dense binary | Done for implemented paths | Implemented for sampling, detection, m2d input/output where accepted, and DEM sampling side streams. |
| `r8` sparse binary | Done for implemented paths | Implemented in result-format readers and writers and covered by M11 sample_dem side-stream tests. |
| `hits` sparse text or binary sparse index format | Done for implemented paths | Implemented by result-format readers and writers for measurement, detection, and DEM-sampling flows where accepted. |
| `dets` sparse text | Done for implemented paths | Implemented with command-specific measurement-only, typed `convert` layout, and detection-output behavior. `sample_dem --out_format=dets` is fixed to keep detector output separate from observable output. |
| `ptb64` transposed packed format | Done for implemented paths | Separate helper APIs support `ptb64`; CLI `sample`, `convert`, `detect`, `sample_dem`, and `m2d` input support relevant `ptb64` surfaces. `m2d --out_format=ptb64` and `--obs_out_format=ptb64` intentionally reject like pinned Stim v1.16.0. |
| Format conversion command coverage | Done for result-format CLI parity | `stab convert` supports `.stim -> .stim` canonicalization as a Stab extension and Stim-style result-format conversion for `01`, `b8`, `r8`, `hits`, `dets`, and `ptb64` with explicit counts, `--dem`, `--circuit`, `--types`, `--obs_out`, and `--obs_out_format`. |
| Streaming IO for large implemented CLI paths | Done for current post-beta surfaces | `stab sample`, `stab sample_dem`, `stab detect`, and implemented `stab m2d` paths stream output or input in bounded chunks. See [plans/post-beta-fix-report.md](plans/post-beta-fix-report.md). |

## 3. Gate And Instruction Surface

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| 81 canonical Stim v1.16.0 gates | Done for registry and validation | `GATES` in [../crates/stab-core/src/gate.rs](../crates/stab-core/src/gate.rs) contains the canonical table excluding `NOT_A_GATE`, with categories, argument rules, target rules, and inverse names. |
| Parser aliases | Done | Aliases such as `MZ`, `MRZ`, `RZ`, `CNOT`, `ZCX`, `H_XZ`, `CORRELATED_ERROR`, `SQRT_Z`, and `SWAPCZ` are handled in [../crates/stab-core/src/gate.rs](../crates/stab-core/src/gate.rs). |
| Gate validation flags and categories | Done for current Rust metadata surface | Stab has typed validation for argument counts, target categories, grouping, probabilities, and selected metadata. Rust `Gate` exposes aliases, argument rules, target rules, target grouping, fusing, noisy/reset/measurement/unitary/single-qubit/two-qubit/target-capability/symmetry flags, unitary inverse, generalized inverse, local Clifford tableau metadata, Stim v1.16.0 `GateData.flows` metadata for tableau-backed unitary gates plus representative measurement-rich and variable-target gates, fixed-shape one- or two-qubit `GateUnitaryMatrix` metadata, and Stim v1.16.0 H/S/CX/M/R `GateDecomposition` metadata for the owned PF1 subset, with `pf1-gate-metadata-api` evidence. Full Python `GateData` object parity remains deferred, and execution support stays tracked separately in the gate semantic row. |
| Gate semantic execution | Done for selected Rust/CLI scope | PFM-B2 classifies all 81 canonical gates across parser, measurement sampler, reference sampler, detection converter, detector frame, detection sampler, error analyzer, and flow-generator surfaces, with 22 typed target patterns and no unknown state. Thirty-seven independently selectable `pfm3-contract-*` cases cover all nineteen semantic families through exact, structural, rejection, state-equivalence, semantic-invariant, or source-owned statistical comparators; `just oracle::run --milestone PF3` and `just oracle::blockers --check-selectors` pass. Review remediation strengthens fixed-tableau inverse equivalence, grouped pair-measurement inversion, pinned four-body MPP invariants, controlled-Pauli directionality and sweep behavior, independent noisy-gate effects, heralded records, and anti-Hermitian rejection. Public interactive simulator products and exact random-stream parity remain deferred and do not keep this selected execution surface partial. See [plans/pfm-b2-gate-surface-progress-report.md](plans/pfm-b2-gate-surface-progress-report.md). |

PFM-B2 contract groundwork classifies all 81 canonical gates across parser, measurement sampler, reference sampler, detection converter, detector frame, detection sampler, error analyzer, and flow-generator surfaces, maps accepted target groups back to typed roles including Hermitian or anti-Hermitian products, and gives each ledger case a machine-readable eight-surface coverage set.
The final 37-case execution matrix turns that contract integrity into completion evidence for all nineteen semantic families; the groundwork report remains historical context and [plans/pfm-b2-gate-surface-progress-report.md](plans/pfm-b2-gate-surface-progress-report.md) is authoritative for closure.

## 4. Core Circuit Features

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| Circuit construction from parsed text | Done | `Circuit::from_stim_str`, item storage, repeat blocks, instructions, tags, and canonical printing are implemented in [../crates/stab-core/src/circuit.rs](../crates/stab-core/src/circuit.rs). |
| Programmatic mutation | Done for current Rust API surface | `append_instruction`, `append_repeat_block`, `append_from_stim_text`, `append_from_stim_program_text`, bounded path-based `from_stim_file`, streaming `write_stim_file`, `append_circuit`, `concatenated`, `repeated`, `repeat_in_place`, `insert_item`, `insert_instruction`, `insert_repeat_block`, `insert_circuit`, `pop_item`, `pop_last_item`, derived `Clone`, and `clear` exist and are covered by `pf1-circuit-rust-api`. Python operator ergonomics remain deferred. |
| Core introspection | Done for current Rust API surface | `count_qubits`, top-level `len`/`is_empty`, `iter_items`, `item_range`, `instruction_range`, lazy flattened forward and reverse instruction iterators, `count_measurements`, `count_detectors`, `count_observables`, `count_ticks`, `count_sweep_bits`, `measurement_record_count`, `detection_record_width`, and related helpers exist and are covered by `pf1-circuit-rust-api`. Python binding-style indexing and full Python property parity are deferred. |
| Circuit coordinate queries | Done for current Rust API surface | Coordinates are parsed and used in analyzer and detection workflows. Rust `Circuit` exposes folded `final_coordinate_shift`, `final_qubit_coordinates`, `detector_coordinates`, `detector_coordinates_for`, and `coordinates_of_detector` using `CircuitDetectorId`, with `pf1-circuit-rust-api` coverage. Rust coordinate queries reject non-finite folded coordinate results as documented hardening; exact Python-style API shape and exact C++ infinity behavior remain deferred or logged. |
| Repeat handling | Partial | Repeat blocks parse, print, sample, analyze, convert, and participate in the Rust `Circuit::flattened`, `Circuit::flattened_operations`, and `Circuit::without_noise` transform subset. Materialized flattening is capped at one million output operations while shift-only repeats are folded; fully folded traversal across every transform remains partial. |
| Circuit transforms | Done for selected Rust API scope | `without_tags`, `flattened`, `flattened_operations`, `without_noise`, `decomposed`, `to_tableau`, `inverse_unitary`, `inverse_qec`, folded unitary and tracker-driven measurement-rich `time_reversed_for_flows`, `simplified`, and a scoped `circuit_with_inlined_feedback` helper for `m2d --ran_without_feedback` are implemented, including pinned demolition feedback and interleaved-ordering exact-output cases, the supported MPP feedback-transform case, selected `XCZ`/`YCZ` measurement-record feedback equivalent to `CX`/`CY`, selected bounded repeat-loop refolding from pinned `transform_without_feedback`, and selected nested bounded-repeat `CY`/`CZ` feedback detector-parity preservation. `inverse_qec` covers the unitary inverse subset plus selected noiseless plain no-flow reset-measure-detector target-list rewrites, the exact selected two-to-one `R`/`CX`/`M` detector-flow packet, the exact selected `m_det` two-detector packet, the exact selected noiseless `MPP` plus one-detector all-record identity-parity packet, selected direct `MPAD` record-tail rewrites, the exact selected noisy `MZZ` detector-flow packet, the exact selected observable Pauli include packet, selected noisy `M`/`MX`/`MY` measurement-only reversal, selected noisy `MR`/`MRX`/`MRY` measure-reset-only reversal, selected exact noisy measure-reset detector-flow, and selected measure-reset pass-through target-list rewrites for their selected detector counts, including pinned `r_m_det`, the exact one-qubit pinned `r_m_det_keep_m` options branch through `InverseQecOptions { keep_measurements: true }`, pinned `two_to_one`, pinned `m_det`, pinned `mpp`, direct pinned `mpad` inverse behavior, pinned `mzz`, pinned `obs_include_pauli`, pinned `noisy_m`, pinned `noisy_mr`, pinned `noisy_mr_det`, pinned `pass_through`, multi-target record remapping where selected, sparse detector subsets for the no-flow packet, duplicate detector-record parity where selected, selected duplicate MPAD record parity, selected duplicate MPAD observable-id record parity, empty-detector behavior where selected, and tag or coordinate preservation where applicable. `flattened` applies Stim-style coordinate shifts, drops `SHIFT_COORDS`, preserves instruction tags, drops repeat tags, and rejects excessive materialized expansion; `without_noise` strips measurement noise, drops ordinary noise, and converts heralded noise to deterministic `MPAD` records; `decomposed` covers public ISWAP, MPP, SPP, pair-measurement, tag-preservation, noise-preservation, annotation-preservation, constant-MPP, anti-Hermitian rejection cases, and selected MPP or pair-measurement flow-generator preservation. The feedback helper rejects excessive repeat work and unsupported classical controlled gates while broader repeat-contained feedback beyond these selected repeat cases remains deferred and requires an explicit plan revision to reopen. The flow time-reversal helper keeps pure unitary repeats folded and uses one `ReverseFlowTransition` and sparse reverse-tracker engine for supported Clifford, measurement, reset, measure-reset, pair-measurement, MPP, MPAD, detector, observable, coordinate, ordinary-noise, and multi-instruction circuits. It batches returned-flow validation, caps tableau validation at 8,192 qubits before sparse fallback, skips recursively empty repeat counts, preserves tags and shifted coordinates, supports generated surface-code reversal and `TimeReversedForFlowsOptions { dont_turn_measurements_into_resets: true }`, expands measurement-rich repeats only after a checked one-million-instruction budget, rejects absolute-relative record aliases like pinned Stim, and keeps feedback, heralded record reversal, and duplicate collapse targets fail-closed. PFM-B1 is complete for the selected Rust transform scope: all nineteen ledger cases have distinct selectors, clean committed-HEAD allocation evidence, final audit, and GPT-5.6/max review closure. Duplicate reset and measure-reset targets remain fail-closed outside that selected scope under [plans/pfm2-time-reverse-duplicate-target-boundary-scope.md](plans/pfm2-time-reverse-duplicate-target-boundary-scope.md), and any other behavior outside the ledger requires an explicit plan revision. |
| Reference samples and determined measurements | Done for current Rust API surface | `Circuit::reference_sample`, `Circuit::reference_sample_tree`, `Circuit::count_determined_measurements`, `CompiledSampler::reference_sample`, `ReferenceSampleTree`, and the free `count_determined_measurements` helper expose the current Rust subset and are covered by `pf1-circuit-rust-api`. Python bit-packed return shapes and full Python API parity are deferred. |
| Circuit exports | Deferred | QASM, Quirk, Crumble, and diagram exports are explicitly future work. |
| Built-in circuit generation | Done for current CLI and core generator surface | Repetition-code, rotated/unrotated surface-code, and color-code generation are implemented in [../crates/stab-core/src/circuit_generation.rs](../crates/stab-core/src/circuit_generation.rs) and `stab gen`. See [plans/m7-completion-report.md](plans/m7-completion-report.md). |

## 5. Detector Error Model Features

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| DEM construction and mutation | Done for current Rust API surface | `DetectorErrorModel::new`, `len`, `is_empty`, `clear`, `append_from_dem_text`, `push_instruction`, `push_repeat_block`, `DemInstruction` constructors, `DemRepeatBlock::new`, typed `Probability`, typed `RepeatCount`, and derived `Clone` exist and are covered by `pf1-dem-rust-api`. Python-style list operations, concatenation operators, repetition operators, and exact Python API shape remain deferred. |
| DEM introspection | Done for selected Rust API scope | Detector, observable, error, and final-shift summaries; full and selected coordinate maps; typed item and range iteration; and lazy adjusted flattened iteration are implemented. PFM-B3 supplies shared folded traversal and resource evidence for the bounded-result queries. Python binding shape and deferred diagram products are not active Rust blockers. |
| DEM transforms | Done for selected Rust API scope | `without_tags` and `rounded` use direct compact recursive transforms, preserve repeat structure, allocate only the transformed body plus recursion state, and retain programmatic depth-257 behavior; rounding changes only error probabilities and retains rounded-to-zero mechanisms. `flattened` and `iter_flattened_instructions` preserve adjusted detector and coordinate semantics, with the materialized `flattened` result retaining its explicit expansion cap. Python operators, diagrams, and other deferred product surfaces remain outside this Rust closure. |
| DEM sampling | Done for current Rust and CLI surface | `CompiledDemSampler` and `stab sample_dem` support deterministic, noisy statistical, observable side output, sampled-error output, replay, and current result formats. PFM-B3 compiles through the shared folded traversal; direct detection-event sampling keeps compact repeat structure and retains sampler-specific parity folds for eligible zero-shift stochastic bodies. Sampled-error output and replay keep Stim-compatible flat error-bit order and therefore retain materialized width and application-work caps for shapes whose requested error record is inherently expanded. See [../crates/stab-core/src/dem_sampler.rs](../crates/stab-core/src/dem_sampler.rs), [../crates/stab-cli/src/sample_dem.rs](../crates/stab-cli/src/sample_dem.rs), [plans/m11-progress-report.md](plans/m11-progress-report.md), [plans/rpf4-dem-sampler-progress-report.md](plans/rpf4-dem-sampler-progress-report.md), and [plans/pfm-b3-folded-dem-traversal-progress-report.md](plans/pfm-b3-folded-dem-traversal-progress-report.md). |
| Streaming DEM sampling | Done for current Rust and CLI surface | `CompiledDemSampler::try_for_each_detection_event_with_seed`, `try_for_each_detection_event_and_error_with_seed`, and `try_for_each_detection_event_from_error_records` provide public visitor APIs that reuse per-shot buffers and stop on visitor errors, while `stab sample_dem` streams detector output, observable side output, sampled-error output, and replayed-error copying through bounded writers. Existing materialized Rust APIs remain available and keep their documented limits. Exact Python `CompiledDemSampler.sample_write` API shape remains deferred under the sampler and converter classes row. See [plans/post-beta-fix-report.md](plans/post-beta-fix-report.md) and [plans/pfm0-sampling-streaming-evidence-lock.md](plans/pfm0-sampling-streaming-evidence-lock.md). |
| DEM analysis and shortest graphlike error | Done for selected Rust API scope | Graphlike, hypergraph, SAT/WCNF, shortest-error, and ErrorMatcher filter APIs use the shared folded DEM visitor for the seven PFM-B3 contracts, while PFM-B5 closes the selected generated analyzer and finite search corpora. Eligible zero-shift bodies are folded or skipped; shifted active bodies retain independent traversal, graph-construction, persistent graph-payload, state, transition, edge-incidence, and SAT materialization caps; the SAT output-byte guard remains redundant defense in depth; and sparse direct-DEM search preserves original detector ids. Diagrams, full ErrorMatcher provenance, and Python product shape remain deferred instead of active Rust blockers. See [plans/pfm-b3-folded-dem-traversal-progress-report.md](plans/pfm-b3-folded-dem-traversal-progress-report.md) and [plans/pfm-b5-analyzer-search-progress-report.md](plans/pfm-b5-analyzer-search-progress-report.md). |
| DEM diagrams | Deferred | Match-graph SVG, 3D, and HTML diagrams remain future work. |

## 6. Sampling, Conversion, And Simulation Features

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| Compiled measurement sampling | Done for current Rust and CLI surface | `CompiledSampler` in [../crates/stab-core/src/sampling](../crates/stab-core/src/sampling) and `stab sample` support deterministic and statistical sampling, output formats, seeds, skip-reference, and skip-loop-folding semantics owned by M8. |
| Measurement-sampler Python API parity | Deferred | No Python `CompiledMeasurementSampler` binding is implemented. |
| Detector sampling | Done for selected Rust and CLI surface | `sample_detection_events`, `try_for_each_sampled_detection_event`, and `stab detect` are implemented in [../crates/stab-core/src/detection.rs](../crates/stab-core/src/detection.rs) and [../crates/stab-cli/src/detection.rs](../crates/stab-cli/src/detection.rs). Non-frame and selected frame-path sweep-conditioned circuits use omitted all-false sweep bits for the current execution subset, including frame-path Pauli-observable circuits with sweep-controlled `CX` and `CY` qubit targets, sweep-controlled `CZ`, `CZ` bit/bit no-op groups, qubit/sweep `XCZ` or `YCZ` no-op groups, and measurement-record `XCZ` or `YCZ` feedback equivalent to `CX` or `CY` feedback. Non-frame detection sampling validation rejects Stim-parsed `CX q sweep[k]` and `CY q sweep[k]` target order like pinned Stim execution. Pinned Stim v1.16.0 has no `stim detect --sweep` flag, so the current CLI surface is closed for sweep input. Exact Python `CompiledDetectorSampler` class shape, sweep-bit API parity, and any future explicit Rust compiled detector-sampler product remain deferred under the sampler and converter classes row. See [plans/m9-completion-report.md](plans/m9-completion-report.md), [plans/rpf3-sweep-gate-progress-report.md](plans/rpf3-sweep-gate-progress-report.md), and [plans/pfm0-sampling-streaming-evidence-lock.md](plans/pfm0-sampling-streaming-evidence-lock.md). |
| Measurement-to-detection conversion | Partial | `CompiledDetectionConverter`, `convert_measurements_to_detection_events`, additive sweep-aware conversion, streaming visitor conversion, and `stab m2d` are implemented for the current detector-conversion subset. `m2d --sweep`, `--sweep_format`, and scoped `--ran_without_feedback` are supported, including source-owned CLI sweep-record coverage for `01`, `b8`, `r8`, `hits`, `dets`, and input-only `ptb64`, sampler-backed rejection of `CX q sweep[k]` and `CY q sweep[k]` target order, selected `XCZ`/`YCZ` measurement-record feedback equivalent to `CX`/`CY`, selected bounded feedback loop refolding, and selected nested bounded-repeat feedback detector-parity evidence, while full transform API parity and broader sweep-conditioned simulator surfaces remain partial. Broader repeat-contained feedback beyond the selected cases remains deferred and requires an explicit plan revision to reopen. |
| Detector-analysis utility APIs | Done for selected Rust API scope | `circuit_detecting_regions` supports the simple H/CX/MXX detecting-region case with typed detector and tick inputs, bounded repeat-tick traversal, additive detector and logical-observable `DemTarget` filters, dense-capped default-like all-target/all-tick helpers, the pinned `MX`, `MZZ`, and tagged ordinary-noise detecting-region examples, generated repetition-code all-target and all-tick selection with selected exact detector and observable regions, selected generated memory-Z and memory-X surface-code all-target and all-tick helper counts plus exact D0, D4, and L0 regions, the full single-qubit Clifford gate set with plain qubit targets, fixed two-qubit tableau-backed Clifford gates with plain qubit-pair targets, selected measurement-record feedback target placements for `CX`, `CY`, `CZ`, `XCZ`, and `YCZ`, selected gate-order-valid sweep-controlled Pauli sign-only no-op groups for `CX`, `CY`, `CZ`, `XCZ`, and `YCZ` plus selected `CZ` classical-only bit-bit groups, explicit fail-closed validation for non-`CZ` sweep/sweep, record/sweep, and record/record groups, ordinary non-record-producing noise no-op traversal, inverted targets for promoted measurement and reset-measurement families, `MPAD` measurement pads, heralded record-producing noise record dropping, `MPP` Pauli-product measurement targets, `SPP` and `SPP_DAG` unitary Pauli-product target shapes, ignored anticommutation mode for the current supported detector-region subset, selected measurement-gauge ignored-mode behavior, and product-measurement gauge-cancellation behavior. `missing_detectors` supports basic reset/measure suggestions plus Gaussian row reduction for multi-record detector rows, repeated MPP and pair-measurement stabilizer-product cases, record-only observable rows, ignored Pauli observable rows, `MPAD` measurement-pad records, tableau-backed single-qubit and fixed two-qubit Clifford propagation with plain qubit target groups, `SPP` and `SPP_DAG` unitary Pauli-product analysis through decomposition, bounded repeat traversal with explicit expansion caps, selected folded final-repeat traversal for covered deterministic measurement loops with flat or bounded nested local bodies, selected observable-neutral final repeats where top-level record-only observable rows are redundant under independent detector evidence, and the pinned honeycomb and toric generated-code suffix cases. `circuit_with_inlined_feedback` covers pinned demolition feedback and interleaved-ordering exact-output cases, the supported MPP feedback-transform case, selected `XCZ`/`YCZ` measurement-record feedback, selected bounded repeat-loop refolding, and selected nested bounded-repeat feedback detector-parity preservation, and `circuit_flow_generators` covers pinned variable-target `SPP` or `SPP_DAG` unitary examples, inverted result-target `MR`/`MRX`/`MRY`, selected unitary-mixed composed measurement-rich instruction sequences, selected all-operation annotation and ordinary-noise no-op traversal, selected composed `SPP` or `SPP_DAG` unitary decomposition, exact signed-flow-set and checker-satisfaction coverage for the pinned generated all-operations fixture, bounded repeat-contained measurement-rich instruction sequences through the current flow-row and materialized flattened-operation caps, selected gate-order-valid sweep-controlled Pauli sign-only no-op groups, and mixed feedback-capable controlled-Pauli groups with classical-only no-ops plus plain-qubit tableau pairs. The detecting-region, missing-detector, flow-generator, and solver closure selected by the PFM-B4 cases in [plans/blocker-closure-ledger.json](plans/blocker-closure-ledger.json) is complete; the solver has no exhaustive measurement-subset fallback, and behavior outside those cases requires an explicit plan revision. Broader repeat-contained feedback beyond selected cases remains deferred and requires an explicit plan revision to reopen. |
| DEM sampling | Done for current Rust and CLI surface | See section 5 and M11 evidence. |
| Single-shot interactive tableau simulator | Deferred | Core tableau algebra and some simulator semantics exist for implemented workflows, but a public Python-style `TableauSimulator` API or new public Rust simulator product is outside the active non-deferred scope. Scoped simulator cross-checks exist in M12 for evidence, not as a public simulator product. |
| Batched flip-frame simulator | Deferred | Internal frame-simulator and packed-frame paths support sampling, detection, and Pauli-target observable cases, but a full public Python-style `FlipSimulator` API or new public Rust simulator product is outside the active non-deferred scope. |
| Exact random-stream parity with Stim | Deferred | The project explicitly requires statistical and semantic equivalence, not exact random-stream reproduction. |

## 7. Error Analysis, Search, And Decoder Configuration

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| Circuit-to-DEM analysis | Done for selected Rust and CLI semantics | `circuit_to_detector_error_model` and `stab analyze_errors` support the selected M10 and PF7 surface, and PFM-B5 uses one shared shifted-recurrence engine for analyzer and sparse reverse-tracker repeat discovery. Exact or semantic evidence covers giant loop-carried observables, periods 8 and 127 discovered generically, nested and coordinate repeats, ordinary and nested billion-round gauge state, generated repetition and surface-code circuits, saturating diagnostics, folded noisy `MPAD`, cumulative nested-probe admission including supported-unitary nested repeats, the inclusive sixteen-detector local-decomposition boundary, decomposition, remnant-edge blocking, unsupported-instruction fallback, and cross-iteration rejection. PFM-B5 milestone-audit and full-code-review closure are recorded in [plans/pfm-b5-analyzer-search-progress-report.md](plans/pfm-b5-analyzer-search-progress-report.md) and [plans/blocker-closure-ledger.json](plans/blocker-closure-ledger.json). Full ErrorMatcher provenance remains deferred. |
| `analyze_errors --decompose_errors` and related flags | Done for selected Rust and CLI scope | Implemented for the scoped M10 and PF7 cases, including `--block_decompose_from_introducing_remnant_edges`, `--ignore_decomposition_failures`, approximate disjoint options, gauge handling, and generic `--fold_loops` plus `--decompose_errors` repeated composite-error and remnant-edge behavior. Additional unselected analyzer families require a new exact plan rather than keeping this selected surface partial. |
| Error explanation value objects | Done for current active Rust value-object subset | Matched-error value objects and `explain_errors_from_circuit` exist in `stab-core`, and PF6 covers `ExplainedError` and `CircuitErrorLocation` canonicalization of DEM terms, circuit locations, flipped Pauli products, and flipped measured observables through `pf6-matched-error-canonicalize-rust`. Future analyzer or search slices may add more value-object hardening if those outputs require it, but full ErrorMatcher provenance, generated surface-code repeat matching, heralded matching, repeat-contained noise stack frames, and `stim explain_errors` CLI UX remain deferred. |
| `stim explain_errors` CLI | Deferred | Explicitly deferred in [plans/rust-stim-drop-in-rewrite.md](plans/rust-stim-drop-in-rewrite.md) and [plans/post-beta-fix-report.md](plans/post-beta-fix-report.md). |
| Shortest graphlike and hypergraph logical-error search | Done for selected Rust API semantics | PFM-B5 covers ten graphlike, eleven hypergraph, and one shared traversal-resource case spanning no-error, exact zero-probability diagnostics, exact distance one through three, bounded hyperedges, generated rotated-surface and repetition codes, sparse high detector IDs, sparse high observable IDs, source-mechanism membership, variable-sized state admission, canonical target signatures, and fail-closed ungraphlike behavior. Graph construction allocates only touched detector nodes and compressed observable slots; expanded nonzero error mechanisms, per-error and aggregate target occurrences, effective detector nodes, unique graph edges, persistent graph terms, unique search states, attempted transitions, per-state terms, aggregate stored state terms, hyperedge degree, and total edge incidences have separate tested limits; two-phase admission checks graph caps before persistent edge or index reservation; collision-checked arena indexing avoids linear edge scans without duplicating edge payloads; and hypergraph adjacency stores each edge once. The clean d25/r25 and d11/r1000 benchmark rows remain report-only at 4.647x and 4.279x Stim. See [plans/pfm-b5-analyzer-search-progress-report.md](plans/pfm-b5-analyzer-search-progress-report.md). |
| SAT or WCNF encoding | Done for selected Rust API semantics | `shortest_error_sat_problem` and `likeliest_error_sat_problem` are exposed through [../crates/stab-core/src/lib.rs](../crates/stab-core/src/lib.rs). PFM-B5 provides twelve independently selected exact finite-corpus cases, including early trivial UNSAT for a large detector-only shifted repeat and Stim's stored-clause header behavior for a low-quantization clause whose line is omitted, plus independently reachable admission limits for mechanisms, total target occurrences, variables, stored clauses, and literals. The conservative 128 MiB output guard remains redundant defense in depth behind those stricter limits instead of an independently reachable contract. Sparse-ID and large folded-repeat encodings intentionally preserve semantic satisfiability with bounded representation instead of claiming universal byte-exact Stim output. |
| Sparse reverse detector-frame tracking | Done for selected internal and Rust-consumer scope | The shared sparse reverse tracker covers the selected Clifford, feedback, `SPP` or `SPP_DAG`, supported-unitary repeat, shifted measurement or detector repeat, analyzer, unsigned-flow, and matched-error consumers. PFM-B5 adds generated folded-versus-unrolled analyzer loops and closes the two independently selected tracker cases without fixture-specific periods. Full ErrorMatcher provenance and any new public simulator product remain deferred. See [plans/pfm-b5-analyzer-search-progress-report.md](plans/pfm-b5-analyzer-search-progress-report.md). |

## 8. Stabilizer Algebra Features

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| Owned Pauli strings | Done for scoped Rust algebra surface | `PauliString`, parsing, multiplication, commutation, iteration, and text round trips are implemented in [../crates/stab-core/src/stabilizers](../crates/stab-core/src/stabilizers). See [plans/m6-completion-report.md](plans/m6-completion-report.md). |
| Flexible Pauli strings | Done for scoped Rust algebra surface | `FlexPauliString` is exposed and covered by M6. |
| Clifford strings | Done for scoped Rust algebra surface | `CliffordString` is exposed and covered by M6, with M12 direct benchmark optimizations. |
| Tableaux and tableau iterators | Done for scoped Rust algebra surface | `Tableau`, `TableauIterator`, composition, inversion, named gates, and conversions needed by current workflows are implemented. |
| Flows | Done for selected Rust API scope | `Flow` parsing, canonicalized measurement-index terms, included-observable terms, display, Stim-compatible Pauli-basis/width/sign ordering, and multiplication are implemented and covered by `coverage-stabilizers-flow`. Unitary circuit flow generators including pinned variable-target `SPP` and `SPP_DAG` examples, scoped measurement, reset, inverted result-target measure-reset, pair-measurement, Python multi-target measurement and `MPP`, nonconstant and constant `MPP`, selected unitary-mixed composed measurement-rich instruction sequences, selected all-operation annotation and ordinary-noise no-op traversal including folded high-index and large-repeat noise-only circuits without unrelated measurements plus mixed unitary-plus-noise propagation, selected composed `SPP` or `SPP_DAG` unitary decomposition, exact signed-flow-set and checker-satisfaction coverage for the pinned generated all-operations fixture, bounded repeat-contained measurement-rich instruction sequences, measurement-record feedback, selected gate-order-valid sweep-controlled Pauli sign-only no-op groups plus selected mixed sweep and plain-qubit groups, `MPAD` with pad values excluded from internal simulated-qubit width and asymmetric values associated with forward records in pinned order, duplicate reset and measure-reset target generators with final-record parity and inversion signs, and selected single- and multi-target heralded-noise MPP flow generators, general GF(2) `solve_for_flow_measurements` over supported generator tables, including measured-idle, multi-target measurement and `MPP`, fewer-measurements heuristic, repetition-code cases, and Stim-compatible Pauli-projection semantics that ignore pre-existing query measurement and observable terms, folded unitary and tracker-driven measurement-rich `time_reversed_for_flows` through the nineteen PFM-B1 cases, including general multi-instruction detector and observable remapping, generated surface-code reversal, the `dont_turn_measurements_into_resets` option, bounded measurement-rich repeats, the MPAD constant, record, observable, duplicate-id, adjacent-Clifford, and 1/8/64 scale matrices, batched input and returned-flow validation, high-index sparse validation, and explicit record-alias, feedback, heralded-record, and duplicate-target rejection, Stim v1.16.0 `GateData.flows` metadata for implemented gate-table metadata shapes, unsigned `check_if_circuit_has_unsigned_stabilizer_flows` measurement-record and observable dependency cases, unsigned `circuit_has_unsigned_stabilizer_flow` plus `circuit_has_all_unsigned_stabilizer_flows` helpers, the additive unsigned `check_unsigned_stabilizer_flows_with_diagnostics` helper for output mismatches, input mismatches, out-of-range measurement records, collapse anticommutation, and unsupported-circuit reasons, and scoped signed `sample_if_circuit_has_stabilizer_flows` checks for unitary, measurement-record, record-backed observable, Pauli-backed observable, inverted Pauli-backed observable, and inverted record-backed observable cases are implemented for the scoped Rust semantics. The flow-generator, solver, diagnostic, and folded-repeat closure is complete under thirty-three independently evidenced `pfm5-flow-*` cases in [plans/blocker-closure-ledger.json](plans/blocker-closure-ledger.json); the solver has no exhaustive measurement-subset fallback, and behavior outside those cases requires an explicit plan revision. Signed sampled Python `has_flow` or `has_all_flows` binding parity and Python API parity remain deferred. See [plans/rpf5-flow-progress-report.md](plans/rpf5-flow-progress-report.md). |
| Public borrowed Pauli string refs | Deferred | M6 explicitly uses owned Rust APIs instead of cloning Stim's borrowed binding-oriented shape. |
| Conversion to or from numpy, state vectors, and arbitrary unitaries | Deferred with scoped Rust subset | The M6-owned `unitary_to_tableau` subset and M12 graph/vector simulator cross-checks exist as Rust semantic evidence, but Python/numpy conversion APIs, state-vector APIs, arbitrary unitary conversion APIs, `tableau_to_unitary`, and public graph/vector simulator products remain deferred. See [plans/m6-completion-report.md](plans/m6-completion-report.md), [plans/m12-progress-report.md](plans/m12-progress-report.md), and [plans/pfm0-matrix-simulator-deferral-evidence-lock.md](plans/pfm0-matrix-simulator-deferral-evidence-lock.md). |
| Graph simulator and vector simulator public APIs | Deferred | M12 added scoped cross-check tests, but new public graph/vector simulator APIs are explicitly deferred. |

## 9. Diagrams And Visualization

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| Circuit timeline diagrams | Deferred | `stim diagram` is intentionally deferred. |
| Detector-slice diagrams | Deferred | Diagram surfaces are future work. |
| DEM match-graph diagrams | Deferred | DEM diagram surfaces are future work. |
| Interactive HTML and Crumble diagram surfaces | Deferred | Crumble and interactive diagrams are future ecosystem or visualization work. |

## 10. Built-In Circuit Generation

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| `repetition_code:memory` | Done | Implemented in core generation and `stab gen`, with exact oracle rows and structural generator tests. |
| `surface_code:rotated_memory_x` and `surface_code:rotated_memory_z` | Done | Implemented and covered by M7 exact and structural evidence. |
| `surface_code:unrotated_memory_x` and `surface_code:unrotated_memory_z` | Done | Implemented and covered by M7 exact and structural evidence. |
| `color_code:memory_xyz` | Done | Implemented and covered by M7 exact and structural evidence. |
| Generator parameters and noise knobs | Done for M7 surface | Distance, rounds, output path, and supported noise knobs are implemented through typed generator parameters in [../crates/stab-core/src/circuit_generation.rs](../crates/stab-core/src/circuit_generation.rs). |
| Python `stim.Circuit.generated` | Deferred | No Python bindings yet. |

## 11. Command-Line API

| Stim command | Stab status | Evidence and notes |
| --- | --- | --- |
| `stim gen` | Done for supported M7 families and tasks | `stab gen` and legacy `--gen` are implemented in [../crates/stab-cli/src/lib.rs](../crates/stab-cli/src/lib.rs). |
| `stim convert` | Done for result-format CLI parity plus Stab `.stim` extension | `stab convert` supports `01`, `b8`, `r8`, `hits`, `dets`, and `ptb64` conversions with explicit counts, `--dem`, `--circuit`, unique `--types` letters, `--obs_out`, and `--obs_out_format`. The Stab-specific `.stim -> .stim` canonical conversion remains supported only for `--in_format=stim --out_format=stim`. |
| `stim sample` | Done for current M8/M12 surface | `stab sample` supports current flags, deterministic and noisy statistical behavior, output formats, seed handling, skip-reference, skip-loop-folding, and streaming output. |
| `stim detect` | Done for current M9/M12 surface | `stab detect` supports shots, input/output paths, detector/observable routing, relevant formats including supported `ptb64`, seeds, and streaming output. |
| `stim m2d` | Done for selected PF7 CLI surface | `stab m2d` supports text, `b8`, `r8`, `hits`, `dets`, and `ptb64` input where accepted, plus detector and observable outputs where accepted. `--sweep` and `--sweep_format` stream sweep records for the current sweep-conditioned conversion subset, with CLI evidence for `01`, `b8`, `r8`, `hits`, `dets`, and input-only `ptb64` sweep records; scoped `--ran_without_feedback` applies feedback inlining before conversion, and `ptb64` output is rejected like pinned Stim v1.16.0. PF7 selected CLI evidence covers `--circuit`, `--in`, `--out`, `--sweep`, and `--obs_out` path IO, path-error precedence before converter setup, selected default detector-only `dets` output, `--append_observables`, `--skip_reference_sample`, observable side-output widths, Pauli-target observable annotations, selected format and width failures, writer failure propagation, stdout behavior, stderr class, exit status, and existing M9 sweep, feedback, format, and resource behavior. Broader detector-converter API parity remains tracked by the measurement-to-detection conversion row. |
| `stim analyze_errors` | Done for selected PF7 CLI surface | `stab analyze_errors` implements the staged M10 and PF7 flag and path-IO surface, including default analysis, generic `--fold_loops`, gauge handling, approximate disjoint errors, decomposition controls, input and output paths, malformed input, stdout and stderr classes, and exit status. PFM-B5 closes the selected generated and giant-loop analyzer semantics behind this command. Full ErrorMatcher provenance and `stim explain_errors` remain deferred, while deprecated `--detector_hypergraph` is intentionally unsupported. |
| `stim sample_dem` | Done for current M11/M12 surface | `stab sample_dem` supports detector output, observable side output, error output, replay input, result formats including `ptb64` where accepted, and streaming CLI writers. Hidden or compatibility observable-routing aliases are implementation conveniences and are not counted as additional Stim parity surface. |
| `stim diagram` | Deferred | No Stab command. |
| `stim explain_errors` | Deferred | No Stab command, despite scoped core matched-error support. |
| `stim repl` | Deferred | No Stab command. |
| `stim help` command | Done for Stab-native structural help | `stab help [topic]` and top-level `stab --help [topic]` are normalized to Stab-native help for implemented commands, result formats, and gate names. Exact byte-for-byte pinned-Stim help text remains intentionally out of scope. |
| Legacy top-level command flags | Done for selected PF7 surface | `--gen`, `--convert`, `--sample`, `--detect`, `--m2d`, and `--analyze_errors` aliases are normalized in [../crates/stab-cli/src/lib.rs](../crates/stab-cli/src/lib.rs), with PF7 executable coverage for accepted aliases, selected multiple-mode conflicts, explicit `--detector_hypergraph` rejection, help-topic absence, and fail-closed behavior for selected unimplemented legacy-style flags. Other legacy and deprecated Stim spellings are intentionally unsupported unless a future plan selects them. Users should use `stab analyze_errors` instead of deprecated analyzer spellings. |

## 12. Python API

| Python feature area | Stab status | Evidence and notes |
| --- | --- | --- |
| Top-level `stim` functions | Deferred | No Python module exists. Rust equivalents exist for some target constructors and helpers but are not exposed as Python. |
| `stim.Circuit` class | Deferred with Rust subset | `Circuit` exists as a Rust type, but Python class operators, binding-style mutation API, file-like object helpers, transforms, diagram/export methods, and properties are not bound. |
| `stim.DetectorErrorModel` class | Deferred with Rust subset | `DetectorErrorModel` exists as a Rust type with current Rust construction, introspection, coordinate, tag-stripping, materialized `flattened`, and `rounded` APIs, but Python class operators, diagrams, Python binding shape, and full method parity are not bound. |
| Sampler and converter classes | Deferred with Rust subset | Rust `CompiledSampler`, `CompiledDetectionConverter`, and `CompiledDemSampler` exist, and current selected detector sampling uses Rust free-function and visitor APIs. Python `CompiledMeasurementSampler`, `CompiledDetectorSampler`, `CompiledMeasurementsToDetectionEventsConverter`, and `CompiledDemSampler` classes do not exist. Exact Python `CompiledDetectorSampler` sweep-bit API shape, Python `CompiledDemSampler.sample_write` ergonomics, and any future explicit Rust `CompiledDetectorSampler` product are deferred. |
| Error explanation classes | Deferred with Rust subset | Rust matched-error value objects exist for scoped M10 support, but Python `ExplainedError` and related classes are not exposed. |
| Stabilizer, gate metadata, and simulator classes | Deferred with Rust subset | Rust Pauli, Clifford, Tableau, Flow, and bounded `Gate` metadata APIs exist. Python `GateData`, `TableauSimulator`, `FlipSimulator`, numpy conversions, and full class operator parity are not exposed. |

## 13. JavaScript And WASM API

| JS/WASM feature area | Stab status | Evidence and notes |
| --- | --- | --- |
| WASM build and JS package | Deferred | No WASM build or JS package exists. |
| JS `Circuit`, `Tableau`, `PauliString`, and `TableauSimulator` | Deferred | Rust core APIs can inform future bindings, but no JS bindings are present. |
| Crumble browser editor | Deferred | Future ecosystem work. |

## 14. Ecosystem And Glue Packages

| Ecosystem surface | Stab status | Evidence and notes |
| --- | --- | --- |
| `stimcirq` | Deferred | No Cirq integration exists. |
| `sinter` | Deferred | No decoding-statistics package exists. |
| Crumble | Deferred | No browser editor integration exists. |
| `stimflow` | Deferred | No separate flow toolkit exists. |
| ZX and lattice-surgery glue | Deferred | No ZX or lattice-surgery integration exists. |
| QASM and Quirk exports | Deferred | Explicitly future work unless drop-in scope expands. |

## 15. Packaging, Build, And Documentation Surfaces

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| Cargo workspace | Done | The repo is a Cargo workspace with `stab-core`, `stab-cli`, `stab-oracle`, `stab-bench`, and `ops` crates. |
| Operational command surface | Done | The repo uses `just` and modular `justfiles`, with complex logic in Rust ops crates. |
| Python packaging | Deferred | No `pyproject.toml` or Python package exists for Stab bindings. |
| JS packaging | Deferred | No JS package exists. |
| Generated API docs | Done for Rust API reference workflow | `just docs::api` generates workspace Rust API documentation under `target/doc/`, and `just docs::api-check` runs rustdoc with warnings denied. Python stubs remain deferred with Python bindings, and generated feature/status matrix tooling remains a separate future documentation surface. |
| Feature and roadmap docs | Done for current planning surface | Roadmap, test-porting plan, completion reports, lessons learned, GOAL, and this checklist live under [plans](plans) and [docs](.). |

## 16. Test And Benchmark Surface

The rows below describe current evidence, not the planned comprehensive qualification claim.
The CQ and PQ plans must split partial checklist rows into exact implemented and unimplemented children, create a case-level correctness disposition for every selected contract, and assign every performance-relevant implemented contract a faithful benchmark disposition before this section can claim comprehensive qualification.
Planning those suites does not change a feature's current availability status until their executable evidence lands.

| Feature | Stab status | Evidence and notes |
| --- | --- | --- |
| Upstream test inventory and porting plan | Done as a file-level hierarchy and CQ0 frozen case inventory | [plans/stim-test-porting-plan.md](plans/stim-test-porting-plan.md) groups upstream files and historical Rust port priorities. CQ0 in [plans/comprehensive-correctness-qualification-plan.md](plans/comprehensive-correctness-qualification-plan.md) freezes 2,886 upstream records, including 165 exact blocker-ledger subcases, 657 multi-domain relevance records, 23 non-executable dynamic parameter families, and 1,922 default-feature public Rust API items in `oracle/qualification-manifest.json`; 424 evidence owners are implemented, 17 are evidence-close, and 3,568 remain planned for domain qualification, including thirteen explicit cross-cutting resource families. The completed selected CQ2 `.stim` slice has 32 implemented evidence parents and zero planned owners: 24 qualification parents map 44 exact upstream owners and nine exported-API owners, while eight oracle-fixture parents remain direct evidence. |
| Oracle fixture matrix | Done for current implemented surfaces and CQ1 harness acceptance | `oracle/fixtures/manifest.csv`, `ops/oracle`, and milestone reports track current exact, statistical, structural, and semantic-mining rows. CQ0 references 440 implemented oracle rows, 165 source-owned blocker-ledger cases, and one exact hostile-path regression as current primary evidence. The CQ1 comparator, selector, statistical, property, resource, receipt, report, and PR/full/soak execution harness is implemented; clean current evidence from revision `ec1ae89e1ae42be92f87d13cbe15cc3aacdfcb77` passes 302 of 302 PR cases and 441 of 441 full and soak cases, as recorded in [plans/cq2-deterministic-qualification-progress-report.md](plans/cq2-deterministic-qualification-progress-report.md). CQ2 through CQ6 still own feature-domain expansion. |
| Benchmark manifest and primary beta gate | Done for current performance infrastructure; PQ0 inventory complete | `benchmarks/manifest.csv`, `benchmarks/m12-primary-thresholds.json`, `benchmarks/m12-primary-beta-waivers.json`, and M12/post-beta reports provide current primary evidence. PQ0 freezes all 161 inherited rows, 126 checklist rows, 1,922 default-feature Rust API items, 23 upstream perf files, and 74 upstream benchmark symbols in `benchmarks/stim-qualification-suite.json`; current PQ1 PR, full, and soak runs are report-only harness evidence, while PQ2 through PQ7 still must add faithful paired, process, memory, scaling, and final product qualification evidence. |
| Current beta performance gate | Done for current report state | The expanded clean 1.25x beta evidence records 80 comparable rows passing and 5 checked no-ratio waivers across 85 primary rows. |
| Tests for deferred Python, JS, diagrams, and ecosystem packages | Deferred | These are intentionally future work and should not be used as blockers for the current Rust/CLI beta surface. |

## 17. Highest-Priority Remaining Feature Gaps

This section is a short triage view of gaps that are visible after mapping the Stim inventory onto Stab.

| Gap | Status | Why it matters |
| --- | --- | --- |
| Full circuit transform API parity | Done for selected Rust API scope | Rust `flattened`, `flattened_operations`, `without_noise`, `decomposed`, scoped `inverse_qec` no-flow reset-measure-detector, exact two-to-one detector-flow, exact `m_det` two-detector packet, exact noiseless MPP all-record identity-parity detector-flow, selected MPAD record-tail rewrite, exact noisy MZZ detector-flow, selected observable Pauli include packet, selected noisy `M`/`MX`/`MY` measurement-only reversal, selected noisy `MR`/`MRX`/`MRY` measure-reset-only reversal, exact noisy measure-reset detector-flow, and measure-reset pass-through target-list rewrites for one detector, folded unitary and tracker-driven measurement-rich `time_reversed_for_flows`, and scoped `with_inlined_feedback` are implemented for the owned RPF2 subset, including selected MPP or pair-measurement decomposition flow-generator preservation, selected measurement-ordering, selected empty-flow plus Pauli-only, measurement-record, and observable MPAD record-tail reversal, selected duplicate MPAD observable-id record parity, selected plain reset-to-measurement over one or more unique qubit targets, selected measurement-to-reset including the selected `dont_turn_measurements_into_resets` single-measurement option, selected measure-reset flow reversal over one or more unique qubit targets including inverted result targets, selected `MZZ` plus plain-qubit unitary suffix reversal, exact pinned `flow_flip` packet, pinned demolition feedback and interleaved-ordering exact-output cases, selected `XCZ`/`YCZ` measurement-record feedback, selected bounded feedback loop refolding, and selected nested bounded-repeat feedback detector-parity preservation. PFM-B1 is complete for the selected Rust transform scope: all nineteen reverse-flow and QEC-transform cases have distinct selectors, clean committed-HEAD allocation evidence, final audit, and GPT-5.6/max review closure. Unselected `inverse_qec`, feedback, heralded-record, and export surfaces require a new exact plan instead of reopening the finite PFM-B1 ledger. Broader repeat-contained feedback remains deferred and requires an explicit plan revision to reopen; QASM, Quirk, and Crumble export also remain deferred. |
| Full DEM public API parity | Done for selected Rust API scope | Rust count, coordinate, iterator, compact transform, sampler, graphlike, hypergraph, SAT/WCNF, and ErrorMatcher filter surfaces have source-owned PFM-B3 traversal evidence. Remaining differences are deferred Python ergonomics and operators, diagram products, and full ErrorMatcher provenance, not an active shared-traversal blocker. |
| Broader sweep-conditioned simulator and analysis parity | Partial | Public `m2d --sweep` covers accepted sweep-record formats `01`, `b8`, `r8`, `hits`, `dets`, and input-only `ptb64`, non-frame and selected frame-path `detect` omitted-sweep default-false sampling plus measurement-record `XCZ` or `YCZ` feedback are implemented, sampler-backed execution rejects `CX q sweep[k]` and `CY q sweep[k]` while accepting neighboring sweep-first `CX` or `CY` and both-order `CZ` sweep/qubit cases, and selected analyzer sweep-control plus `CZ` classical-only no-op behavior and invalid target-position rejection behavior are implemented for current subsets under [plans/pfm3-analyzer-sweep-boundary-scope.md](plans/pfm3-analyzer-sweep-boundary-scope.md), but the analyzer sweep boundary is evidence-closed by `pfm3-analyzer-sweep-matrix`; future shapes require an explicit plan revision, while Python detector-sampler sweep APIs and every remaining sweep target shape remain broader work. Pinned Stim v1.16.0 does not expose `stim detect --sweep`, so that flag is not a current CLI parity gap. |
| Full feedback-inlining transform parity | Partial | `m2d --ran_without_feedback`, `circuit_with_inlined_feedback`, and scoped Rust `Circuit::with_inlined_feedback` cover the supported top-level Pauli and MPP feedback subset, pinned `demolition_feedback` and `interleaved_feedback_does_not_reorder_operations` exact-output cases, selected `XCZ`/`YCZ` measurement-record feedback equivalent to `CX`/`CY`, selected pinned bounded repeat-loop refolding, and nested bounded-repeat `CY`/`CZ` detector-parity cases while rejecting excessive repeat work and unsupported classical controlled gates. Broader repeat-block feedback beyond these selected cases remains deferred and requires an explicit plan revision to reopen. |
| Full ErrorMatcher provenance and `explain_errors` CLI | Deferred | Core value objects exist, but full stack-frame provenance and CLI UX remain future work. |
| Public interactive `TableauSimulator` and `FlipSimulator` APIs | Deferred | Current internals support implemented workflows, but public simulator API parity is not exposed. |
| Diagrams and visualization | Deferred | Stim's diagram command and Python diagram APIs are a large independent rendering surface. |
| Python bindings | Deferred | Needed for eventual Stim drop-in package parity, but intentionally after Rust and CLI surfaces mature. |
| JS/WASM and ecosystem packages | Deferred | Separate product and integration projects, not part of current core Rust/CLI beta. |
| Generated feature/status matrix tooling | Deferred | Rust API documentation is already covered by the section 15 `Generated API docs` row. Generated feature/status matrix tooling remains outside the current Rust and CLI beta closure; plan it separately if it becomes a product requirement. |

## 18. Maintenance Notes

- When a Stab feature moves from `Partial`, `Deferred`, or `Missing` to `Done`, update this checklist in the same change set as the implementation and tests.
- When a deferred surface becomes in scope, first update [plans/rust-stim-drop-in-rewrite.md](plans/rust-stim-drop-in-rewrite.md) and [plans/GOAL.md](plans/GOAL.md), then add tests or oracle rows before claiming checklist completion.
- Do not use this checklist to weaken existing milestone done criteria; milestone-specific reports remain the evidence source for whether a planned slice is complete.
