# Partial Feature Closure Inventory

## Purpose

This inventory is the source-owned map from `docs/stab-feature-checklist.md` partial rows to the non-deferred partial feature milestones in `docs/plans/non-deferred-partial-feature-milestones.md`.
It still records the earlier PF milestone ids because existing oracle rows, benchmark rows, and progress reports use those ids.
It exists so later implementation agents do not have to infer active scope from broad Stim source files or from checklist prose.

Every active item below has an owner milestone, owner crate or surface, upstream source, first test strategy, oracle status, benchmark plan, and explicit exclusions.
Rows that are partial only because deferred products are absent are recorded in the exclusion table and must not block the current Rust and CLI beta closure work.

## Status Classes

- `Active`: implement or tighten the named Rust or CLI surface during the matching PFM milestone in `docs/plans/non-deferred-partial-feature-milestones.md`, using the historical RPF mapping as source material.
- `Rollup`: parent checklist row that becomes complete only after its active child rows are complete.
- `Deferred-only`: partial or missing checklist row whose remaining work is intentionally outside this plan.
- `Decision`: RPF0 must lock the subcases before implementation because the checklist currently mixes active and deferred work.

## RPF Mapping

| Historical inventory milestone | Active RPF milestone | Notes |
| --- | --- | --- |
| PF1 circuit and DEM Rust API rows | RPF0 classification, RPF4 DEM follow-up | The current Rust circuit and basic DEM API slices are implemented; RPF0 keeps them classified and RPF4 owns remaining DEM transform and folded-traversal work. |
| PF1 gate metadata rows | RPF1 | RPF1 owns unsupported metadata accessors, gate-table flow metadata, and gate execution contract documentation after decomposition metadata. |
| PF2 transform rows | RPF2 | RPF2 owns circuit flattening, noise removal, decomposition, feedback inlining, time reversal for flows, and repeat traversal. |
| PF3 sweep and gate execution rows | RPF3 | RPF3 owns sweep-conditioned execution and legal-gate execution gaps. |
| PF4 DEM rows | RPF4 | RPF4 owns DEM APIs, transforms, coordinates, and folded traversal. |
| PF5 utility and flow rows | RPF5 | RPF5 owns detecting regions, missing detectors, and measurement-rich flows. |
| PF6 analyzer, search, and sparse tracker rows | RPF6 | RPF6 owns analyzer, search, sparse reverse tracker, and active matched-error value-object hardening. |
| PF7 CLI rows | RPF7 | RPF7 owns `stab m2d`, `stab analyze_errors`, accepted legacy aliases, and explicit `--detector_hypergraph` exclusion. |
| PF0/PF8 evidence rules | RPF0 and RPF8 | RPF0 owns inventory lock; RPF8 owns benchmark, audit, documentation, and rollup closure. |

## Locked RPF Subcases

This section is the RPF0 split from broad upstream files into owned, semantic-mining, deferred, and out-of-scope subcases.
Manifest-only oracle rows stay broad extraction contracts, but implementation milestones must use the locked subcases below instead of treating a whole upstream file as acceptance.

### RPF1 Gate Metadata

Owned:

- Gate decomposition metadata accessor behavior for the H/S/CX/M/R decomposition table, including representative exact text, the full supported gate set, parseability as `.stim` text, and fail-closed behavior for gates without decomposition metadata. Implemented by `pf1-gate-decomposition-metadata`; keep this row current if the public API changes.
- Measurement-rich or variable-target gate flow metadata decision, implemented by `pf1-gate-flow-metadata` for Stim v1.16.0 `GateData.flows` metadata while execution support remains tracked separately.
- A canonical gate support table separating validation support, tableau metadata, unitary metadata, flow metadata, decomposition metadata, sampler execution, detector conversion, analyzer propagation, and explicit rejection. Implemented as `docs/plans/rpf1-gate-execution-support-contract.md`; keep it synchronized with execution behavior.

Semantic-mining:

- Python `GateData` tests are used only to infer semantics for Rust accessors.

Deferred or out of scope:

- Python `GateData` object shape and binding ergonomics.
- Full circuit decomposition behavior, which belongs to RPF2.

### RPF2 Circuit Transforms

Owned:

- `flattened` and flattened-operation traversal for repeat blocks, tags, annotations, detectors, observables, measurement references, coordinate shifts, and large-repeat resource behavior. Implemented for Rust `Circuit::flattened` and `Circuit::flattened_operations` with a one-million-output materialization cap, folded shift-only repeats, exact canonical-output tests, structural operation tests, an implemented oracle row, and report-only benchmark runners.
- `without_noise` for noise removal while preserving deterministic operations, coordinates, ticks, detectors, observables, and measurement-record semantics. Implemented for Rust `Circuit::without_noise` with exact tests for noisy measurements, ordinary noise removal, heralded-noise `MPAD` replacement, tags, annotations, detectors, observables, and measurement-record preservation.
- `decomposed` for compound gates, MPP, SPP, pair measurements, grouped targets, base-gate lowering, tableau or flow semantic checks, and unsupported decomposition target errors. Rust `Circuit::decomposed` now covers public ISWAP, MPP, SPP, pair-measurement, tag-preservation, noise-preservation, annotation-preservation, constant-MPP, anti-Hermitian rejection cases, and selected MPP or pair-measurement flow-generator preservation; broader flow-driven decomposition checks should reopen only when a future exact PFM5 measurement-rich flow family is selected.
- `inverse_qec` for selected QEC inverse packets. Rust `Circuit::inverse_qec` now covers the selected reset-measure-detector, two-to-one, measure-reset pass-through, MPP, MPAD record-tail, noisy measurement, noisy measure-reset, noisy measure-reset detector-flow, `m_det`, noisy MZZ, and observable Pauli include packets, with the selected MPAD scope locked in `docs/plans/pfm2-inverse-qec-mpad-scope.md`.
- Feedback inlining for selected single-control Pauli feedback and MPP feedback cases, including pinned `demolition_feedback`, pinned `interleaved_feedback_does_not_reorder_operations`, selected `XCZ`/`YCZ` measurement-record feedback, and selected repeat-block support or explicit repeat-block rejection. A scoped Rust `Circuit::with_inlined_feedback` API is implemented for the supported top-level Pauli and MPP subset, pinned demolition and interleaved-ordering exact-output cases under oracle row `pf2-feedback-inline-pinned-upstream-rust`, selected `XCZ`/`YCZ` measurement-record feedback equivalent to `CX`/`CY`, selected bounded repeat-loop refolding, selected nested bounded-repeat feedback detector-parity preservation, excessive repeat-work rejection, and unsupported classical controlled gates; broader repeat-contained feedback parity beyond these selected cases is under-specified in `docs/plans/milestone-spec-gaps.md` until exact repeat structures, comparator behavior, resource behavior, oracle metadata, and benchmark policy are selected.
- Measurement-rich `time_reversed_for_flows` cases locked by RPF5 and the selected MPAD record-tail slice locked by PFM2 beyond the scoped unitary bridge, the selected `dont_turn_measurements_into_resets` single-measurement option, selected empty-flow plus Pauli-only, measurement-record, and observable MPAD record-tail reversal, the selected `MZZ` plus plain-qubit unitary suffix packet, and the exact pinned `flow_flip` packet.

Semantic-mining:

- Python transform tests are used only to infer Rust transform semantics and canonical-output expectations.

Deferred or out of scope:

- QASM, Quirk, Crumble, diagrams, Python operator ergonomics, and broader repeat-contained feedback beyond the pinned demolition and interleaved-ordering cases, selected `XCZ`/`YCZ` measurement-record feedback, bounded loop-refolding, and nested bounded-repeat detector-parity cases, which is under-specified until a future exact-subcase plan exists.

### RPF3 Sweep-Conditioned Execution And Gate Semantics

Owned:

- `m2d` sweep records in accepted formats, including `01`, `b8`, `r8`, `hits`, `dets`, and input-only `ptb64` where supported, omitted-sweep default-false behavior, width mismatches, invalid record counts, side outputs, and streaming resource behavior.
- Sweep-conditioned detection sampling if the converter and sampler can prove semantic equivalence to explicit small-circuit expansion.
- Sweep-aware analyzer behavior for public `analyze_errors` or Rust DEM generation cases selected by the analyzer milestone.
- Legal-gate execution support classification across sampler, converter, detection, and analyzer paths, with tests for accepted execution and explicit unsupported-shape errors.

Semantic-mining:

- Frame-simulator and error-analyzer tests are used to mine legal-gate execution behavior, not to claim public simulator API parity.

Deferred or out of scope:

- Exact random-stream parity and public interactive simulator products.

### RPF4 DEM APIs And Folded Traversal

Owned:

- Public materialized DEM `flattened`, `rounded`, and `without_tags` behavior for tags, separators, detector shifts, coordinate shifts, logical observables, and repeats.
- DEM count, coordinate, final-shift, and selected-coordinate queries for large and nested repeats, with folded traversal or documented caps.
- Folded or capped traversal for DEM sampler, graphlike search, hypergraph search, SAT or WCNF encoding, matcher-adjacent operations, and analyzer-adjacent operations, including the implemented graphlike and hypergraph zero-probability repeat skip, selected flat detector-touching and detectorless logical-only zero-shift graphlike and hypergraph repeat folding, selected flat no-target zero-shift graphlike and hypergraph repeat skipping, selected flat zero-detector-shift graphlike and hypergraph repeat folding, selected flat annotation-bearing graphlike and hypergraph repeat folding, selected flat and nested annotation-only zero-shift graphlike, hypergraph, unweighted SAT, and weighted WCNF repeat skipping including high sparse annotation ids, selected nested zero-shift graphlike and hypergraph repeat folding, selected mixed zero-probability plus active zero-shift graphlike and hypergraph repeat folding, selected sparse high-detector direct DEM graphlike and hypergraph search indexing, selected flat and nested detector-touching plus detectorless logical-observable-only and annotation-bearing ErrorMatcher filter DEM repeat folding, weighted SAT/WCNF zero-probability variable elision and repeated-body skip, selected SAT flat and nested zero-shift repeat folding including unweighted zero-probability structural mechanisms, and dense-target caps for high-index structural SAT error-target repeats including dedicated high-observable cap evidence.
- Resource-boundary tests for huge repeats, nested repeats, high detector shifts, high observable counts, malformed DEMs, and unsafe transform expansion.

Semantic-mining:

- Python DEM tests are used to infer Rust API semantics only.

Deferred or out of scope:

- DEM diagrams, Python class operators, and Python API shape.

### RPF5 Detector Utilities And Measurement-Rich Flows

Owned:

- Detecting-region repeat traversal, tick windows, detector filtering, multi-detector regions, ignored anticommutation behavior, selected gauge behavior, and the promoted target-shape set. Broader target-shape, generated-code, and gauge behavior are under-specified in `docs/plans/milestone-spec-gaps.md` until exact subcases are selected.
- Missing-detector generated-code suffix cases for the pinned honeycomb and toric circuits, plus precise errors for any unpromoted family. Gaussian row reduction, repeated MPP and pair-measurement stabilizer products, observable-row interactions, `MPAD` measurement-pad records, tableau-backed Clifford propagation, `SPP` and `SPP_DAG` unitary Pauli-product analysis through decomposition, bounded repeat traversal with explicit caps, selected folded final-repeat traversal for covered deterministic measurement loops with flat or bounded nested local bodies, selected observable-neutral final-repeat traversal where top-level record-only observable rows are redundant under independent detector evidence, and the pinned honeycomb and toric generated-code suffix cases are implemented for the promoted Rust subset; the generated-code suffix boundary is locked in `docs/plans/pfm5-missing-detectors-generated-boundary-scope.md`, and broader `missing_detectors` utility families outside the promoted evidence are logged as under-specified until exact circuits or generated families, comparators, resource behavior, oracle metadata, and benchmark policy are selected.
- Measurement-rich `has_flow`, `has_all_flows`, `flow_generators`, `solve_for_flow_measurements`, signed sampled flow checking, folded traversal, and transform integration for selected exact subcases. The basic `Flow` object surface for parsing, canonicalized measurement indices, included observables, display, ordering, and multiplication is already closed by `coverage-stabilizers-flow` and should not be treated as active PF5 work unless that API changes; the current unsigned checker also has additive diagnostics for mismatches, out-of-range records, and unsupported circuits, and the current sampled signed Rust checker covers selected pinned Stim sign-sensitive unitary, measurement-record, record-backed observable, Pauli-backed observable, inverted Pauli-backed observable, and inverted record-backed observable cases. Broader solver, generator, signed sampled diagnostic, folded traversal, and transform diagnostics are under-specified in `docs/plans/milestone-spec-gaps.md` until exact subcases are selected.

Semantic-mining:

- Python flow tests are used only to infer Rust flow semantics.

Deferred or out of scope:

- Python flow binding ergonomics.

### RPF6 Analyzer Search And Sparse Reverse Tracking

Owned:

- Generated-circuit analyzer behavior, loop folding, gauge detectors, approximate disjoint errors, decomposition options, remnant-edge blocking, ignored decomposition failures, and exact or structural `.dem` comparators.
- Generated-circuit graphlike search, hypergraph search, shortest errors, selected generated-QEC SAT or WCNF encoding, selected generated-QEC ordering-insensitive target-signature invariant checks, and selected sparse high-detector direct DEM search resource behavior. Broader generated search, SAT/WCNF, tie-sensitive comparator, loop-folded search, and resource behavior are under-specified in `docs/plans/milestone-spec-gaps.md` until exact subcases are selected.
- Sparse reverse detector-frame tracker optimized loop folding, generated supported-unitary repeat cases, analyzer and search consumption, and active matched-error value-object hardening needed by analyzer/search outputs. The supported-Clifford unitary-repeat folding subset is promoted for the full single-qubit Clifford gate set and fixed two-qubit tableau-backed Clifford gates with plain qubit-pair targets, with deterministic generated repeat tests for nested and grouped target bodies, and the unsigned tracker path supports `SPP` and `SPP_DAG` product propagation for public unsigned-flow checking. Analyzer/search-specific consumption and broader variable-target unitary semantics outside this unsigned tracker path are under-specified in `docs/plans/milestone-spec-gaps.md` until exact subcases are selected.

Semantic-mining:

- ErrorMatcher and matched-error tests are used only for value-object behavior required by active analyzer and search paths.

Deferred or out of scope:

- Full ErrorMatcher provenance, heralded matching, repeat-contained noise stack frames, and `stim explain_errors` CLI.

### RPF7 Visible CLI Parity

Owned:

- `stab m2d` accepted flags, sweep records, feedback inlining, skip-reference behavior, append observables, side outputs, input formats, output formats, path errors, writer errors, stdout behavior, stderr class, exit status, and resource boundaries.
- `stab analyze_errors` accepted flags, decomposition behavior, gauge behavior, approximate disjoint errors, fold-loop behavior, input and output paths, stdout behavior, stderr class, exit status, and malformed input behavior.
- Accepted legacy aliases `--gen`, `--convert`, `--sample`, `--detect`, `--m2d`, and `--analyze_errors`, including conflicts between multiple modes.
- Explicit rejection or absence of deprecated `--detector_hypergraph`.

Semantic-mining:

- Usage examples are used to infer command shape and error-class expectations.

Deferred or out of scope:

- `stim explain_errors`, diagrams, `stim repl`, and deprecated aliases not selected for Stab.

## Rollup Rows

| Checklist row | Status class | Completion rule |
| --- | --- | --- |
| Rust core library equivalent for core Stim semantics | Rollup | Complete only after RPF1 through RPF6 close the active Rust API, transform, DEM, utility, flow, analyzer, and search rows. |
| CLI binary | Implemented for selected Stab CLI surface | Section 11 checklist command rows and PF7 evidence close the selected `stab` CLI surface, while deferred commands and drop-in `stim` packaging remain excluded unless a later release plan promotes them. |
| `.stim`, `.dem`, and result-format compatibility | Rollup | Complete only after RPF2, RPF3, RPF4, and RPF7 close active transform, sweep, DEM, and command-specific format gaps. |
| Full semantic execution of every legal circuit operation | Rollup | Complete only after RPF3 records which legal gates execute in sampler, converter, detection, and analyzer paths, with explicit rejection tests for unsupported shapes. |
| Highest-priority remaining feature gaps | Rollup | Complete only after the child rows for circuit transforms, DEM API, sweep-conditioned behavior, and feedback-inlining parity are implemented or explicitly deferred. |

## Active Work Items

| Inventory id | Checklist rows | Milestone | Owner crate or surface | Upstream sources | First tests and comparator | Oracle status | Benchmark plan | Exclusions |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PF1-CIRCUIT-RUST-API | Programmatic mutation; Core introspection; Circuit coordinate queries; Reference samples and determined measurements | PF1 | `stab-core` circuit API | `src/stim/circuit/circuit_pybind_test.py`; `src/stim/circuit/circuit.test.cc`; `src/stim/util_top/reference_sample_tree.test.cc`; `src/stim/util_top/count_determined_measurements.test.cc` | Structural Rust tests cover the implemented stats, coordinate, and mutation-helper subset for clear, top-level length, top-level item iteration, validated top-level item ranges, instruction-only ranges with repeat-block rejection, lazy flattened forward and reverse instruction iteration through nested repeat blocks, append-from-Stim-text helpers, atomic parse-failure handling, bounded path-based `.stim` file reads, streaming `.stim` file writes, circuit append and concatenation, repeat special cases, nested repeat-count fusion, repeat overflow rejection, insertion with boundary fusion, repeat-block insertion, pop behavior without neighbor refusion, circuit reference samples, reference-sample trees, deterministic-measurement counts, sweep-controlled reference/count helpers under default-false semantics, measurement counts, detector counts, observable counts, tick counts, sweep-bit counts, folded final coordinate shifts, final qubit coordinates through repeats, folded detector-coordinate lookup through repeats, all-detector coordinate maps including empty-coordinate detectors, missing-detector rejection, and non-finite folded-shift rejection. No active PF1 circuit API test subcase remains for the current Rust scope. | `pf1-circuit-rust-api` is implemented as the selected PF1 circuit Rust API closure, with supporting rows `pf1-circuit-stats-coordinates`, `pf1-circuit-append-text`, `pf1-circuit-file-helpers`, `pf1-circuit-concat`, `pf1-circuit-repeat`, `pf1-circuit-insert-pop`, `pf1-circuit-iterators`, `pf1-circuit-reference-determined`, and `pf1-circuit-detector-coordinates`. | `pf1-circuit-coordinate-query` has a non-primary report-only Rust runner with measurement work and compare notes. Existing reference-sample and count-determined rows remain separate historical coverage evidence for lower-level implementations; iterator/range views are covered structurally and are not separate PF1 benchmark gates. | Python operators, Python bit-packed reference-sample return shapes, file-like objects, binding-specific slicing, and unbounded streaming `.stim` file reads remain deferred; exact C++ infinity behavior for folded coordinate overflow is logged in `docs/plans/milestone-spec-gaps.md`. |
| PF1-GATE-METADATA | Gate validation flags and categories | PF1 | `stab-core` gate registry | `src/stim/gates/gates_test.py`; `src/stim/gates/gates.test.cc`; `src/stim/gates/gates.perf.cc` | Structural Rust tests cover the implemented accessor subset for aliases, argument rules, target rules, target grouping, fusing, noisy/reset/measurement/unitary/single-qubit/two-qubit/target-capability/symmetry flags, unitary inverse, generalized inverse, local Clifford tableau metadata, Stim v1.16.0 `GateData.flows` metadata for tableau-backed unitary gates plus representative measurement-rich and variable-target gates, fixed-shape one- or two-qubit `GateUnitaryMatrix` metadata, H/S/CX/M/R `GateDecomposition` metadata, metadata-column support-contract synchronization against Rust accessors, validation regressions for identity-error probability lists and controlled-bit target capabilities, and parser-versus-execution boundary behavior for the owned `SPP` and `SPP_DAG` cases. No active PF1 Rust metadata accessor subcase remains for the current Rust metadata surface. | `pf1-gate-metadata-api` is implemented as the selected PF1 gate metadata Rust API closure, with supporting rows `pf1-gate-metadata-rust-accessors`, `pf1-gate-tableau-metadata`, `pf1-gate-flow-metadata`, `pf1-gate-unitary-matrix-metadata`, `pf1-gate-decomposition-metadata`, `pf1-gate-metadata-identity-error-probabilities`, and `pf1-gate-metadata-controlled-bit-targets`. | `pf1-gate-metadata-lookup` has a non-primary report-only Rust runner with measurement work and compare notes, including tableau-supported gate, flow, fixed-shape unitary matrix, H/S/CX/M/R decomposition, and alias reads. | Python `GateData` object shape remains deferred; execution behavior continues to be tracked by PF3 gate-semantic evidence instead of metadata alone. |
| PF1-DEM-RUST-API | DEM construction and mutation; DEM detector shifts, observables, coordinates, and counts | PF1 | `stab-core` DEM API | `src/stim/dem/detector_error_model_pybind_test.py`; `src/stim/dem/detector_error_model_repeat_block_pybind_test.py`; `src/stim/dem/dem_instruction_pybind_test.py`; `src/stim/dem/detector_error_model.test.cc` | Structural Rust tests cover the implemented basic and introspection subset for length, emptiness, clear, programmatic instruction and repeat construction, push helpers, derived `Clone`, append-from-text atomic parse failure, recursive tag stripping, folded final coordinate shifts, non-finite folded-shift rejection, error counts through repeats, all-detector coordinate maps, selected-detector coordinate maps, single-detector lookup, top-level item iteration, validated item ranges, instruction-only ranges with repeat-block rejection, typed item downcasts, lazy adjusted flattened instruction iteration through repeats, huge-repeat lazy traversal for yielding bodies, and exact-one-target validation for detector and logical observable declarations. No additional non-Python mutation ergonomic helper is selected beyond the current Rust API surface. | `pf1-dem-rust-api` is implemented as the selected PF1 DEM Rust API closure, with supporting rows `pf1-dem-basic-rust-api`, `pf1-dem-counts-coordinates`, and `pf1-dem-iterators`. PF4 transform and traversal rows remain separate. | `pf1-dem-counts-repeat` and `pf1-dem-without-tags` have non-primary report-only Rust runners with measurement work and compare notes; `pf1-dem-counts-repeat` now includes count, final-coordinate, and selected detector-coordinate submeasurements. | Python list operators, binding-only mutation ergonomics, exact Python API shape, materialized transform parity, and full folded traversal for every large-repeat coordinate-map or transform case remain deferred or owned by PF4. |
| PF2-FLATTEN-WITHOUT-NOISE | Repeat handling; Circuit transforms; Full circuit transform API parity | PF2 | `stab-core` circuit transforms | `src/stim/circuit/circuit_pybind_test.py`; `src/stim/circuit/circuit.test.cc` | `cargo test -p stab-core --test circuit_transforms` covers Rust `Circuit::flattened`, `Circuit::flattened_operations`, and `Circuit::without_noise` for tags, annotations, detectors, observables, measurement references, coordinate shifts, heralded-noise `MPAD` preservation, excessive materialized expansion rejection, and folded shift-only repeats. | `pf2-circuit-flatten-without-noise-rust` is implemented and supplements the broad `pf2-circuit-flatten-without-noise` manifest-only row. | `pf2-circuit-flatten-repeat` and `pf2-circuit-without-noise` have non-primary report-only Rust runners with measurement work and compare notes. | QASM, Quirk, Crumble, diagrams, Python iterator ergonomics, and broader transform parity remain deferred or owned by later PF2 rows. |
| PF2-DECOMPOSED | Circuit transforms; Gate semantic execution | PF2 | `stab-core` decomposition and algebra integration | `src/stim/circuit/gate_decomposition.test.cc`; `src/stim/util_top/simplified_circuit.test.cc`; `src/stim/util_top/circuit_vs_tableau.test.cc` | `cargo test -p stab-core --test circuit_transforms decomposed` covers the Rust `Circuit::decomposed` API for public ISWAP and MPP output, tag preservation across RX, noise, MPP, detector, and SPP, constant-MPP products, anti-Hermitian MPP/SPP rejection, and selected measurement-rich flow-generator preservation for MPP and pair-measurement decomposition. Broader flow-semantic decomposition checks should reopen only when a future exact PFM5 flow family is selected. | `pf2-circuit-decomposed-public-rust` is implemented and supplements the broad `pf2-circuit-decomposed` manifest-only row. | `pf2-circuit-decompose-mpp-spp` has a non-primary report-only Rust runner with measurement work and compare notes. | Any decomposition requiring deferred simulator products must fail closed. |
| PF3-SWEEP-CONVERSION-DETECTION | Target kinds; Measurement-to-detection conversion; Broader sweep-conditioned simulator and analysis parity | PF3 | `stab-core` detection converter and `stab-cli` detect or m2d support | `src/stim/simulators/measurements_to_detection_events.test.cc`; `src/stim/cmd/command_m2d.test.cc`; `src/stim/cmd/command_detect.test.cc` | Exact CLI and structural core tests cover current `m2d --sweep` records for `01`, `b8`, `r8`, `hits`, `dets`, and input-only `ptb64`, `dets` blank-line handling, strict `01` and `hits` text record termination, omitted-sweep defaults, width mismatches, unsupported sweep shapes, side outputs, streaming resource behavior, sampler-backed `CX q sweep[k]` and `CY q sweep[k]` rejection with accepted neighboring sweep-first `CX` or `CY` and both-order `CZ` cases, and non-frame plus selected frame-path `detect` sampling with omitted all-false sweep bits, including qubit/sweep `XCZ` and `YCZ` no-op groups plus measurement-record `XCZ` or `YCZ` feedback. Pinned Stim v1.16.0 has no `stim detect --sweep` flag. See [rpf3-sweep-gate-progress-report.md](rpf3-sweep-gate-progress-report.md). | `pf3-sweep-m2d-detect` manifest-only row plus `pf3-m2d-sweep-format-matrix-cli`, `pf3-m2d-text-format-negative-cli`, `pf3-sampler-sweep-target-order-rust`, existing M9 sweep rows, `pf3-detect-default-false-sweep-core`, `pf3-detect-default-false-sweep-cli`, `pf3-detect-xcz-ycz-feedback-core`, and `pf3-detect-xcz-ycz-feedback-cli`. | `pf3-m2d-sweep-b8`, `pf3-m2d-sweep-ptb64-input`, and `pf3-detect-sweep-sampling` have non-primary report-only runners with measurement work and compare notes. `pf3-sampler-sweep-target-order-rust` has no benchmark because it only hardens validation. | Python converter and detector-sampler sweep APIs remain deferred, unsupported frame-path sweep target placements remain fail-closed, and broader analyzer sweep-shape behavior is under-specified until exact remaining gate-target shapes, comparator, CLI and Rust surfaces, oracle metadata, resource behavior, and benchmark policy are selected. |
| PF3-SWEEP-ANALYZER-GATES | Target kinds; Gate semantic execution; Circuit-to-DEM analysis | PF3 | `stab-core` sampler, converter, detection, and analyzer semantics | `src/stim/simulators/error_analyzer.test.cc`; `src/stim/simulators/frame_simulator.test.cc`; `src/stim/simulators/tableau_simulator.test.cc` | Structural and exact tests cover the selected analyzer sweep-control and `CZ` classical-only no-op matrix, including `CZ` sweep/sweep, record/sweep, sweep/record, and record/record groups, with invalid target-position rejections for neighboring controlled-Pauli shapes, all 46 fixed-tableau gate inverse-canceling sampler/detection/analyzer execution circuits, supported Hermitian `SPP`/`SPP_DAG` sampler and detection-conversion execution through decomposition-equivalence checks, detector-frame supported Hermitian `SPP`/`SPP_DAG` acceptance, analyzer `SPP`/`SPP_DAG` state propagation and gauge tracking against explicit small-circuit expansions including multiple product groups, selected public `analyze_errors` SPP success and nondeterministic failure CLI behavior against pinned Stim, anti-Hermitian `SPP`/`SPP_DAG` rejection for sampler, detection conversion, detector-frame, and analyzer paths, selected deterministic `MPP` Pauli-product measurement execution through sampler, detection-conversion reference behavior, skip-reference conversion behavior, non-frame detection sampling, frame detection sampling, and analyzer propagation, selected deterministic `MPAD` measurement-pad execution through the same promoted surfaces, selected stochastic `MPAD(p)` sampler four-bucket distribution, detection-converter reference mapping, non-frame detection-sampling distribution, and frame-path detection-sampling distribution, and selected noisy `MPAD(p)` analyzer pad-flip effects for detector and observable DEM terms. The selected analyzer sweep boundary is locked in `docs/plans/pfm3-analyzer-sweep-boundary-scope.md`, and the selected fixed-tableau plus supported Hermitian `SPP` or `SPP_DAG` plus selected deterministic `MPP`, selected deterministic `MPAD`, selected stochastic `MPAD(p)` sampler and detection-sampling, and selected noisy `MPAD(p)` analyzer gate semantic boundary is locked in `docs/plans/pfm3-gate-semantic-boundary-scope.md`. Broader sweep-controlled analyzer behavior beyond the selected matrix is under-specified until exact remaining gate-target shapes, comparator, CLI and Rust surfaces, oracle metadata, resource behavior, and benchmark policy are selected; remaining non-tableau legal-operation expansion beyond the selected gate semantic boundary is under-specified until exact gate families, execution surfaces, comparator, resource behavior, oracle metadata, and benchmark policy are selected; unsupported target shapes and future execution surfaces should reopen only after such exact subcases are selected. | `pf3-analyze-errors-sweep-core`, `pf3-analyze-errors-sweep-cli`, `pf3-gate-semantic-wide-rust`, `pf3-gate-mpp-execution-rust`, `pf3-gate-mpad-execution-rust`, `pf3-gate-mpad-stochastic-rust`, `pf3-analyze-errors-mpad-noisy-cli`, `pf3-gate-spp-analyzer-rust`, `pf3-gate-spp-contract-rust`, `pf3-analyze-errors-spp-state-propagation-cli`, and `pf3-analyze-errors-spp-nondeterministic-cli` supplement the broad `pf3-sweep-analyzer` and `pf3-gate-semantic-execution` manifest-only rows. | `pf3-analyze-errors-sweep` and `pf3-gate-semantic-wide` have non-primary report-only Rust runners with measurement work and compare notes. The selected deterministic `MPP`, selected deterministic `MPAD`, selected stochastic `MPAD(p)` sampler or detection-sampling, and selected noisy `MPAD(p)` analyzer slices have no separate benchmark rows because they are semantic bookkeeping evidence and do not change a hot throughput path. | Exact random-stream parity and public interactive simulator APIs remain deferred; remaining legal non-tableau execution and full analyzer sweep-shape parity are under-specified until exact subcase plans select gate or target shapes, comparator, surfaces, resource behavior, oracle metadata, and benchmark policy. |
| PF4-DEM-INTROSPECTION-TRANSFORMS | DEM parser and canonical printer evidence lock; DEM introspection; DEM transforms; Full DEM public API parity | PF4 | `stab-core` DEM API | `src/stim/dem/detector_error_model.test.cc`; `src/stim/dem/detector_error_model_pybind_test.py`; `src/stim/dem/dem_instruction.test.cc` | Exact and structural tests cover the closed parser and canonical-printer surface through `m10-dem-parse-print-exact`, `m10-dem-parse-print-basic`, and `coverage-py-dem`. Exact and structural tests now also cover the materialized `flattened` and `rounded` subset for empty models, detector shifts, coordinate shifts, tags, separators, logical observables, repeats, zero-probability rounding, excessive materialized repeat rejection, recursive `without_tags`, final detector shifts, final coordinate shifts, error counts, detector counts, observable counts, pinned Stim trivial selected-coordinate cases, selected coordinates through shifted repeats, malformed DEM text, invalid probabilities, invalid separators, invalid targets, invalid repeat counts, invalid tags, high detector ids, high observable ids, detector-shift overflow, programmatic non-finite coordinate rejection, repeat-block rejection from instruction-only ranges, the all-detector coordinate-map cap with selected-coordinate lookup fallback for huge-repeat models, folded late selected lookup through non-overlapping repeats, pinned Stim nested-loop coordinates, first-declaration behavior for bounded overlapping repeats, flat sparse overlapping selected-coordinate lookup beyond the previous candidate cap, nested sparse overlapping selected-coordinate lookup through bounded flattened detector-declaration scans beyond the previous fallback cap, empty coordinates for valid flat sparse holes and selected large non-flat sparse holes filtered by actual declared-detector bounds, many-selected flat-overlap coordinate scans, and the pinned Stim generated surface-code coordinate comparison through the bounded PFM6 analyzer fallback. Broader exact and structural tests for nested or non-flat ambiguous overlapping large-repeat coordinate traversal beyond bounded flattened-declaration scans, declared-bound pruning, or documented fallback caps, and true folded generated-loop analyzer output are under-specified in `docs/plans/milestone-spec-gaps.md` until exact subcases are selected. | `pf4-dem-materialized-transforms-rust`, `pf4-dem-introspection-query-rust`, `pf4-dem-validation-negative-rust`, and `pf4-dem-coordinate-resource-rust` supplement the broad `pf4-dem-introspection-transforms` and `pf4-dem-coordinate-api` manifest-only rows; the related generated surface-code coordinate dependency is covered by PF6 row `pf6-analyzer-generated-fold-loop-fallback-rust`. Parser and canonical-printer evidence is already executable under M10 and is now an evidence-lock dependency rather than an active partial row. | `pf4-dem-flatten-repeat`, `pf4-dem-rounded`, `pf4-dem-coordinate-map`, and historical `pf1-dem-without-tags` have non-primary report-only Rust runners with measurement work and compare notes. | DEM diagrams and Python operator ergonomics remain deferred. |
| PF4-DEM-FOLDED-TRAVERSAL | DEM flattening and large repeat traversal; DEM analysis and shortest graphlike error | PF4 | `stab-core` DEM traversal consumers | `src/stim/simulators/dem_sampler.test.cc`; `src/stim/simulators/error_analyzer.test.cc`; `src/stim/simulators/error_matcher.test.cc`; `src/stim/search/graphlike/algo.test.cc`; `src/stim/search/hyper/algo.test.cc`; `src/stim/search/sat/wcnf.test.cc` | Resource-boundary tests now cover current capped-repeat behavior for graphlike search, hypergraph search, SAT problem generation, analyzer traversal, ErrorMatcher traversal and filter DEM traversal. Graphlike and hypergraph search now skip repeated bodies containing only zero-probability errors, fold selected flat detector-touching and detectorless logical-only zero-shift repeated error bodies against compact-model semantic parity, skip selected flat no-target zero-shift repeated error bodies, fold selected flat zero-detector-shift `shift_detectors 0` repeated bodies, fold selected flat annotation-bearing repeated bodies with `detector` or standalone `logical_observable` declarations, skip selected flat and nested annotation-only zero-shift repeated bodies with `detector`, standalone `logical_observable`, and zero-detector-shift `shift_detectors` declarations including high sparse annotation ids that do not appear on error targets, fold selected nested zero-shift repeated bodies, fold selected mixed zero-probability plus active zero-shift repeated error bodies, avoid materializing oversized graph nodes from ignored zero-probability repeat targets, and sparsely index selected high-detector direct DEM search graphs when only a small active target set matters; ErrorMatcher filter DEM traversal folds selected flat and nested detector-touching plus detectorless logical-observable-only and annotation-bearing zero-shift repeated keys while shifted filter DEM repeats, annotation-only bodies, broader mixed-instruction filter bodies, and repeat-contained circuit noise remain capped or rejected; weighted SAT/WCNF omits zero-probability error variables, skips repeated zero-probability and selected annotation-only zero-shift bodies before flattening, folds selected flat and nested zero-shift repeat bodies by concrete MAP parity cost, and rejects shifted zero-probability repeats that would place later active errors beyond the current dense SAT target cap, while unweighted SAT folds selected flat and nested zero-shift repeat bodies structurally including zero-probability mechanisms, skips selected annotation-only zero-shift bodies, and keeps high-index dense-target error mechanisms, shifted, non-flat, or other non-selected structural repeats capped. DEM sampler compilation and direct detection-event sampling now use a folded operation tree, zero-probability repeat bodies are skipped, deterministic zero-shift repeat bodies are folded by parity, selected single-stochastic zero-shift repeat bodies are folded by odd-parity probability, selected flat stochastic zero-shift repeat bodies are folded by per-error odd-parity probability, selected nested zero-shift stochastic repeat bodies are folded by recursive effective-error parity, and sampled-error output and replay preserve flat error-bit order while keeping materialized width caps plus an explicit stochastic sampled-error application work cap for non-selected shapes. True folded traversal for consumers outside the selected repeat families, shifted or otherwise non-selected repeated stochastic nonzero-probability DEM sampler bodies, and broader RPF6 analyzer and search parity are under-specified in `docs/plans/milestone-spec-gaps.md` until exact subcases, resource boundaries, oracle metadata, and benchmark policy are selected. | `pf4-dem-search-sat-repeat-resource-rust`, `pf4-dem-hypergraph-logical-repeat-rust`, `pf4-dem-hypergraph-no-target-repeat-rust`, `pf4-dem-hypergraph-zero-shift-repeat-rust`, `pf4-dem-hypergraph-annotation-repeat-rust`, `pf4-dem-search-mixed-zero-probability-repeat-rust`, `pf4-dem-hypergraph-mixed-zero-probability-repeat-rust`, `pf4-dem-search-nested-repeat-rust`, `pf4-dem-hypergraph-nested-repeat-rust`, `pf4-dem-sat-flat-repeat-fold-rust`, `pf4-dem-sat-nested-repeat-fold-rust`, `pf4-dem-analyzer-repeat-resource-rust`, `pf4-error-matcher-repeat-resource-rust`, `pf4-error-matcher-filter-repeat-rust`, `pf4-dem-sampler-repeat-resource-rust`, `pf6-search-sparse-high-detectors-graphlike-rust`, and `pf6-search-sparse-high-detectors-hypergraph-rust` supplement the broad `pf4-dem-folded-traversal` manifest-only row. | `pf4-dem-folded-traversal`, `pf4-dem-folded-graphlike-traversal`, `pf4-dem-hypergraph-logical-repeat`, `pf4-dem-hypergraph-no-target-repeat`, `pf4-dem-search-zero-shift-repeat`, `pf4-dem-search-annotation-repeat`, `pf4-dem-search-mixed-zero-probability-repeat`, `pf4-dem-search-nested-repeat`, `pf4-dem-sat-flat-repeat-fold`, `pf4-error-matcher-filter-flat-repeat`, `pf4-error-matcher-filter-nested-repeat`, `pf4-error-matcher-filter-logical-repeat`, `pf4-error-matcher-filter-annotation-repeat`, and `pf4-dem-sampler-folded-repeat` have non-primary report-only Rust runner coverage for current search/analyzer caps plus selected graphlike, hypergraph, and ErrorMatcher filter DEM flat and nested-repeat folding, selected detectorless logical-only graphlike and hypergraph flat-repeat folding, selected detectorless logical-observable-only ErrorMatcher filter flat and nested-repeat folding, selected annotation-bearing ErrorMatcher filter flat and nested-repeat folding, selected no-target graphlike and hypergraph flat-repeat skipping, selected zero-detector-shift graphlike and hypergraph flat-repeat folding, selected annotation-bearing graphlike and hypergraph flat-repeat folding, selected mixed zero-probability plus active graphlike and hypergraph repeat folding, selected nested zero-shift graphlike and hypergraph repeat folding, folded sampler compile, stochastic direct-sampling, graphlike, hypergraph, weighted SAT/WCNF zero-probability variable elision and repeated-body skip, selected SAT flat and nested zero-shift repeat folding including unweighted zero-probability structural folding, sampler zero-probability repeat-skip behavior, sampler deterministic zero-shift repeat parity folding, sampler selected direct detection-event single-stochastic zero-shift repeat parity folding, sampler selected direct detection-event flat stochastic zero-shift repeat parity folding, and sampler selected direct detection-event nested zero-shift stochastic repeat parity folding. Sparse high-detector direct DEM search and selected annotation-only search/SAT/WCNF repeat skipping including high sparse annotation ids have no separate benchmark because they are admission/resource evidence, not throughput paths. | Match-graph rendering remains deferred. |
| PF5-DETECTING-REGIONS | Detector-analysis utility APIs | PF5 | `stab-core` detector utility API | `src/stim/util_top/circuit_to_detecting_regions.test.cc`; `src/stim/util_top/circuit_to_detecting_regions_test.py`; `src/stim/circuit/circuit_pybind_test.py`; `src/stim/simulators/sparse_rev_frame_tracker.test.cc`; `src/stim/gen/gen_surface_code.test.cc` | Structural tests now cover the M9 simple H/CX/MXX case, the PF5 bounded repeat-tick traversal subset with excessive repeat-expansion rejection, additive detector and logical-observable `DemTarget` filters, duplicate target deduplication, dense-capped default-like all-target and all-tick helpers, the pinned `MX`, `MZZ`, and tagged ordinary-noise Python examples, generated repetition-code all-target and all-tick selection with selected exact detector and observable regions, selected generated memory-Z and memory-X surface-code all-target and all-tick helper counts plus exact D0, D4, and L0 regions, the full single-qubit Clifford gate set with plain qubit targets, fixed two-qubit tableau-backed Clifford gates with plain qubit-pair targets, selected measurement-record feedback target placements for `CX`, `CY`, `CZ`, `XCZ`, and `YCZ`, selected gate-order-valid sweep-controlled Pauli sign-only no-op groups for `CX`, `CY`, `CZ`, `XCZ`, and `YCZ` plus selected `CZ` classical-only bit-bit groups, ordinary non-record-producing noise no-op traversal, inverted targets for promoted measurement and reset-measurement families, `MPAD` measurement-pad record dropping, heralded record-producing noise record dropping, `MPP` Pauli-product measurement targets, `SPP` and `SPP_DAG` unitary Pauli-product target shapes compared against decomposed propagation, ignored anticommutation mode for in-circuit and implicit-start conflicts, selected measurement-gauge ignored-mode behavior, product-measurement gauge-cancellation behavior, invalid target rejection for out-of-range detector, out-of-range observable, separator, and numeric filters, unsupported feedback placement rejection, non-`CZ` sweep/sweep rejection, non-`CZ` record/sweep rejection, non-`CZ` record/record rejection, anti-Hermitian Pauli-product rejection, and dense helper rejection before large allocation. Broader detecting-region target shapes outside the promoted positive set and source-owned fail-closed set, broader generated-code regions beyond the promoted repetition-code and selected memory-Z and memory-X surface-code cases, and broader gauge behavior are under-specified in `docs/plans/milestone-spec-gaps.md` until exact subcases are selected. | `pf5-detecting-regions-repeat-rust`, `pf5-detecting-regions-targets-rust`, `pf5-detecting-regions-target-shapes-rust`, `pf5-detecting-regions-noise-tags-rust`, `pf5-detecting-regions-clifford-rust`, `pf5-detecting-regions-anticommutation-rust`, `pf5-detecting-regions-gauge-rust`, `pf5-detecting-regions-generated-repetition-rust`, `pf5-detecting-regions-generated-surface-rust`, `pf5-detecting-regions-generated-unrotated-surface-rust`, and `pf5-detecting-regions-generated-surface-memory-x-rust` supplement the broad `pf5-detecting-regions-extended` manifest-only row plus M9 simple implemented row. | `pf5-detecting-regions-repeat`, `pf5-detecting-regions-targets`, `pf5-detecting-regions-clifford`, `pf5-detecting-regions-generated-repetition`, and `pf5-detecting-regions-generated-surface` have non-primary report-only Rust runners with measurement work and compare notes. Selected target-shape, ordinary-noise, tag, gauge, generated unrotated memory-Z, and generated memory-X exact-output behavior is not separately benchmarked because it shares the same sparse-tracker traversal and generated surface-code utility path and changes only validation, no-op traversal, feedback propagation, sweep-control no-op traversal, measurement-pad or heralded-record dropping, product-measurement, unitary-product, error-policy, or exact-output evidence branches. | Unpromoted broader gauge and generated-region behavior must fail closed or stay under-specified until exact subcases are selected. |
| PF5-MISSING-DETECTORS | Detector-analysis utility APIs | PF5 | `stab-core` detector utility API | `src/stim/util_top/missing_detectors.test.cc` and `src/stim/circuit/gate_decomposition.test.cc` | Structural tests now cover Gaussian row reduction for multi-record detector rows, repeated MPP and pair-measurement stabilizer products, record-only observable rows, ignored Pauli observable rows, unknown-input behavior, `MPAD` measurement-pad records, tableau-backed single-qubit and fixed two-qubit Clifford propagation with plain qubit target groups, `SPP` and `SPP_DAG` unitary Pauli-product analysis through decomposition, bounded repeat traversal with explicit expansion caps, selected folded final-repeat traversal for covered deterministic measurement loops with flat or bounded nested local bodies, selected observable-neutral final-repeat traversal where top-level record-only observable rows are redundant under independent detector evidence, fallback rejection for cross-iteration, nested cross-iteration, observable-dependent, Pauli-observable, nested-observable, nested-large-repeat, public-API over-depth nested-repeat, or tracker-changing bodies, and the pinned Stim honeycomb and toric generated-code suffix cases. The generated-code suffix boundary is locked in `docs/plans/pfm5-missing-detectors-generated-boundary-scope.md`, and broader `missing_detectors` utility families outside the promoted evidence, broader folded repeat traversal, and broader measurement-rich public flow semantics are under-specified in `docs/plans/milestone-spec-gaps.md` until exact subcases are selected. | `pf5-missing-detectors-row-reduction-rust`, `pf5-missing-detectors-mpp-observable-rust`, `pf5-missing-detectors-generated-honeycomb-rust`, `pf5-missing-detectors-generated-toric-rust`, `pf5-missing-detectors-clifford-rust`, `pf5-missing-detectors-spp-rust`, `pf5-missing-detectors-mpad-rust`, `pf5-missing-detectors-repeat-rust`, `pf5-missing-detectors-nested-final-repeat-rust`, and `pf5-missing-detectors-observable-neutral-final-repeat-rust` supplement the broad `pf5-missing-detectors-extended` manifest-only row plus historical M9 future rows. | `pf5-missing-detectors-mpp`, `pf5-missing-detectors-mpad`, and `pf5-missing-detectors-generated-code` have non-primary report-only Rust runners with measurement work and compare notes. The `SPP` and `SPP_DAG` slice reuses existing decomposition and is structural parity work, while bounded and selected folded repeat traversal is structural resource-boundary work, so neither is separately benchmarked. | Any `missing_detectors` utility family beyond the promoted evidence needs an exact-subcase plan before implementation; other unimplemented flow-dependent missing-detector families must remain explicitly deferred, fail closed, or stay logged as under-specified. |
| PF5-MEASUREMENT-RICH-FLOWS | Flows; Circuit transforms | PF5 | `stab-core` flow API | `src/stim/stabilizers/flow.test.cc`; `src/stim/stabilizers/flow_pybind_test.py`; `src/stim/util_top/circuit_flow_generators.test.cc`; `src/stim/util_top/circuit_flow_generators_test.py`; `src/stim/util_top/circuit_inverse_qec.test.cc`; `src/stim/util_top/has_flow.test.cc` | Structural tests now cover the basic `Flow` object surface for parsing, canonicalization, measurement-index terms, included observable terms, display, ordering, and multiplication through `coverage-stabilizers-flow`; unsigned `has_flow` measurement-record dependencies, pair-measurement records, observable dependencies, observable Pauli targets, sign-insensitive matching, unsigned `circuit_has_unsigned_stabilizer_flow` and `circuit_has_all_unsigned_stabilizer_flows` helpers for unitary, measurement/observable, false-flow, empty-batch, folded-repeat, and unsigned `SPP`/`SPP_DAG` cases, unsigned diagnostic output-mismatch, input-mismatch, out-of-range record, and unsupported-circuit cases, scoped signed sampled flow checks for sign-sensitive unitary, measurement-record, record-backed observable, Pauli-backed observable, inverted Pauli-backed observable, inverted record-backed observable, and malformed-record cases, exact `circuit_flow_generators` output for `M`/`MX`/`MY`, `R`/`RX`/`RY`, `MR`/`MRX`/`MRY` including inverted result targets, `MXX`/`MYY`/`MZZ`, Python multi-target `M`/`MX`/`MYY`/`MPP`, nonconstant and constant single-instruction `MPP`, pinned variable-target `SPP` and `SPP_DAG` unitary examples, selected unitary-mixed composed measurement-rich instruction sequences, selected all-operation annotation and ordinary-noise no-op traversal, selected composed `SPP` and `SPP_DAG` unitary decomposition, property/checker-satisfaction coverage for the pinned generated all-operations fixture, bounded repeat-contained measurement-rich instruction sequences, `MPAD`, scoped measurement-record feedback examples, selected gate-order-valid sweep-controlled Pauli sign-only no-op groups, selected single- and multi-target heralded-noise MPP generator cases, generated-flow checker satisfaction for the supported checker subset, explicit rejection of unpromoted measurement-rich generator shapes, anti-Hermitian `MPP` and `SPP` products, excessive repeat-contained flow-generator rows or expansion, pinned Stim `solve_for_flow_measurements` empty, `MX`, measured-idle, multi-target measurement and `MPP`, fewer-measurements heuristic, and repetition-code examples with Stab-specific fallback resource hardening, scoped unitary `time_reversed_for_flows` with idle far-qubit and folded large-repeat H, SQRT_X, and CY flows, selected single-instruction measurement-rich `time_reversed_for_flows` for pinned `M` and `MZZ` examples with selected multi-record measurement ordering, selected plain `R`/`RX`/`RY` reset-to-measurement conversion over one or more unique qubit targets, selected single-target `M`/`MX`/`MY` measurement-to-reset conversion including the selected `dont_turn_measurements_into_resets` single-measurement option, selected `MR`/`MRX`/`MRY` measure-reset flow reversal over one or more unique qubit targets including inverted result targets, selected empty-flow plus Pauli-only, measurement-record, and observable MPAD record-tail reversal with selected duplicate MPAD observable-id record parity and unsatisfied observable-flow terms rejected, the selected `MZZ` plus plain-qubit unitary suffix packet matching pinned `flow_through_mzz_h_cx_s`, and the exact pinned `flow_flip` packet. Broader all-operation composed measurement-rich `flow_generators` beyond the promoted pinned generated all-operations fixture, no-op, tableau, measurement-record feedback, selected gate-order-valid sweep-controlled Pauli sign-only no-op groups, heralded-record, and `SPP` or `SPP_DAG` decomposition subcases, broader heralded-noise generator synthesis beyond the selected MPP cases, folded repeat traversal beyond the current flow-row and materialized flattened-operation caps, full generator-table measurement solving, broader solver or generator diagnostics, broader MPAD observable flow terms outside selected record-only tails, broader measurement-rich `time_reversed_for_flows`, and transform integration are under-specified in `docs/plans/milestone-spec-gaps.md` until exact subcases are selected; Python-style signed sampled `has_flow` or `has_all_flows` binding parity remains deferred. | `pf5-has-flow-record-observable-rust`, `pf5-has-all-flows-rust`, `pf5-has-flow-diagnostics-rust`, `pf5-signed-sampled-flows-rust`, `pf5-flow-generators-measurement-rust`, `pf5-flow-generators-measurement-python-rust`, `pf5-flow-solve-measurement-rust`, `pf5-flow-solve-measurement-python-rust`, `pf5-flow-solve-measurement-resource-rust`, `pf2-inverse-qec-mpad-rust`, `pf2-time-reverse-flow-unitary-rust`, `pf2-time-reverse-flow-measurement-rust`, and `pf2-time-reverse-flow-mzz-unitary-suffix-rust` supplement the broad `pf5-measurement-rich-flows` manifest-only row. | `pf5-has-all-flows-batch`, `pf5-flow-generators-measurement-rich`, `pf5-flow-generators-measurement-python`, `pf5-flow-solve-measurement-rich`, `pf5-flow-solve-measurement-python`, `pf2-time-reverse-flow`, and `pf2-time-reverse-flow-measurement` have non-primary report-only Rust runners with measurement work and compare notes; `pf2-time-reverse-flow-measurement` includes the selected `dont_turn_measurements_into_resets` option case, selected `MZZ` unitary-suffix packet, and exact `flow_flip` packet. The selected MPAD record-tail reversal slice has no separate benchmark row because `pf2-inverse-qec-mpad-rust` is a narrow selector over existing QEC inverse and flow-validation dispatch rather than a new high-volume traversal. | Python flow binding ergonomics and Python-style signed sampled `has_flow` or `has_all_flows` binding parity remain deferred. |
| PF6-ANALYZER-GENERATED | Circuit-to-DEM analysis; `analyze_errors --decompose_errors` and related flags; Sparse reverse detector-frame tracking | PF6 | `stab-core` analyzer | `src/stim/simulators/error_analyzer.test.cc`; `src/stim/util_top/circuit_to_dem.test.cc`; `src/stim/cmd/command_analyze_errors.test.cc` | Generated-QEC semantic DEM tests now cover noisy generated repetition-code and rotated-surface-code circuits against pinned Stim v1.16.0 expectations, the selected prefix/repeat/tail detector-chain tests cover true compact folded output, tail-error output, and large repeat-count folding after measurement-record-lookback-sized candidate validation, the selected loop-carried observable tests cover the pinned huge odd-repeat `OBSERVABLE_INCLUDE` tail case after the same validation style, the selected period-8 and period-127 observable tests cover the pinned Stim-style compact detector declaration layouts after non-folded nine-iteration and 338-iteration validation, the promoted loop-folded decomposition tests cover repeated composite-error decomposition plus remnant-edge blocking with `fold_loops` and `decompose_errors`, the selected folded observable-dependency guard rejects incomplete loop-carried dependencies with nondeterministic-observable evidence, and the bounded mixed-top-level fallback covers unsafe, delayed, or still-unsupported prefix-repeat-tail `fold_loops=true` shapes through the capped non-folded analyzer. The selected pinned generated surface-code coordinate comparison now proves detector-coordinate equivalence for that fallback shape. Selected matched-error canonicalization hardening covers DEM-term, circuit-location, flipped-Pauli-product, and flipped-measured-observable ordering. Broader true folded generated looping, gauge detectors, approximate disjoint errors, broader decomposition options, ignored failures, additional loop-folded decomposition families, additional logical-observable period families, and future analyzer value-object hardening are under-specified in `docs/plans/milestone-spec-gaps.md` until exact subcases are selected. | `pf6-analyzer-generated-qec-rust`, `pf6-analyzer-prefix-repeat-tail-folding-rust`, `pf6-analyzer-loop-carried-observable-rust`, `pf6-analyze-errors-loop-carried-observable-cli`, `pf6-analyzer-period8-observable-rust`, `pf6-analyze-errors-period8-observable-cli`, `pf6-analyzer-period127-observable-rust`, `pf6-analyze-errors-period127-observable-cli`, `pf6-error-decomp-loop-folded-rust`, `pf6-analyzer-folded-observable-guard-rust`, `pf6-analyzer-mixed-top-level-fallback-rust`, `pf6-analyzer-generated-fold-loop-fallback-rust`, and `pf6-matched-error-canonicalize-rust` supplement the broad `pf6-analyzer-generated-looping` manifest-only row plus existing M10 analyzer rows. | `pf6-analyze-errors-generated-surface`, `pf6-error-decomp-loop-folded`, `pf6-analyzer-loop-observable-folded`, `pf6-analyzer-period8-observable-folded`, and `pf6-analyzer-period127-observable-folded` have non-primary report-only Rust core runners with measurement work and compare notes. The selected prefix/repeat/tail detector-chain folding slice is a structural compact-output and resource-boundary contract, the folded observable-dependency guard is negative correctness evidence rather than a throughput path, the bounded mixed-top-level fallback reuses the capped non-folded analyzer path, and the matched-error canonicalization slice is a value-object ordering contract, so these are covered structurally rather than by separate benchmark rows. | Full ErrorMatcher provenance remains deferred unless needed for analyzer correctness. |
| PF6-SEARCH | Shortest graphlike and hypergraph logical-error search; DEM analysis and shortest graphlike error | PF6 | `stab-core` search APIs | `src/stim/search/graphlike/algo.test.cc`; `src/stim/search/hyper/algo.test.cc`; `src/stim/search/sat/wcnf.test.cc` | Structural tests now cover selected direct DEM graphlike and hypergraph rejection, distance, canonical-ordering, and bounded hyper-error exact outputs, selected sparse high-detector direct DEM graphlike and hypergraph resource behavior with original detector ids preserved, selected high-observable analyzer-to-search `L1200` parity, generated-QEC rotated-surface-code and repetition-code graphlike and hypergraph search instruction counts, deterministic error rows, canonical target-set uniqueness, zero detector parity, exact `L0` observable parity, ungraphlike generated DEM rejection without the ignore flag, and selected generated-QEC shortest plus weighted SAT/WCNF WDIMACS shape for decomposed and ungraphlike generated DEMs. Broader direct DEM resource and tie-sensitive families, broader generated-circuit search families, broader generated SAT or WCNF families, exact or structural target-set comparators for broader tie-sensitive outputs, loop-folded generated search, and sparse reverse tracker integration are under-specified in `docs/plans/milestone-spec-gaps.md` until exact subcases are selected. | `pf6-search-direct-dem-graphlike-rust`, `pf6-search-direct-dem-hypergraph-rust`, `pf6-search-sparse-high-detectors-graphlike-rust`, `pf6-search-sparse-high-detectors-hypergraph-rust`, `pf6-search-many-observables-graphlike-rust`, `pf6-search-many-observables-hypergraph-rust`, `pf6-search-generated-qec-rust`, and `pf6-search-generated-sat-wcnf-rust` supplement the broad `pf6-search-generated` manifest-only row. | `pf6-graphlike-search-generated`, `pf6-hypergraph-search-generated`, and `pf6-generated-sat-wcnf` have non-primary report-only runner coverage for generated rotated-surface-code DEM search or SAT/WCNF encoding. The direct DEM exact-output, sparse high-detector resource, and high-observable rows are tiny compatibility or admission fixtures and have no separate benchmark. | New public graph or vector simulator APIs remain deferred. |
| PF6-SPARSE-REV-TRACKER | Sparse reverse detector-frame tracking; Error explanation value objects | PF6 | `stab-core` sparse reverse tracker and analyzer support | `src/stim/simulators/sparse_rev_frame_tracker.test.cc`; `src/stim/simulators/error_matcher.test.cc`; `src/stim/simulators/matched_error.test.cc` | Structural tests now cover full single-qubit Clifford reverse propagation, fixed two-qubit tableau-backed reverse propagation, selected valid `CX`/`CY`/`CZ`/`XCZ`/`YCZ` measurement-record feedback placement, supported-Clifford unitary-repeat folding for the full single-qubit Clifford gate set plus fixed two-qubit tableau-backed Clifford gates with plain qubit-pair targets, generated supported-unitary loops, nested supported-unitary repeats, grouped single-qubit and two-qubit targets, mixed-loop equivalence to no-fold traversal, shifted-copy comparison for record and detector offsets, huge shifted measurement/detector repeat-period folding, fallback for unsupported repeat bodies that do not reach a shifted period before the remaining-iteration guard, public unsigned-flow consumption through both a measurement-dependent fixed two-qubit repeat and a shifted measurement repeat, unsigned `SPP`/`SPP_DAG` propagation with anti-Hermitian product rejection when public flow checking reaches the sparse tracker, and selected matched-error canonicalization of DEM terms, circuit locations, flipped Pauli products, and flipped measured observables. Analyzer/search-specific consumption, broader variable-target unitary execution semantics outside this unsigned tracker path, and future value-object hardening are under-specified in `docs/plans/milestone-spec-gaps.md` until exact analyzer or search outputs select them. | `pf6-sparse-rev-unitary-repeat-rust`, `pf6-sparse-rev-shifted-repeat-rust`, `pf6-sparse-rev-spp-rust`, and `pf6-matched-error-canonicalize-rust` supplement the broad `pf6-sparse-rev-tracker` manifest-only row. | `pf6-sparse-rev-frame-loop` has a non-primary report-only Rust runner with unitary-repeat and shifted-measurement submeasurement work plus compare notes. The matched-error canonicalization slice is covered structurally because it is not a throughput path. | Heralded matching, repeat-contained noise stack frames, and `stim explain_errors` CLI remain deferred. |
| PF7-M2D-CLI | `stim m2d`; Measurement-to-detection conversion | PF7 | `stab-cli` m2d | `src/stim/cmd/command_m2d.test.cc`; `src/stim/simulators/measurements_to_detection_events.test.cc`; `doc/usage_command_line.md` | Exact and structural CLI oracle rows for formats, sweep, feedback inlining, skip-reference, append observables, side outputs, invalid paths, invalid formats, writer failure, and resource boundaries. The selected CLI closure is implemented for `--circuit`, `--in`, `--out`, `--sweep`, and `--obs_out` path IO, path-error precedence before converter setup, default detector-only `dets` output, `--append_observables`, `--skip_reference_sample`, observable side-output widths for sparse observable ids, Pauli-target observable annotations, selected `XCZ`/`YCZ` feedback under `--ran_without_feedback`, selected format and width failures, writer failure propagation, stdout behavior, stderr class, exit status, and existing M9 sweep, feedback, format, and resource behavior. | `pf7-m2d-cli-parity` is implemented as the selected `m2d` CLI closure, with supporting rows `pf7-m2d-path-io-rust` and `pf7-m2d-command-contract-rust` plus existing M9 rows. | `pf7-cli-m2d-sweep-b8` and `pf7-cli-m2d-feedback-inline` have non-primary report-only public CLI runners with measurement work and compare notes; see [rpf7-cli-parity-progress-report.md](rpf7-cli-parity-progress-report.md). | `--detector_hypergraph` remains excluded, and broader detector-converter API parity remains outside this CLI closure. |
| PF7-ANALYZE-ERRORS-CLI | `stim analyze_errors`; `analyze_errors --decompose_errors` and related flags; Circuit-to-DEM analysis | PF7 | `stab-cli` analyze_errors | `src/stim/cmd/command_analyze_errors.test.cc`; `src/stim/simulators/error_analyzer.test.cc`; `doc/usage_command_line.md` | Exact and structural CLI oracle rows for flags, decomposition, gauge handling, approximate disjoint errors, fold loops, input and output paths, stdout behavior, stderr class, and exit status. The path-IO subset is implemented for `--in` and `--out` success, missing input, output-open precedence, stdout behavior, stderr class, and exit status. The selected flag and malformed-input subset covers default analysis, fold loops, gauge allowance and rejection, approximate-disjoint options and threshold failures, decomposition failure controls, malformed stdin, stdout behavior, stderr class, and exit status. | `pf7-analyze-errors-cli-parity` is implemented as the selected `analyze_errors` CLI closure, with supporting rows `pf7-analyze-errors-path-io-rust` and `pf7-analyze-errors-flags-rust` plus existing M10 analyzer rows. | `pf7-cli-analyze-errors-decompose` and `pf7-cli-analyze-errors-generated` have non-primary report-only public CLI runners with measurement work and compare notes. | `stim explain_errors`, deprecated `--detector_hypergraph`, and broader generated-loop analyzer semantics remain deferred or active outside this PF7 CLI closure. |
| PF7-LEGACY-DISPATCH | Legacy top-level command flags | PF7 | `stab-cli` dispatch | `src/stim/main_namespaced.test.cc`; `src/stim/main_namespaced.perf.cc`; `doc/usage_command_line.md` | CLI tests cover accepted aliases through the selected `--gen`, `--convert`, `--sample`, `--detect`, `--m2d`, and `--analyze_errors` legacy-dispatch row, selected conflicts between multiple modes, help normalization, explicit rejection or absence of `--detector_hypergraph`, and fail-closed behavior for unselected legacy-style `--diagram`, `--explain_errors`, `--repl`, and `--sample_dem` flags. Broader deprecated spelling coverage should reopen only where the CLI plan selects exact flags. | `pf7-legacy-dispatch-parity` is implemented as the selected legacy-dispatch closure, with supporting rows `pf7-legacy-dispatch-accepted-rust`, `pf7-legacy-dispatch-conflicts-rust`, `pf7-detector-hypergraph-excluded-rust`, and `pf7-legacy-unselected-modes-rust`. | `pf7-cli-legacy-dispatch-startup` has a non-primary report-only public CLI runner with measurement work and compare notes. | Deprecated aliases not selected for Stab remain excluded, including `--detector_hypergraph`. |

## Deferred-Only Or Excluded Partial Rows

| Checklist row | Reason this is not active PF work | Evidence to keep current |
| --- | --- | --- |
| Single-shot interactive tableau simulator | The remaining public `TableauSimulator` product is a Python-style simulator API, while current internals only support evidence and implemented workflows. | Keep checklist row deferred until a public Rust simulator API plan exists. |
| Batched flip-frame simulator | The remaining public `FlipSimulator` product is binding-style API parity, while current internals support sampling and detection. | Keep checklist row deferred until a public simulator API plan exists. |
| Error explanation value objects | Active RPF6 has implemented the selected matched-error canonicalization slice, and additional value-object hardening belongs to future analyzer or search slices only when those outputs need it; full ErrorMatcher provenance and `stim explain_errors` CLI are explicitly deferred. | Keep full provenance and CLI rows deferred. |
| Public borrowed Pauli string refs | M6 intentionally chose owned Rust APIs instead of binding-oriented borrowed refs. | Keep deferred. |
| Numpy, state-vector, arbitrary-unitary, and tableau-unitary conversion parity | The M6 `unitary_to_tableau` subset and M12 graph/vector simulator cross-checks are scoped Rust semantic evidence, while Python/numpy conversion APIs, state-vector APIs, arbitrary unitary conversion APIs, `tableau_to_unitary`, random tableau/unitary round trips, and public graph/vector simulator products are outside active PF work. | Keep the checklist row deferred with scoped Rust evidence through `docs/plans/pfm0-matrix-simulator-deferral-evidence-lock.md`. |
| Graph simulator and vector simulator public APIs | M12 uses scoped cross-checks only. New public APIs are out of scope. | Keep deferred. |
| Circuit exports | QASM, Quirk, Crumble, and diagrams are future surfaces. | Keep deferred. |
| DEM diagrams | Rendering surfaces are future work. | Keep deferred. |
| Sampler class-shaped API parity | Current Rust and CLI streaming DEM sampling plus detector sampling surfaces are closed by `docs/plans/pfm0-sampling-streaming-evidence-lock.md`, while Python `CompiledDetectorSampler`, Python `CompiledDemSampler`, Python `sample_write` ergonomics, and any future explicit Rust compiled detector-sampler product remain outside active PF work. | Keep `Streaming DEM sampling` and `Detector sampling` tied to current Rust and CLI evidence, and keep class-shaped API parity deferred through the checklist's sampler and converter classes row. |
| Generated feature/status matrix tooling | The Rust API documentation workflow is implemented by `just docs::api` and `just docs::api-check`; generated Python or JS/WASM docs, Python stubs, and generated feature/status matrix tooling remain future documentation surfaces. | Keep the Rust docs workflow synchronized with `docs/plans/generated-api-docs-progress-report.md`; plan generated feature/status matrix tooling separately if it becomes a product requirement. |

## RPF0 Manifest Rows

The earlier PF0 pass added manifest-only oracle rows with ids beginning `pf1-` through `pf7-` and non-primary benchmark placeholders with ids beginning `pf1-` through `pf7-`.
RPF0 keeps those historical ids as source-owned extraction contracts, and PFM0 keeps any missing placeholders promised by `docs/plans/non-deferred-partial-feature-milestones.md` synchronized.
These rows are intentionally not implementation evidence.
They are extraction contracts: before a matching RPF milestone claims implementation, the matching manifest-only row must be replaced or supplemented by executable exact, structural, property, statistical, or benchmark evidence.

Benchmark PF rows use `non-primary-report-only` and `contract-only`.
They must not enter the primary 1.25x gate until a later implementation milestone adds a real runner, classifies the benchmark, collects stable comparable evidence, and updates profiler notes.

## RPF0 Done Criteria

RPF0 is complete when:

- This inventory covers every active partial row and every deferred-only partial row in `docs/stab-feature-checklist.md`.
- `oracle/fixtures/manifest.csv` has manifest-only PF rows for each active implementation work item.
- `benchmarks/manifest.csv` has non-primary PF benchmark placeholders for each milestone that will need performance evidence.
- Oracle and benchmark tooling can parse PF milestones without adding them to the current M12 primary gate.
- `just oracle::list`, `just oracle::matrix --check`, `cargo test -p stab-oracle fixtures --quiet`, and `just bench::list` pass.

Current evidence-lock report:

- `docs/plans/pfm0-current-evidence-lock-report.md` records the latest PFM0 pass over the active partial rows, manifest-only extraction contracts, deferred-only exclusions, and metadata health checks.
- `docs/plans/pfm8-rollup-evidence-report.md` records the current PFM8 rollup evidence snapshot and confirms that rollup rows should stay conservative until broad PFM2 through PFM6 under-specification entries are selected as exact subcases, implemented, or explicitly deferred.
