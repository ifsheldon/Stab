# RPF3 Sweep And Gate Progress Report

## Implemented Slice: `detect` Default-False Sweep Sampling And Selected Feedback

Stab now permits `detect` sampling to execute selected sweep-conditioned circuits by using omitted all-false sweep bits for the current non-frame sampler, detector-conversion subset, and frame-path Pauli-observable subset.
The same frame-path subset now also accepts measurement-record `XCZ` or `YCZ` target feedback, treating it as X or Y feedback equivalent to `CX` or `CY` feedback.

The implemented boundary is deliberately narrow:

- `sample_detection_events` and `try_for_each_sampled_detection_event` compile non-frame circuits with sweep support and feed the detection converter an all-false sweep record.
- The frame-path detector sampler treats sweep controls on `CX` and `CY` qubit targets as false no-ops when `detect` has no sweep input, follows Stim's `CZ` bit-target semantics by treating sweep/qubit pairs as false no-ops and bit/bit pairs as no-ops, and treats qubit/sweep `XCZ` or `YCZ` groups as false no-ops.
- The frame-path detector sampler accepts `XCZ q rec[-k]` and `YCZ q rec[-k]` as measurement-record-controlled X and Y feedback, matching the equivalent `CX rec[-k] q` and `CY rec[-k] q` forms for the selected Pauli-observable detection surface.
- `stab detect` accepts the same non-frame sweep-conditioned circuit shape through the public CLI.
- `stab detect` also accepts selected frame-path Pauli-observable circuits with sweep-controlled `CX`, `CY`, `CZ`, qubit/sweep `XCZ`, and qubit/sweep `YCZ` groups plus `CZ` bit/bit no-op groups under the same omitted all-false semantics, and accepts measurement-record `XCZ` or `YCZ` feedback in the selected frame path.
- Sampler-backed execution now rejects the Stim-parsed but execution-invalid `CX q sweep[k]` and `CY q sweep[k]` target order for reference sampling, sweep-aware detection conversion, non-frame detection sampling validation, and public `stab m2d --sweep`; sweep-first `CX sweep[k] q`, sweep-first `CY sweep[k] q`, and both-order `CZ` sweep/qubit groups remain accepted.
- This slice does not add a Stab-specific sweep-input extension to `detect`, claim full sweep target-shape parity, or close analyzer sweep behavior beyond the selected no-op matrix and invalid target-position rejections without an exact-subcase plan. Pinned Stim v1.16.0 has no `stim detect --sweep` flag; typed detector-sampler sweep APIs are deferred to future Python or explicit Rust API work.

## Evidence

Oracle rows:

- `pf3-detect-default-false-sweep-core` runs `cargo test -p stab-core detection_sampling_uses_all_false_default_sweep_bits`.
- `pf3-detect-default-false-sweep-cli` runs `cargo test -p stab-cli detect_accepts_default_false`, covering both non-frame and selected frame-path CLI cases.
- `pf3-detect-xcz-ycz-feedback-core` runs `cargo test -p stab-core detection_sampling_supports_xcz_ycz_measurement_feedback_frame_path`, covering selected frame-path measurement-record `XCZ` or `YCZ` feedback against equivalent `CX` or `CY` feedback.
- `pf3-detect-xcz-ycz-feedback-cli` runs `cargo test -p stab-cli detect_accepts_frame_path_xcz_ycz_measurement_feedback`, covering public `stab detect` stdout, stderr, exit status, and seeded output equivalence for selected frame-path measurement-record `XCZ` or `YCZ` feedback.
- `pf3-m2d-sweep-format-matrix-cli` runs `cargo test -p stab-cli m2d_accepts_sweep_records`, covering public `stab m2d --sweep_format` input records for `01`, `b8`, `r8`, `hits`, `dets`, and input-only `ptb64`.
- `pf3-m2d-text-format-negative-cli` runs `cargo test -p stab-cli m2d_rejects_unterminated_text_records`, covering public `stab m2d` rejection of unterminated `01` measurement records and unterminated `01` or `hits` sweep records.
- `pf3-analyze-errors-sweep-core` runs `cargo test -p stab-core --test dem_analyzer_classical sweep`, covering selected analyzer sweep no-op and invalid target-position cases.
- `pf3-analyze-errors-sweep-cli` runs `cargo test -p stab-cli analyze_errors_sweep_controls`, covering public `stab analyze_errors` stdout, stderr, and exit-status behavior for the selected analyzer sweep matrix.
- `pf3-gate-semantic-wide-rust` runs `cargo test -p stab-core --test gate_semantic_execution`, covering the fixed-tableau gate contract plus supported Hermitian `SPP` or `SPP_DAG` sampler, detection-conversion, and detector-frame execution plus selected deterministic `MPP` and `MPAD` execution.
- `pf3-gate-mpp-execution-rust` runs `cargo test -p stab-core --test gate_semantic_execution mpp`, covering selected deterministic `MPP` sampler, detection-conversion, non-frame detection-sampling, frame detection-sampling, and analyzer behavior.
- `pf3-gate-mpp-stochastic-rust` runs `cargo test -p stab-core --test gate_semantic_execution stochastic_mpp`, covering selected stochastic `MPP(p)` sampler four-bucket distribution, detection-converter reference mapping, non-frame detection-sampling distribution, and frame-path detection-sampling distribution.
- `pf3-gate-mpad-execution-rust` runs `cargo test -p stab-core --test gate_semantic_execution mpad`, covering selected deterministic `MPAD` sampler, detection-conversion, non-frame detection-sampling, frame detection-sampling, and analyzer behavior.
- `pf3-gate-mpad-stochastic-rust` runs `cargo test -p stab-core --test gate_semantic_execution stochastic_mpad`, covering selected stochastic `MPAD(p)` sampler four-bucket distribution, detection-converter reference mapping, non-frame detection-sampling distribution, and frame-path detection-sampling distribution.
- `pf3-analyze-errors-mpad-noisy-cli` compares selected public `stab analyze_errors` noisy `MPAD(p)` detector and observable effects against pinned Stim v1.16.0.
- `pf3-gate-spp-analyzer-rust` runs `cargo test -p stab-core --test dem_analyzer_mpp spp`, covering analyzer SPP and SPP_DAG state and gauge behavior against explicit expansions.
- `pf3-gate-spp-contract-rust` runs `cargo test -p stab-core --test gate_metadata gate_execution_contract`, covering the synchronized support-contract table for SPP and SPP_DAG execution surfaces.
- `pf3-analyze-errors-spp-state-propagation-cli` compares the selected public `stab analyze_errors` SPP state-propagation output against pinned Stim v1.16.0.
- `pf3-analyze-errors-spp-nondeterministic-cli` compares the selected public `stab analyze_errors` nondeterministic SPP failure status and stderr class against pinned Stim v1.16.0.
- `pf3-sampler-sweep-target-order-rust` runs `cargo test -p stab-core --test sampler_sweep_target_order pf3_sampler_sweep_target_order`, covering the sampler-backed `CX q sweep[k]` and `CY q sweep[k]` rejection while preserving accepted sweep-first `CX` or `CY` and both-order `CZ` sweep/qubit behavior.

Direct tests:

- `detection_sampling_uses_all_false_default_sweep_bits` compares materialized and streaming non-frame detection sampling against an equivalent explicit all-false circuit.
- `detection_sampling_uses_all_false_default_sweep_bits_frame_path` compares materialized and streaming frame-path Pauli-observable detection sampling against an equivalent explicit all-false circuit, including repeated sweep controls, `CZ` bit/bit no-op groups, and qubit/sweep `XCZ` or `YCZ` no-op groups.
- `detection_sampling_supports_xcz_ycz_measurement_feedback_frame_path` compares materialized and streaming frame-path `XCZ` or `YCZ` measurement-record feedback against the equivalent `CX` or `CY` feedback circuit for two seeded measurement-producing setups.
- `detection_conversion_rejects_bad_sweep_records_and_unsupported_sampling_surfaces` continues to validate unsupported sweep target shapes in converter and frame contexts, including preflight validation of frame-path controlled-Pauli target shapes.
- `detect_accepts_default_false_sweep_conditioned_sampling` proves the public CLI accepts omitted all-false sweep sampling for a non-frame circuit.
- `detect_accepts_default_false_frame_path_sweep_conditioned_sampling` proves the public CLI accepts omitted all-false sweep sampling for a frame-path Pauli-observable circuit.
- `detect_accepts_frame_path_xcz_ycz_measurement_feedback` proves the public CLI accepts selected frame-path `XCZ` or `YCZ` measurement-record feedback and emits the same seeded output as the equivalent `CX` or `CY` feedback circuit.
- `detect_rejects_invalid_frame_path_sweep_targets_before_opening_output` proves invalid frame-path sweep targets fail before `stab detect --out` opens or truncates the output path.
- `m2d_accepts_sweep_records_in_all_text_and_byte_formats` proves public `stab m2d --sweep_format` accepts `01`, `b8`, `r8`, `hits`, and `dets` sweep records, skips Stim-valid blank `dets` lines, and preserves stdout, empty stderr, and successful exit behavior.
- `m2d_accepts_sweep_records_in_ptb64_format` proves public `stab m2d --sweep_format=ptb64` accepts a complete 64-shot sweep group while `ptb64` remains an input-only format for `m2d`.
- `m2d_rejects_unterminated_text_records` proves the streaming public CLI reader rejects unterminated `01` measurement records and unterminated `01` or `hits` sweep records like pinned Stim's strict text readers.
- `dem_analyzer_ignores_sweep_controls_like_upstream` ports the pinned Stim v1.16.0 `ErrorAnalyzer, ignores_sweep_controls` case and extends it to selected `CY`, `CZ`, `XCZ`, and `YCZ` sweep-control no-op cases, including `CZ` sweep/sweep, record/sweep, sweep/record, and record/record classical-only no-op groups.
- `dem_analyzer_rejects_invalid_sweep_target_positions` proves invalid controlled-Pauli sweep target positions fail with explicit analyzer errors instead of being silently ignored.
- `analyze_errors_sweep_controls_match_pf3_oracle` and `analyze_errors_sweep_controls_reject_invalid_target_positions` prove the public CLI returns the selected no-op DEM for the same matrix, including `CZ rec[-a] rec[-b]`, and preserves empty stderr on success, empty stdout on failure, nonzero failure status, and actionable stderr.
- `fixed_tableau_gates_execute_across_current_public_surfaces` proves all 46 fixed-tableau gates execute through inverse-canceling sampler, detection-conversion, and analyzer circuits.
- `mpp_executes_across_current_public_surfaces` proves selected deterministic `MPP` product records execute through sampler output, detection-conversion reference behavior, skip-reference conversion behavior, non-frame detection sampling, frame detection sampling, and analyzer propagation.
- `stochastic_mpp_executes_across_sampler_and_detection_surfaces` proves selected stochastic `MPP(p)` product records execute as independently sampled probabilistic Pauli-product measurement flips in sampler output, detection-converter reference mapping, non-frame detection sampling, and frame-path detection sampling.
- `mpad_executes_across_current_public_surfaces` proves selected deterministic `MPAD` records execute through sampler output, detection-conversion reference behavior, skip-reference conversion behavior, non-frame detection sampling, frame detection sampling, and analyzer propagation.
- `stochastic_mpad_executes_across_sampler_and_detection_surfaces` proves selected stochastic `MPAD(p)` records execute as independently sampled probabilistic pad flips in sampler output, detection-converter reference mapping, non-frame detection sampling, and frame-path detection sampling.
- `dem_analyzer_noisy_mpad_matches_upstream_subset` proves selected noisy `MPAD(p)` analyzer pad-flip effects reach detector-only, observable-only, and combined detector-plus-observable DEM targets like pinned Stim v1.16.0 while `MPAD(0)` remains deterministic.
- `variable_target_spp_execution_matches_decomposed_circuit` proves supported Hermitian `SPP` and `SPP_DAG` products execute in the sampler and detection-conversion path with seeded output matching the public decomposed circuit.
- `variable_target_spp_executes_in_frame_detection_path` proves the detector-frame sampling path accepts supported Hermitian `SPP` and `SPP_DAG` circuits that require frame sampling because they contain Pauli observable targets.
- `anti_hermitian_spp_execution_is_rejected_by_sampler_and_detection_conversion` proves anti-Hermitian `SPP` and `SPP_DAG` products still fail closed in sampler and detection-conversion compilation.
- `dem_analyzer_spp_matches_explicit_phase_product_expansions` proves analyzer `SPP` and `SPP_DAG` state propagation matches explicit H/S/CX expansions for deterministic single-product, signed-product, and multi-product cases.
- `dem_analyzer_spp_nondeterministic_detector_matches_explicit_expansion` and `dem_analyzer_spp_nondeterministic_observable_matches_explicit_expansion` prove the analyzer gauge tracker reports the selected nondeterministic detector and observable cases like explicit expansions; the public CLI nondeterministic status is additionally covered by `pf3-analyze-errors-spp-nondeterministic-cli`.
- `dem_analyzer_rejects_anti_hermitian_spp_products` proves anti-Hermitian `SPP` and `SPP_DAG` products fail closed in analyzer execution.
- `gate_execution_contract_accepts_supported_spp_execution_paths` and `gate_metadata_api_contract_table_matches_rust_accessors` prove the support-contract table now tracks sampler, detection-conversion, and analyzer SPP execution support.
- `pf3_sampler_sweep_target_order_rejects_cx_cy_second_sweep` and `pf3_sampler_sweep_target_order_keeps_accepted_orders` prove the sampler-backed target-order boundary across `Circuit::reference_sample`, sweep-aware detection conversion, detection sampling validation, and direct detection sampling.
- `m2d_rejects_invalid_sampler_sweep_target_order` proves the public `stab m2d --sweep` CLI rejects the same Stim-parsed but execution-invalid `CX q sweep[k]` and `CY q sweep[k]` circuits with empty stdout, nonzero status, and diagnostic stderr.

Benchmark row:

- `pf3-m2d-sweep-b8` now has a non-primary report-only public CLI runner named `stab_pf3_m2d_sweep_b8`, normalized as shots per second, using the source-owned M9 packed sweep fixture.
- `pf3-m2d-sweep-ptb64-input` now has a non-primary report-only public CLI runner named `stab_pf3_m2d_sweep_ptb64`, normalized as shots per second, using deterministic source-owned `ptb64` measurement and sweep records generated by the benchmark harness.
- `pf3-detect-sweep-sampling` now has non-primary report-only Rust runners named `stab_detect_sweep_default_false` and `stab_detect_frame_sweep_default_false`, both normalized as shots per second.
- `pf3-analyze-errors-sweep` now has a non-primary report-only Rust runner named `stab_analyze_errors_sweep_control`, normalized as circuits per second over the selected analyzer sweep-control and `CZ` classical-only no-op matrix.
- `pf3-gate-semantic-wide` now has a non-primary report-only Rust runner named `stab_pf3_gate_semantic_contract`, normalized as surface checks per second across fixed-tableau sampler compilation, detection-conversion compilation, analyzer propagation, promoted supported Hermitian `SPP`/`SPP_DAG` sampler plus detection-conversion checks, and deterministic analyzer `SPP`/`SPP_DAG` checks.
- `pf3-gate-mpp-execution-rust` and `pf3-gate-mpp-stochastic-rust` have no benchmark rows because they are semantic bookkeeping contracts and do not change a hot throughput path.
- `pf3-gate-mpad-execution-rust`, `pf3-gate-mpad-stochastic-rust`, and `pf3-analyze-errors-mpad-noisy-cli` have no benchmark rows because they are semantic bookkeeping contracts and do not change a hot throughput path.
- `pf3-sampler-sweep-target-order-rust` has no benchmark row because it is a validation and fail-closed compatibility slice rather than a throughput path.
- Earlier local probe command `just bench::compare --only pf3-m2d-sweep-b8 --only pf3-detect-sweep-sampling --only pf3-analyze-errors-sweep --baseline target/benchmarks/pf3-sweep-analyzer-probe-baseline/baseline.json --report target/benchmarks/pf3-sweep-analyzer-probe-compare` measured `stab_pf3_m2d_sweep_b8=0.000062208s`, or approximately `8.038e4 shots/s`, `stab_detect_sweep_default_false=0.000095184s`, or approximately `1.076e7 shots/s`, and `stab_analyze_errors_sweep_control=0.000001184s`, or approximately `8.446e5 circuits/s`, as report-only evidence on the local machine. The `ptb64` row has runner coverage but no fresh timing probe recorded in this report yet.
- Fresh local probe command `just bench::compare --only pf3-detect-sweep-sampling --baseline target/benchmarks/rpf3-detect-sweep-frame-probe/baseline.json --report target/benchmarks/rpf3-detect-sweep-frame-compare` measured `stab_detect_sweep_default_false=0.000093504s`, or approximately `1.095e7 shots/s`, and `stab_detect_frame_sweep_default_false=0.000263568s`, or approximately `3.885e6 shots/s`, as report-only evidence on the local machine.
- Historical local probe command `just bench::compare --only pf3-analyze-errors-sweep --baseline target/benchmarks/rpf3-analyze-sweep-matrix-probe/baseline.json --report target/benchmarks/rpf3-analyze-sweep-matrix-compare` measured `stab_analyze_errors_sweep_control=0.000001872s`, or approximately `5.342e5 circuits/s`, as report-only evidence for the selected analyzer sweep-control matrix before the explicit `CZ rec[-a] rec[-b]` fixture was added.
- Fresh local probe command `just bench::compare --only pf3-analyze-errors-sweep --baseline target/benchmarks/pfm3-analyzer-cz-rec-rec-probe-baseline/baseline.json --report target/benchmarks/pfm3-analyzer-cz-rec-rec-probe-compare` measured `stab_analyze_errors_sweep_control=0.000002096s`, or approximately `4.771e5 circuits/s`, with Stab commit `b7ed8ef1264af2b0c13fa124a3613967510c9b61`, `local_modifications=true`, one measurement run, no warmup, and report-only status for the selected analyzer sweep-control plus `CZ` classical-only matrix.

## Still Open In RPF3

- Broader `pf3-analyze-errors-sweep` coverage beyond the selected sweep-control no-op matrix and invalid target-position rejections is locked by [pfm3-analyzer-sweep-boundary-scope.md](pfm3-analyzer-sweep-boundary-scope.md) and remains under-specified until exact remaining gate-target shapes, comparator, CLI and Rust surfaces, oracle metadata, resource behavior, and benchmark policy are selected. The public `m2d --sweep_format` input matrix for `01`, `b8`, `r8`, `hits`, `dets`, and input-only `ptb64` is now covered by `pf3-m2d-sweep-format-matrix-cli`.
- Broader legal-gate execution beyond the fixed-tableau contract, supported Hermitian `SPP` or `SPP_DAG` sampler, detection-conversion, detector-frame, and analyzer subset, selected deterministic `MPP` execution slice, selected stochastic `MPP(p)` sampler and detection-sampling slice, selected deterministic `MPAD` execution slice, selected stochastic `MPAD(p)` sampler and detection-sampling slice, and selected noisy `MPAD(p)` analyzer pad-flip slice is locked by [pfm3-gate-semantic-boundary-scope.md](pfm3-gate-semantic-boundary-scope.md) and remains under-specified until exact gate families, execution surfaces, comparator, resource behavior, oracle metadata, and benchmark policy are selected.
- Broader frame-path sweep behavior remains open for unsupported sweep target placements, but sampler-backed `CX q sweep[k]` and `CY q sweep[k]` target-order rejection is now source-owned by `pf3-sampler-sweep-target-order-rust`. Any `detect --sweep` surface would be a Stab extension or future API decision, not a pinned Stim v1.16.0 CLI parity gap.

## Audit And Review Notes

Milestone audit found the slice complete against the selected PFM3 frame-path `detect` contract after logging broader analyzer sweep behavior as under-specified and keeping full sweep target-shape parity explicitly open; later scope review confirmed `detect --sweep` is not a pinned Stim v1.16.0 CLI parity target.
The GPT-5.5/xhigh docs and metadata sidecar found stale roadmap wording that still described frame-path sweep sampling as open; this report and `rust-stim-drop-in-rewrite.md` now identify the selected frame-path subset as implemented.
The GPT-5.5/xhigh core sidecar found that unsupported frame-path sweep target shapes passed preflight validation and that `CZ` bit/bit groups were incorrectly treated as rejections instead of Stim-compatible no-ops; validation now rejects unsupported `CX` and `CY` bit targets before output files are opened, and `CZ` bit/bit groups are accepted as no-ops.
The follow-up analyzer audit found the selected sweep-control matrix complete after adding public `stab analyze_errors` CLI evidence. A later PF3 evidence-lock added explicit `CZ rec[-a] rec[-b]` analyzer coverage with selected `CX`, `CY`, `XCZ`, and `YCZ` record-record rejection evidence, without promoting broader analyzer target-shape parity beyond the selected matrix. The analyzer sweep boundary is now summarized in [pfm3-analyzer-sweep-boundary-scope.md](pfm3-analyzer-sweep-boundary-scope.md). The GPT-5.5/xhigh analyzer sidecar found no correctness issues and suggested extra classical-bit edge cases, which are now covered. The GPT-5.5/xhigh docs and metadata sidecar found stale report boundary text and missing public CLI evidence; the report boundary is reworded and `pf3-analyze-errors-sweep-cli` now proves stdout, stderr, and exit-status behavior.
The PF3 `CZ rec[-a] rec[-b]` evidence-lock review found two documentation and evidence issues: the report-only analyzer benchmark text still cited the older pre-record-record fixture probe, and the negative-scope prose was broader than the source-owned rejection tests. The report now cites a fresh report-only probe for the changed fixture with `local_modifications=true`, and the tests plus scope note source-own the selected `CX`, `CY`, `XCZ`, and `YCZ` record-record rejection set while leaving broader analyzer target-shape parity under-specified until exact subcases are selected.
The follow-up frame-path feedback review found the initial oracle metadata overstated `XCZ` or `YCZ` feedback evidence under the existing default-false rows; dedicated `pf3-detect-xcz-ycz-feedback-core` and `pf3-detect-xcz-ycz-feedback-cli` rows now run the new feedback tests directly.
The sampler target-order review locked down a pinned Stim v1.16.0 execution distinction that parsing alone hides: flow generators accept `CX q sweep[k]` and `CY q sweep[k]` as no-ops, while sampler-backed execution rejects them. The scope note [pfm3-sampler-sweep-target-order-scope.md](pfm3-sampler-sweep-target-order-scope.md) keeps that boundary explicit so flow-generator parity is not accidentally narrowed while sampler, conversion, detection sampling, and `m2d --sweep` fail closed.
The selected noisy `MPAD(p)` analyzer follow-up fixed a compatibility gap where pad flip probabilities were counted as deterministic pad records instead of completed measurement errors; [pfm3-mpad-noisy-analyzer-scope.md](pfm3-mpad-noisy-analyzer-scope.md) records the detector and observable exact-output boundary.
The selected stochastic `MPAD(p)` sampler and detection-sampling follow-up records the already implemented probabilistic pad-record execution contract in [pfm3-stochastic-mpad-sampler-detection-scope.md](pfm3-stochastic-mpad-sampler-detection-scope.md), with no new benchmark row because the evidence is semantic distribution and reference-mapping coverage rather than a distinct throughput path.
The selected stochastic `MPP(p)` sampler and detection-sampling follow-up records the already implemented probabilistic Pauli-product measurement-record execution contract in [pfm3-stochastic-mpp-sampler-detection-scope.md](pfm3-stochastic-mpp-sampler-detection-scope.md), with no new benchmark row because the evidence is semantic distribution and reference-mapping coverage rather than a distinct throughput path. The GPT-5.5/xhigh full-code-review sidecar found that the first stochastic `MPP(p)` test only compared aggregate detector and observable flip counts and did not prove the frame-path second observable existed; the test now asserts per-record detector or observable shapes, same-record equality, and the Pauli-backed observable's always-false value directly.
