# M10 Progress Report

## Milestone

M10: Detector Error Model Core

## Status

Complete against the current scoped M10 contract.
This milestone implements the `.dem` format core, staged `analyze_errors` paths, direct graphlike and hypergraph search subsets, SAT problem generation, matched-error values, the staged ErrorMatcher subset, the staged sparse reverse tracker subset, generated-QEC structural DEM equivalence, semantic-mining traceability rows, M10 benchmark rows, and resource rejection checks required by the M10 done criteria.
Full ErrorMatcher provenance parity for repeat-contained noise stack frames, generated surface-code repeat matching, heralded matching, and full sparse reverse tracker consumption is intentionally deferred and logged in `docs/plans/milestone-spec-gaps.md` plus the Future Plan.

## Tests Ported Or Created

- `cargo test -p stab-core dem` covers DEM target limits, DEM instruction separated target groups, repeat blocks, detector shifts, coordinates, observables, probabilities, separators, invalid input, analytical detector counting through large repeats, bounded public DEM flattening for graphlike and hypergraph APIs, deterministic detector declarations, measurement-flip analyzer output, identical-symptom merging and cancellation, unconditional and conditional correlated-error output, graphlike decomposition fallback, known-component decomposition, remnant-edge blocking, ignored decomposition failures, and undecomposable triple rejection under `--decompose_errors`, identity-noise no-ops, reset cutoff of pending single-qubit errors and channels, simple Pauli-error analyzer output, propagation of pending Pauli errors through single-qubit Clifford gates, `SWAP`, non-controlled two-qubit Clifford gates, and controlled-Pauli gates, propagation of approximate `PAULI_CHANNEL_1` through single-qubit, non-controlled two-qubit Clifford, controlled-Pauli, pair-measurement, and `MPP` gates, propagation through pair measurements, Pauli-target `OBSERVABLE_INCLUDE` logical sensitivity and gauge rejection, `HERALDED_PAULI_CHANNEL_1` basis behavior, `HERALDED_ERASE` threshold and basis behavior, propagation of approximate `PAULI_CHANNEL_1` basis probabilities through `H_XY`, `--allow_gauge_detectors` on upstream Bell-correlation gauge examples through `H`, `H_XY`, and `CX`, default gauge rejection, gauge-observable rejection even with gauge detectors allowed, single-qubit `DEPOLARIZE1` analyzer output using Stim's independent-channel conversion, two-qubit `DEPOLARIZE2` analyzer output and over-mixing rejection, exact-solved and approximate single-qubit `PAULI_CHANNEL_1` analyzer output, thresholded approximate two-qubit `PAULI_CHANNEL_2` analyzer output, shifted detector coordinates, and top-level folded repeat output.
- `cargo test -p stab-core --test dem_model_regressions` covers programmatic DEM detector-count overflow after raw `shift_detectors` offsets.
- `cargo test -p stab-core dem_analyzer` is the executable structural row for the M10-owned staged subset ported from `src/stim/simulators/error_analyzer.test.cc`; the remaining upstream `ErrorAnalyzer` cases stay represented by narrower M10 feature gaps and future generated-circuit parity work.
- `cargo test -p stab-core dem_analyzer_quantum_controlled_pauli` covers the M10-owned quantum controlled-Pauli propagation subset adapted from `src/stim/simulators/error_analyzer.test.cc`, including `CY`, `CZ`, `XCX`, `XCY`, `XCZ`, `YCX`, `YCY`, `YCZ`, and the decomposed composite controlled-Pauli analyzer case.
- `cargo test -p stab-core dem_analyzer_ignores_sweep_controls` and `cargo test -p stab-core dem_analyzer_measurement_record_feedback` cover the M10-owned classical-control subset ported from `src/stim/simulators/error_analyzer.test.cc`, including sweep-control no-ops and measurement-record feedback through `CX`, `CY`, `CZ`, `XCZ`, and `YCZ`.
- `cargo test -p stab-core --test dem_analyzer_measurements` covers the M10-owned duplicate detector-record parity, measurement-record out-of-range rejection, measure/reset basis behavior, repeated measure-reset behavior, `MPAD`, noisy basis-measurement, and pair-measurement subset ported from `src/stim/simulators/error_analyzer.test.cc`, including `MX`, `MY`, `MZ`, `MRX`, `MRY`, `MRZ`, `MXX`, `MYY`, and `MZZ` behavior.
- `cargo test -p stab-core --test dem_analyzer_heralded` covers the M10-owned heralded-erasure conditional-division and heralded single-qubit Pauli-channel subset ported from `src/stim/simulators/error_analyzer.test.cc`.
- `cargo test -p stab-core --test dem_analyzer_gauge` covers the M10-owned gauge-detector subset ported from `src/stim/simulators/error_analyzer.test.cc`, including default gauge-detector rejection, gauge-observable rejection even when gauge detectors are allowed, the simple reset and measurement-prelude gauge-pair variants, the no-fold multi-round gauge-detector case, and the noisy `DEPOLARIZE1` semantic distribution case.
- `cargo test -p stab-core --test dem_analyzer_observables` covers the M10-owned Pauli-target `OBSERVABLE_INCLUDE` commuting, Clifford-propagated, depolarizing-boundary, and vacuous detector/observable declaration subset ported from `src/stim/simulators/error_analyzer.test.cc`.
- `cargo test -p stab-core --test dem_analyzer_clifford` covers the M10-owned period-3 single-qubit Clifford analyzer subset ported from `src/stim/simulators/error_analyzer.test.cc`, including `C_XYZ` and `C_ZYX` pending-error propagation and deterministic-detector gauge checks.
- `cargo test -p stab-core --test dem_analyzer_coordinates` covers the M10-owned coordinate-tracking declaration subset ported from `src/stim/simulators/error_analyzer.test.cc`, including local detector coordinates and explicit DEM `shift_detectors` declarations.
- `cargo test -p stab-core --test dem_analyzer_tags` covers the M10-owned tagged-noise subset ported from `src/stim/simulators/error_analyzer.test.cc`, including tagged error output, detector declarations, logical observable declarations, coordinate shifts, empty observable declarations, and the tagged prefixed-repeat `--fold_loops` case.
- `cargo test -p stab-core dem_analyzer_mpp` covers the M10-owned `MPP` analyzer subset ported from `src/stim/simulators/error_analyzer.test.cc`, including product-measurement flips, noisy MPP result flips, deterministic MPP detector ordering, and rejection of non-deterministic MPP detector ordering.
- `cargo test -p stab-core error_decomp` covers the M10-owned public independent/disjoint XYZ conversion and depolarizing-channel conversion subset ported from `src/stim/util_bot/error_decomp.test.cc`, including the exact-solvable `PAULI_CHANNEL_1(0.1792, 0.1008, 0.2592)` Newton edge case.
- `cargo test -p stab-core graphlike` covers the M10-owned graphlike edge, node, graph, search-state, and shortest-undetectable-logical-error construction, canonicalization, equality, ordering, hashing, display, graph target conversion, separator handling, repeat flattening, DEM transition, no-error, distance-1, distance-2, distance-3, ignore-ungraphlike, and high-observable direct DEM subsets ported from `src/stim/search/graphlike/edge.test.cc`, `src/stim/search/graphlike/node.test.cc`, `src/stim/search/graphlike/graph.test.cc`, `src/stim/search/graphlike/search_state.test.cc`, and `src/stim/search/graphlike/algo.test.cc`.
- `cargo test -p stab-core hyper` covers the M10-owned hypergraph edge, node, graph, search-state, and undetectable-logical-error search construction, equality, ordering, display, DEM transition, separator target toggling, repeat flattening, max-weight filtering, no-error, distance-1, distance-2, distance-3, and direct hyperedge-search subsets ported from `src/stim/search/hyper/edge.test.cc`, `src/stim/search/hyper/node.test.cc`, `src/stim/search/hyper/graph.test.cc`, `src/stim/search/hyper/search_state.test.cc`, and `src/stim/search/hyper/algo.test.cc`.
- `cargo test -p stab-core sat_problem` covers the M10-owned shortest and likeliest WDIMACS SAT problem subset ported from `src/stim/search/sat/wcnf.test.cc`, including unsatisfiable empty cases, unweighted probability-insensitive clauses, weighted sign changes, half-probability omission, quantization validation, repeat flattening, and bounded repeat-expansion rejection.
- `cargo test -p stab-core matched_error` covers the M10-owned matched-error value-object, coordinate formatting, stack-frame formatting, circuit-location formatting, explained-error formatting, equality, and fill-helper subset ported from `src/stim/simulators/matched_error.test.cc`.
- `cargo test -p stab-core error_matcher` covers the M10-owned staged ErrorMatcher subset ported from `src/stim/simulators/error_matcher.test.cc`, including `X_ERROR`, correlated-error, noisy `MX`, noisy `MXX`, generated repetition-code `DEPOLARIZE1` matching, generated surface-code `DEPOLARIZE2` filter matching, `PAULI_CHANNEL_2` component matching, coordinate attribution, measurement-index attribution, DEM filtering with unmatched symptoms, representative reduction, bounded repeat traversal, bounded filter DEM expansion, clear rejection for repeat-contained noise, and formatting through the matched-error value objects. Generated surface-code repeat matching, heralded matching, and repeat-contained noise provenance remain future work.
- `cargo test -p stab-core sparse_rev_frame_tracker` covers the M10-owned sparse reverse detector-frame tracker subset ported from `src/stim/simulators/sparse_rev_frame_tracker.test.cc`, including generic tableau undo for the full single-qubit Clifford gate set, fixed two-qubit tableau-backed Clifford reverse propagation, `CX`/`CY`/`CZ` feedback-capable reverse propagation, measurement/reset feedback, pair measurements, detector and observable record propagation, Pauli observable targets, anticommutation recording, `MPAD`, and naive repeat traversal. PF6 later promoted supported-Clifford unitary-repeat folding with generated repeat coverage; analyzer/search-specific consumption and full error-matcher consumption remain future work.
- `cargo test -p stab-core generated_qec_dem` covers noisy generated repetition-code and rotated surface-code analyzer output against pinned Stim v1.16.0 DEM semantics, normalizing detector coordinate shifts, graphlike decomposition separators, repeat structure, and tight floating probability drift.
- `cargo test -p stab-core dem_analyzer_rejects_nested_repeat_expansion_budget` covers the non-folded analyzer expanded-repeat budget before gauge analysis can unroll nested repeats.
- `cargo test -p stab-core --test stim_format parser_rejects_excessive_repeat_nesting` covers the shared circuit parser repeat-nesting resource bound used by `analyze_errors`.
- `cargo test -p stab-cli m10` covers `stab analyze_errors`, the legacy `--analyze_errors` alias, oversized input rejection, excessive repeat-nesting rejection, measurement-flip output, unconditional and conditional correlated-error output, Pauli-target `OBSERVABLE_INCLUDE` output, vacuous observable declarations, tagged noise declarations, coordinate tracking, `H`, `H_XY`, period-3 Clifford gates, `CX`, `SWAP`, controlled-Pauli, composite controlled-Pauli decomposition, measurement-record feedback, and non-controlled two-qubit Clifford Pauli-error propagation fixtures, `MPP` product-measurement and deterministic-ordering output, `--decompose_errors` fallback output, `--block_decompose_from_introducing_remnant_edges`, `--ignore_decomposition_failures`, `--allow_gauge_detectors` on upstream CLI, `H_XY`, reset, measure-reset, multi-round gauge, and measurement-prelude gauge examples, default gauge rejection, gauge-observable rejection even with gauge detectors allowed, identity-noise no-op output, reset cutoff output, simple Pauli-error output, `DEPOLARIZE1`, `DEPOLARIZE2`, exact-solved `PAULI_CHANNEL_1`, bare and numeric-threshold `--approximate_disjoint_errors` for `PAULI_CHANNEL_1`, approximate `PAULI_CHANNEL_1` through non-controlled two-qubit Clifford, controlled-Pauli, pair-measurement, and `MPP` gates, numeric-threshold `--approximate_disjoint_errors` for `PAULI_CHANNEL_2`, `HERALDED_ERASE`, `ELSE_CORRELATED_ERROR` fixtures, `--fold_loops` on high-repeat, repeat-only nested, and tagged-prefixed repeat fixtures, and current flag parsing on supported circuits.
- `cargo test -p stab-bench m10_dem_benchmark_rows_have_stab_compare_runners` covers Stab-side M10 graphlike search, generated surface-code analyzer, error-decomposition utility, `analyze_errors --decompose_errors`, `.dem` parse, `.dem` print, and loop-folding analyzer benchmark runners. `just bench::baseline --only M10 --out target/benchmarks/m10-goal-baseline --target-seconds 0.01 --cli-iterations 3` records a fresh local pinned Stim v1.16.0 M10 baseline, and `just bench::compare --milestone M10 --baseline target/benchmarks/m10-goal-baseline/baseline.json --strict --report target/benchmarks/m10-goal-strict-compare` compares the Stab runners against that baseline with contract-only rows reported explicitly.

## Implementation Areas

- Added `DetectorErrorModel`, DEM instruction, target, repeat block, detector id, observable id, parser, canonical printer, detector counting, observable counting, and detector-shift helpers in `stab-core`.
- Added checked detector-count overflow handling for programmatic DEMs that use raw `shift_detectors` offsets near `u64::MAX`.
- Added analytical detector counting through large DEM repeats and bounded flattening validation for public graphlike, hypergraph, and SAT analysis paths.
- Added the first graphlike search internal types for observable masks, edges, nodes, graphs, canonical search states, and public shortest graphlike undetectable logical error search over direct DEM graph structures, including DEM flattening into graphlike edges and DEM error-instruction emission for search-state transitions.
- Added the first hypergraph search internal types and direct undetectable-logical-error algorithm for observable masks, hyperedges, nodes, graphs, and search states, including separator-aware target toggling, max-weight filtering, DEM flattening into hyperedges, detector-set BFS, path backtracking with duplicate-pair cancellation, and DEM error-instruction emission for search-state transitions.
- Added shortest and likeliest error SAT problem generation in WDIMACS form, including Stim-compatible XOR parity constraints, soft-clause weighting, half-probability omission, detector hard clauses, observable-flip hard clauses, and repeat flattening.
- Added structural generated-QEC DEM comparison that canonicalizes graphlike decomposition separators by detector and observable XOR semantics.
- Added matched-error value objects for coordinate-aware DEM targets, circuit targets, stack frames, circuit error locations, flipped measurements, and explained errors, including Stim-compatible display and fill helpers used by later error-matcher work.
- Added a staged error matcher that isolates individual candidate circuit errors, reuses the existing analyzer to derive each candidate's DEM symptoms, applies optional DEM filtering, and returns matched-error locations with detector and qubit coordinates for direct small-circuit cases, generated repetition-code `DEPOLARIZE1` noise, generated surface-code `DEPOLARIZE2` filter matches, and `PAULI_CHANNEL_2` component noise.
- Added ErrorMatcher preflight checks for repeat traversal budgets, bounded repeated filter DEM expansion, and explicit repeat-contained-noise rejection until recursive provenance support is implemented.
- Added a staged sparse reverse detector-frame tracker for symbolic detector/observable sensitivities across reverse circuit traversal, including full single-qubit Clifford undo, fixed two-qubit tableau-backed Clifford undo, `CX`/`CY`/`CZ` feedback-capable reverse propagation, measurement/reset undo, detector and observable propagation, record feedback, anticommutation reporting, pair measurements, `MPAD`, and naive repeat traversal.
- Added a staged circuit-to-DEM analyzer for deterministic detector declarations, measurement-flip errors, unconditional and conditional correlated Pauli errors, identity-noise no-ops, reset cutoff of pending single-qubit noise, identical-symptom error merging, Pauli-target `OBSERVABLE_INCLUDE` logical sensitivity, and simple single-qubit `X_ERROR`, `Y_ERROR`, and `Z_ERROR` effects feeding measurement-record detectors and observables.
- Added single-qubit `DEPOLARIZE1` handling for probabilities up to `3/4` using Stim's independent per-channel probability conversion, with over-mixing rejection.
- Added two-qubit `DEPOLARIZE2` handling for probabilities up to `15/16`, using Stim's independent two-qubit Pauli-channel decomposition and identical-symptom XOR merging.
- Added exact-solved `PAULI_CHANNEL_1` handling for single-qubit channels that can be represented as independent errors without `--approximate_disjoint_errors`, including the Newton-solved edge cases covered by the public error-decomposition utility.
- Exposed the M10 independent/disjoint XYZ error-decomposition utility through typed probability triples for direct testing, benchmarking, and analyzer reuse.
- Added approximate `PAULI_CHANNEL_1` handling for remaining single-qubit channels under bare and numeric-threshold `--approximate_disjoint_errors`, including rejection when approximation is not explicitly enabled or when a component exceeds the threshold.
- Added approximate `PAULI_CHANNEL_2` handling for two-qubit channels under numeric-threshold `--approximate_disjoint_errors`, including per-channel disjoint component summation before independent error merging.
- Added pair-measurement analyzer handling for `MXX`, `MYY`, `MZZ`, and `MPP`, including pending Pauli-error parity updates, MPP result-flip probability handling, and matching backward gauge-tracker sensitivity for deterministic detector checks.
- Added `HERALDED_PAULI_CHANNEL_1` analyzer handling for single-component basis errors and thresholded disjoint multi-component errors.
- Added `HERALDED_ERASE` analyzer handling as thresholded disjoint heralded Pauli components with one herald measurement per target.
- Added declaration-order tracking for analyzer detector, logical observable, and coordinate-shift declarations so `SHIFT_COORDS` becomes explicit DEM `shift_detectors` output while detector coordinates remain local.
- Added scoped Clifford propagation for pending Pauli errors through single-qubit Clifford gates, `SWAP`, controlled-Pauli gates, and non-controlled two-qubit Clifford gates by applying local tableaux, sweep-control no-op handling for `CX`, propagation of pending single-qubit `PAULI_CHANNEL_1` basis probabilities through single-qubit Clifford gates and `SWAP`, expansion of pending approximate single-qubit `PAULI_CHANNEL_1` into disjoint grouped Pauli-product errors when crossing non-controlled entangling two-qubit Clifford, controlled-Pauli, pair-measurement, and `MPP` gates, and cancellation of duplicate Pauli effects after propagation.
- Added `--decompose_errors` target-level decomposition paths that first try exact decomposition using known graphlike components, then optionally introduce a graphlike remnant edge, respect `--block_decompose_from_introducing_remnant_edges`, support `--ignore_decomposition_failures`, and reject simple undecomposable detector triples.
- Added a backward detector-sensitivity pass for the first `--allow_gauge_detectors` cases, including default rejection of non-deterministic detectors, rejection of non-deterministic measurement-record and Pauli-target observables even with gauge detectors allowed, 50 percent gauge-error emission for detector-only gauge sets through `H`, `H_XY`, `CX`, reset, measure-reset, and measurement-prelude variants, and generic inverse-Clifford single-qubit sensitivity propagation for deterministic checks through period-3 gates.
- Added staged `--fold_loops` handling for top-level repeat blocks, repeat-only nested bodies, and the current prefixed-repeat tagged-noise case by analyzing one body, wrapping repeated bodies in DEM repeat blocks, appending detector shifts for non-folded bodies, and preserving final unshifted declarations when required for Stim-compatible output.
- Added non-folded analyzer preflight budgets for nested repeat expansion so analyzer and gauge passes reject excessive expansion before unrolling repeated bodies.
- Added `stab analyze_errors` CLI dispatch, including the legacy `--analyze_errors` alias, current staged flag parsing, a 64 MiB input cap, and rejection coverage for excessive repeat nesting through the shared circuit parser.
- Extended the oracle core fixture runner to support `core-dem-parse-print`.
- Implemented the current M10 exact and structural rows for `.dem` parse-print and basic `analyze_errors`.
- Added Stab-side benchmark compare runners for the M10 graphlike search, generated surface-code analyzer, error-decomposition utility, `analyze_errors --decompose_errors`, `.dem` parse, `.dem` print, and loop-folding analyzer rows.

## Current Evidence

Rows marked `Partial` satisfy the scoped M10 done criteria for the named subset and intentionally defer broader upstream parity to explicit future work or follow-up scope. A `Partial` row is complete for M10 only when its evidence names the implemented subset and the deferred cases are recorded in this report, the milestone gap log, or the Future Plan.

| Requirement | Status | Evidence |
| --- | --- | --- |
| `.dem` parser and canonical printer | Partial | `DetectorErrorModel::from_dem_str`, `DetectorErrorModel::to_dem_string`, `just oracle::run --milestone M10 --exact` |
| DEM core types, repeats, coordinates, detector shifts, observables, separators, probability validation | Partial | `coverage-dem-dem-instruction`, `cargo test -p stab-core dem_instruction`, `cargo test -p stab-core dem` |
| M10 resource boundaries | Implemented | `cargo test -p stab-core --test dem_model_regressions`, `cargo test -p stab-core dem_analyzer_rejects_nested_repeat_expansion_budget`, `cargo test -p stab-core error_matcher_rejects_nested_repeat_expansion_budget`, `cargo test -p stab-core error_matcher_rejects_filter_dem_repeat_expansion_budget`, `cargo test -p stab-cli m10`; remaining scale removal requires streaming or folded traversal evidence |
| Graphlike search structures and direct shortest-error algorithm | Partial | `coverage-search-graphlike-algo`, `coverage-search-graphlike-edge`, `coverage-search-graphlike-node`, `coverage-search-graphlike-graph`, `coverage-search-graphlike-search-state`, `cargo test -p stab-core graphlike`, `cargo test -p stab-core dem_shortest_graphlike_logical_error_api_matches_direct_dem`, `cargo test -p stab-bench m10_dem_benchmark_rows_have_stab_compare_runners`, `just oracle::run --milestone M10 --structural`, `just bench::compare --milestone M10` |
| Hypergraph search structures and direct undetectable-logical-error algorithm | Partial | `coverage-search-hyper-algo`, `coverage-search-hyper-edge`, `coverage-search-hyper-node`, `coverage-search-hyper-graph`, `coverage-search-hyper-search-state`, `cargo test -p stab-core hyper`, `just oracle::run --milestone M10 --structural`; generated circuit search parity remains future analyzer/QEC structural work |
| SAT WDIMACS problem generation | Implemented | `coverage-search-sat-wcnf`, `cargo test -p stab-core sat_problem`, `just oracle::run --milestone M10 --structural` |
| Matched-error value objects | Implemented | `coverage-simulators-matched-error`, `cargo test -p stab-core matched_error`, `just oracle::run --milestone M10 --structural` |
| ErrorMatcher staged subset | Implemented | `coverage-simulators-error-matcher`, `cargo test -p stab-core error_matcher`, `cargo test -p stab-core generated_repetition_code_data_depolarization`, `cargo test -p stab-core generated_surface_code_clifford_depolarization_filter`, `cargo test -p stab-core pauli_channel2_components_match_stim_error_matcher_order`, `cargo test -p stab-core error_matcher_rejects_repeat_contained_noise_until_recursive_matching_exists`, `cargo test -p stab-core error_matcher_rejects_nested_repeat_expansion_budget`, `cargo test -p stab-core error_matcher_rejects_filter_dem_repeat_expansion_budget`, `just oracle::run --milestone M10 --structural`; generated surface-code repeat matching, heralded matching, and repeat-contained noise provenance remain future work |
| Sparse reverse detector-frame tracker staged subset | Implemented | `coverage-simulators-sparse-rev-frame-tracker`, `cargo test -p stab-core sparse_rev_frame_tracker`, `just oracle::run --milestone M10 --structural`; PF5 later promoted fixed two-qubit tableau-backed direct propagation and PF6 later promoted supported-Clifford unitary-repeat folding for the full single-qubit Clifford gate set plus fixed two-qubit tableau-backed Clifford gates with plain qubit-pair targets and generated repeat coverage, while analyzer/search-specific sparse-tracker consumption and error-matcher integration remain future work |
| `ErrorAnalyzer` staged structural coverage | Implemented | `coverage-simulators-error-analyzer`, `cargo test -p stab-core dem_analyzer`, `cargo test -p stab-core dem_analyzer_obs_include`, `cargo test -p stab-core dem_analyzer_rejects_unpaired_obs_include`, `cargo test -p stab-core dem_analyzer_mpp`, `cargo test -p stab-core dem_analyzer_quantum_controlled_pauli`, `cargo test -p stab-core dem_analyzer_ignores_sweep_controls`, `cargo test -p stab-core dem_analyzer_measurement_record_feedback`, `cargo test -p stab-core --test dem_analyzer_gauge`, `just oracle::run --milestone M10 --structural`; remaining upstream behavior is tracked by narrower partial rows and future generated-circuit parity |
| `stim analyze_errors` staged default behavior | Partial | `m10-analyze-errors-tagged-noise`, `cargo test -p stab-cli m10`, `cargo test -p stab-cli tagged_noise`, `cargo test -p stab-bench m10_dem_benchmark_rows_have_stab_compare_runners`, `just oracle::run --milestone M10 --structural`, `just bench::compare --milestone M10` |
| `stim analyze_errors` unconditional correlated Pauli errors | Partial | `m10-analyze-errors-correlated-error`, `cargo test -p stab-core correlated_error`, `cargo test -p stab-cli correlated_error`, `just oracle::run --milestone M10 --exact`, `just oracle::record --check-clean` |
| `stim analyze_errors --decompose_errors` graphlike decomposition | Partial | `m10-analyze-errors-decompose-fallback`, `m10-analyze-errors-decompose-known-components`, `m10-analyze-errors-composite-controlled-pauli`, `cargo test -p stab-core decompose`, `cargo test -p stab-cli decompose`, `cargo test -p stab-cli ignore_decomposition`, `cargo test -p stab-cli composite_controlled_pauli`, `cargo test -p stab-bench m10_dem_benchmark_rows_have_stab_compare_runners`, `just oracle::run --milestone M10 --exact`, `just bench::compare --milestone M10` |
| `stim analyze_errors --allow_gauge_detectors` | Partial | `m10-analyze-errors-allow-gauge-detectors`, `m10-analyze-errors-allow-gauge-detectors-hxy`, `m10-analyze-errors-gauge-detector-variants`, `m10-analyze-errors-multi-round-gauge`, `cargo test -p stab-core gauge`, `cargo test -p stab-core --test dem_analyzer_gauge`, `cargo test -p stab-cli gauge`, `cargo test -p stab-cli multi_round_gauge`, `just oracle::run --milestone M10 --exact`, `just oracle::record --check-clean` |
| `stim analyze_errors --approximate_disjoint_errors` conditional correlated Pauli errors | Partial | `m10-analyze-errors-else-correlated-error`, `cargo test -p stab-core else_correlated`, `cargo test -p stab-cli else_correlated`, `just oracle::run --milestone M10 --exact`, `just oracle::record --check-clean` |
| `stim analyze_errors` identity-noise no-ops | Partial | `m10-analyze-errors-identity-noise`, `cargo test -p stab-core identity_noise`, `cargo test -p stab-cli identity_noise`, `just oracle::run --milestone M10 --exact` |
| `stim analyze_errors` measurement-flip errors, measure/reset basis behavior, noisy basis measurements, `MPAD`, pair measurements, `MPP`, duplicate detector-record parity, coordinate tracking, and identical-symptom merging | Partial | `m10-analyze-errors-measurement-flip`, `m10-analyze-errors-measure-reset-basis`, `m10-analyze-errors-noisy-basis-measurements`, `m10-analyze-errors-mpad-pair-measurements`, `m10-analyze-errors-mpp-product-measurements`, `m10-analyze-errors-pauli-channel1-product-measurements`, `m10-analyze-errors-coordinate-tracking`, `cargo test -p stab-core dem_analyzer_maps_measurement_flip_probability_to_error`, `cargo test -p stab-core dem_analyzer_merges_identical_error_symptoms`, `cargo test -p stab-core --test dem_analyzer_measurements`, `cargo test -p stab-core --test dem_analyzer_mpp`, `cargo test -p stab-core --test dem_analyzer_coordinates`, `cargo test -p stab-cli measurement_flip`, `cargo test -p stab-cli measure_reset_basis`, `cargo test -p stab-cli noisy_basis_measurements`, `cargo test -p stab-cli mpad_pair_measurements`, `cargo test -p stab-cli mpp_product_measurements`, `cargo test -p stab-cli pauli_channel1_product_measurements`, `cargo test -p stab-cli coordinate_tracking`, `just oracle::run --milestone M10 --exact` |
| `stim analyze_errors` `OBSERVABLE_INCLUDE` declarations | Partial | `m10-analyze-errors-obs-include-pauli`, `m10-analyze-errors-obs-include-boundaries`, `m10-analyze-errors-vacuous-observable`, `cargo test -p stab-core dem_analyzer_obs_include`, `cargo test -p stab-core dem_analyzer_rejects_unpaired_obs_include`, `cargo test -p stab-core --test dem_analyzer_observables`, `cargo test -p stab-cli obs_include_pauli`, `cargo test -p stab-cli obs_include_boundaries`, `cargo test -p stab-cli vacuous_observable`, `just oracle::run --milestone M10 --exact` |
| `stim analyze_errors` reset cutoff behavior | Partial | `m10-analyze-errors-reset-clears-error`, `cargo test -p stab-core reset`, `cargo test -p stab-cli reset_clears_error`, `just oracle::run --milestone M10 --exact` |
| `stim analyze_errors` Clifford propagation | Partial | `m10-analyze-errors-h-propagates-pauli-error`, `m10-analyze-errors-hxy-propagates-pauli-error`, `m10-analyze-errors-period3-gates`, `m10-analyze-errors-cnot-propagates-pauli-error`, `m10-analyze-errors-swap-propagates-pauli`, `m10-analyze-errors-two-qubit-cliffords`, `m10-analyze-errors-pauli-channel1-two-qubit-clifford`, `m10-analyze-errors-pauli-channel1-controlled-pauli`, `cargo test -p stab-core propagates_pauli`, `cargo test -p stab-core --test dem_analyzer_clifford`, `cargo test -p stab-core --test dem_analyzer_swap`, `cargo test -p stab-core --test dem_analyzer_two_qubit_clifford`, `cargo test -p stab-core --test dem_analyzer_pauli_channel_clifford`, `cargo test -p stab-cli propagates_pauli`, `cargo test -p stab-cli period3_gates`, `cargo test -p stab-cli two_qubit_cliffords`, `cargo test -p stab-cli pauli_channel1_two_qubit_clifford`, `cargo test -p stab-cli pauli_channel1_controlled_pauli`, `just oracle::run --milestone M10 --exact`, `just oracle::record --check-clean` |
| `stim analyze_errors` controlled-Pauli propagation | Partial | `m10-analyze-errors-controlled-pauli`, `m10-analyze-errors-pauli-channel1-controlled-pauli`, `m10-analyze-errors-composite-controlled-pauli`, `cargo test -p stab-core dem_analyzer_quantum_controlled_pauli`, `cargo test -p stab-core --test dem_analyzer_pauli_channel_clifford`, `cargo test -p stab-cli controlled_pauli`, `cargo test -p stab-cli pauli_channel1_controlled_pauli`, `cargo test -p stab-cli composite_controlled_pauli`, `just oracle::run --milestone M10 --exact`, `just oracle::record --check-clean` |
| `stim analyze_errors` measurement-record feedback | Partial | `m10-analyze-errors-measurement-feedback`, `cargo test -p stab-core dem_analyzer_measurement_record_feedback`, `cargo test -p stab-cli measurement_feedback`, `just oracle::run --milestone M10 --exact`, `just oracle::record --check-clean` |
| `stim analyze_errors` single-qubit `DEPOLARIZE1` | Partial | `m10-analyze-errors-depolarize1`, `coverage-util-bot-error-decomp`, `cargo test -p stab-core depolarize1`, `cargo test -p stab-core error_decomp`, `cargo test -p stab-core dem_error_decomposition_api_returns_typed_probability_triples`, `cargo test -p stab-cli depolarize1`, `cargo test -p stab-bench m10_dem_benchmark_rows_have_stab_compare_runners`, `just oracle::run --milestone M10 --exact`, `just bench::compare --milestone M10` |
| `stim analyze_errors` two-qubit `DEPOLARIZE2` | Partial | `m10-analyze-errors-depolarize2`, `cargo test -p stab-core depolarize2`, `cargo test -p stab-cli depolarize2`, `just oracle::run --milestone M10 --exact`, `just oracle::record --check-clean` |
| `stim analyze_errors` exact-solved single-qubit `PAULI_CHANNEL_1` | Partial | `m10-analyze-errors-exact-pauli-channel1`, `cargo test -p stab-core exact_solved_pauli_channel1`, `cargo test -p stab-core dem_analyzer_pauli_channel1_accepts_exact_newton_edge_case`, `cargo test -p stab-core disjoint_to_independent_pauli_channel1_solves_newton_edge_case`, `cargo test -p stab-cli exact_pauli_channel1`, `just oracle::run --milestone M10 --exact` |
| `stim analyze_errors --approximate_disjoint_errors` | Partial | `m10-analyze-errors-approx-pauli-channel1`, `m10-analyze-errors-approx-pauli-channel1-threshold`, `m10-analyze-errors-pauli-channel1-two-qubit-clifford`, `m10-analyze-errors-pauli-channel1-controlled-pauli`, `m10-analyze-errors-pauli-channel1-product-measurements`, `m10-analyze-errors-approx-pauli-channel2-threshold`, `cargo test -p stab-core threshold`, `cargo test -p stab-core pauli_channel1`, `cargo test -p stab-core pauli_channel2`, `cargo test -p stab-core --test dem_analyzer_pauli_channel_clifford`, `cargo test -p stab-core --test dem_analyzer_measurements`, `cargo test -p stab-cli numeric_threshold`, `cargo test -p stab-cli pauli_channel1_two_qubit_clifford`, `cargo test -p stab-cli pauli_channel1_controlled_pauli`, `cargo test -p stab-cli pauli_channel1_product_measurements`, `cargo test -p stab-cli pauli_channel2`, `just oracle::run --milestone M10 --exact` |
| `stim analyze_errors --approximate_disjoint_errors` heralded erasure | Partial | `m10-analyze-errors-heralded-erase`, `m10-analyze-errors-heralded-erase-conditional`, `cargo test -p stab-core heralded_erase`, `cargo test -p stab-core --test dem_analyzer_heralded`, `cargo test -p stab-cli heralded_erase`, `cargo test -p stab-cli heralded_erase_conditional`, `just oracle::run --milestone M10 --exact` |
| `stim analyze_errors --approximate_disjoint_errors` heralded Pauli channel | Partial | `m10-analyze-errors-heralded-pauli-channel1`, `coverage-util-top-circuit-to-dem`, `cargo test -p stab-core circuit_to_dem`, `cargo test -p stab-core --test dem_analyzer_heralded`, `cargo test -p stab-cli heralded_pauli_channel1`, `just oracle::run --milestone M10 --exact`, `just oracle::run --milestone M10 --structural` |
| Structural DEM comparators for generated QEC circuits and generated error matching | Partial | `coverage-simulators-generated-qec-dem`, `cargo test -p stab-core generated_qec_dem`, `just oracle::run --milestone M10 --structural`; broader generated QEC DEM coverage and generated surface-code repeat error-matcher parity remain future analyzer/search work |
| Loop folding without flattening high-repeat circuits | Partial | `m10-analyze-errors-fold-repeat`, `m10-analyze-errors-fold-nested-repeat`, `m10-analyze-errors-tagged-repeat`, `cargo test -p stab-core dem_analyzer_fold`, `cargo test -p stab-core --test dem_analyzer_loop_folding`, `cargo test -p stab-core --test dem_analyzer_tags`, `cargo test -p stab-core dem_analyzer_rejects_nested_repeat_expansion_budget`, `cargo test -p stab-cli tagged_repeat`, `just bench::compare --milestone M10`; prefix/suffix nested loop folding and observable-period solving remain future work |
| M10 benchmark reporting | Implemented | `cargo test -p stab-bench m10_dem_benchmark_rows_have_stab_compare_runners`, `just bench::baseline --only M10 --out target/benchmarks/m10-goal-baseline --target-seconds 0.01 --cli-iterations 3`, and `just bench::compare --milestone M10 --baseline target/benchmarks/m10-goal-baseline/baseline.json --strict --report target/benchmarks/m10-goal-strict-compare`; broader scale comparability remains future work for generated QEC and M12 performance hardening |

## Audit And Review Closure

- The latest M10 milestone audit found missing durable audit/review closure evidence, a stale strict benchmark baseline path, and unclear `Partial` row meaning. This report now records the audit/review closure, cites fresh `m10-goal-baseline` and `m10-goal-strict-compare` artifacts, and defines `Partial` as complete only for the scoped M10 subset with explicit deferrals.
- The latest M10 full-code-review found the `PAULI_CHANNEL_1` independent/disjoint Newton derivative bug, unchecked detector-count overflow after shifted detector ids, unbounded nested analyzer repeat expansion, and misleading ErrorMatcher behavior for repeat-contained noise, including repeated noisy `MPP`, plus repeated filter DEM expansion. The implementation now fixes those issues and adds the targeted regressions named in the evidence table.
- The benchmark evidence loophole revealed by the audit is logged as resolved in `docs/plans/milestone-spec-gaps.md`; strict M10 benchmark claims now require a current named baseline and matching strict compare report. No open M10 under-specification entry remains after this closure pass.

## Verification Commands

- `cargo fmt --all --check`
- `cargo test -p stab-core dem_analyzer_pauli_channel1_accepts_exact_newton_edge_case --quiet`
- `cargo test -p stab-core disjoint_to_independent_pauli_channel1_solves_newton_edge_case --quiet`
- `cargo test -p stab-core dem_count_detectors_rejects_shifted_detector_count_overflow --quiet`
- `cargo test -p stab-core dem_analyzer_rejects_nested_repeat_expansion_budget --quiet`
- `cargo test -p stab-core error_matcher_rejects_repeat_contained_mpp_noise_until_recursive_matching_exists --quiet`
- `cargo test -p stab-core --test error_matcher_basic --quiet`
- `cargo test -p stab-core dem --quiet`
- `cargo test -p stab-cli m10 --quiet`
- `cargo test -p stab-bench m10_dem_benchmark_rows_have_stab_compare_runners --quiet`
- `cargo test -p stab-bench --quiet`
- `just oracle::run --milestone M10 --exact`
- `just oracle::run --milestone M10 --structural`
- `just bench::baseline --only M10 --out target/benchmarks/m10-goal-baseline --target-seconds 0.01 --cli-iterations 3`
- `just bench::compare --milestone M10 --baseline target/benchmarks/m10-goal-baseline/baseline.json --strict --report target/benchmarks/m10-goal-strict-compare`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace --quiet`
- `just maintenance::pre-commit`
