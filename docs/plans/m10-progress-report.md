# M10 Progress Report

## Milestone

M10: Detector Error Model Core

## Status

Partial progress, not milestone-complete.
This slice implements the `.dem` format core and staged `analyze_errors` paths needed by the existing deterministic oracle rows, correlated-error rows, the first single-qubit and `CX` Pauli-propagation rows, the first `--decompose_errors` fallback and known-components rows, the high-repeat loop-folding row, the first `--allow_gauge_detectors` rows, the first approximate disjoint-error threshold rows, the first generated-QEC DEM structural rows, and the first generated repetition-code and surface-code error-matcher structural rows.
M10 still requires broader graphlike algorithm integration, generated-circuit hypergraph search parity, general loop folding, broader two-qubit Clifford propagation, broader decomposition behavior, broader gauge-detector behavior, broader approximation behavior, broader sparse reverse tracker loop folding and matcher integration, broader generated-QEC DEM equivalence, broader generated-circuit error-matcher parity, and broader benchmark scale comparability before milestone audit can accept it.

## Tests Ported Or Created

- `cargo test -p stab-core dem` covers DEM target limits, DEM instruction separated target groups, repeat blocks, detector shifts, coordinates, observables, probabilities, separators, invalid input, deterministic detector declarations, measurement-flip analyzer output, identical-symptom merging and cancellation, unconditional and conditional correlated-error output, graphlike decomposition fallback, known-component decomposition, remnant-edge blocking, ignored decomposition failures, and undecomposable triple rejection under `--decompose_errors`, identity-noise no-ops, reset cutoff of pending single-qubit errors and channels, simple Pauli-error analyzer output, propagation of pending Pauli errors through single-qubit Clifford gates and controlled-Pauli gates, propagation through pair measurements, Pauli-target `OBSERVABLE_INCLUDE` logical sensitivity and gauge rejection, `HERALDED_PAULI_CHANNEL_1` basis behavior, `HERALDED_ERASE` threshold and basis behavior, propagation of approximate `PAULI_CHANNEL_1` basis probabilities through `H_XY`, `--allow_gauge_detectors` on upstream Bell-correlation gauge examples through `H`, `H_XY`, and `CX`, default gauge rejection, gauge-observable rejection even with gauge detectors allowed, single-qubit `DEPOLARIZE1` analyzer output using Stim's independent-channel conversion, two-qubit `DEPOLARIZE2` analyzer output and over-mixing rejection, exact-solved and approximate single-qubit `PAULI_CHANNEL_1` analyzer output, thresholded approximate two-qubit `PAULI_CHANNEL_2` analyzer output, shifted detector coordinates, and top-level folded repeat output.
- `cargo test -p stab-core dem_analyzer` is the executable structural row for the M10-owned staged subset ported from `src/stim/simulators/error_analyzer.test.cc`; the remaining upstream `ErrorAnalyzer` cases stay represented by narrower M10 feature gaps and future generated-circuit parity work.
- `cargo test -p stab-core dem_analyzer_quantum_controlled_pauli` covers the M10-owned quantum controlled-Pauli propagation subset adapted from `src/stim/simulators/error_analyzer.test.cc`, including `CY`, `CZ`, `XCX`, `XCY`, `XCZ`, `YCX`, `YCY`, `YCZ`, and the decomposed composite controlled-Pauli analyzer case.
- `cargo test -p stab-core dem_analyzer_ignores_sweep_controls` and `cargo test -p stab-core dem_analyzer_measurement_record_feedback` cover the M10-owned classical-control subset ported from `src/stim/simulators/error_analyzer.test.cc`, including sweep-control no-ops and measurement-record feedback through `CX`, `CY`, `CZ`, `XCZ`, and `YCZ`.
- `cargo test -p stab-core --test dem_analyzer_measurements` covers the M10-owned duplicate detector-record parity, measurement-record out-of-range rejection, `MPAD`, noisy basis-measurement, and pair-measurement subset ported from `src/stim/simulators/error_analyzer.test.cc`, including `MX`, `MY`, `MZ`, `MRX`, `MRY`, `MRZ`, `MXX`, `MYY`, and `MZZ` behavior.
- `cargo test -p stab-core --test dem_analyzer_heralded` covers the M10-owned heralded-erasure conditional-division and heralded single-qubit Pauli-channel subset ported from `src/stim/simulators/error_analyzer.test.cc`.
- `cargo test -p stab-core --test dem_analyzer_gauge` covers the M10-owned no-fold multi-round gauge-detector subset ported from `src/stim/simulators/error_analyzer.test.cc`, including the noiseless exact gauge-pair case and the noisy `DEPOLARIZE1` semantic distribution case.
- `cargo test -p stab-core --test dem_analyzer_observables` covers the M10-owned Pauli-target `OBSERVABLE_INCLUDE` commuting, Clifford-propagated, and depolarizing-boundary subset ported from `src/stim/simulators/error_analyzer.test.cc`.
- `cargo test -p stab-core --test dem_analyzer_tags` covers the M10-owned non-repeat tagged-noise subset ported from `src/stim/simulators/error_analyzer.test.cc`, including tagged error output, detector declarations, logical observable declarations, coordinate shifts, and empty observable declarations.
- `cargo test -p stab-core dem_analyzer_mpp` covers the M10-owned `MPP` analyzer subset ported from `src/stim/simulators/error_analyzer.test.cc`, including product-measurement flips, noisy MPP result flips, deterministic MPP detector ordering, and rejection of non-deterministic MPP detector ordering.
- `cargo test -p stab-core error_decomp` covers the M10-owned public independent/disjoint XYZ conversion and depolarizing-channel conversion subset ported from `src/stim/util_bot/error_decomp.test.cc`.
- `cargo test -p stab-core graphlike` covers the M10-owned graphlike edge, node, graph, search-state, and shortest-undetectable-logical-error construction, canonicalization, equality, ordering, hashing, display, graph target conversion, separator handling, repeat flattening, DEM transition, no-error, distance-1, distance-2, distance-3, ignore-ungraphlike, and high-observable direct DEM subsets ported from `src/stim/search/graphlike/edge.test.cc`, `src/stim/search/graphlike/node.test.cc`, `src/stim/search/graphlike/graph.test.cc`, `src/stim/search/graphlike/search_state.test.cc`, and `src/stim/search/graphlike/algo.test.cc`.
- `cargo test -p stab-core hyper` covers the M10-owned hypergraph edge, node, graph, search-state, and undetectable-logical-error search construction, equality, ordering, display, DEM transition, separator target toggling, repeat flattening, max-weight filtering, no-error, distance-1, distance-2, distance-3, and direct hyperedge-search subsets ported from `src/stim/search/hyper/edge.test.cc`, `src/stim/search/hyper/node.test.cc`, `src/stim/search/hyper/graph.test.cc`, `src/stim/search/hyper/search_state.test.cc`, and `src/stim/search/hyper/algo.test.cc`.
- `cargo test -p stab-core sat_problem` covers the M10-owned shortest and likeliest WDIMACS SAT problem subset ported from `src/stim/search/sat/wcnf.test.cc`, including unsatisfiable empty cases, unweighted probability-insensitive clauses, weighted sign changes, half-probability omission, quantization validation, and repeat flattening.
- `cargo test -p stab-core matched_error` covers the M10-owned matched-error value-object, coordinate formatting, stack-frame formatting, circuit-location formatting, explained-error formatting, equality, and fill-helper subset ported from `src/stim/simulators/matched_error.test.cc`.
- `cargo test -p stab-core error_matcher` covers the M10-owned staged ErrorMatcher subset ported from `src/stim/simulators/error_matcher.test.cc`, including `X_ERROR`, correlated-error, noisy `MX`, noisy `MXX`, generated repetition-code `DEPOLARIZE1` matching, generated surface-code `DEPOLARIZE2` filter matching, `PAULI_CHANNEL_2` component matching, coordinate attribution, measurement-index attribution, DEM filtering with unmatched symptoms, representative reduction, and formatting through the matched-error value objects. Generated surface-code repeat matching, heralded matching, and repeat-contained noise remain future work.
- `cargo test -p stab-core sparse_rev_frame_tracker` covers the M10-owned sparse reverse detector-frame tracker subset ported from `src/stim/simulators/sparse_rev_frame_tracker.test.cc`, including tableau undo for `H`, `S`, `C_XYZ`, and `CX`, measurement/reset feedback, pair measurements, detector and observable record propagation, Pauli observable targets, anticommutation recording, `MPAD`, and naive repeat traversal. Optimized loop folding, all-unitary fuzzing, and full error-matcher consumption remain future work.
- `cargo test -p stab-core generated_qec_dem` covers noisy generated repetition-code and rotated surface-code analyzer output against pinned Stim v1.16.0 DEM semantics, normalizing detector coordinate shifts and allowing only tight floating probability drift.
- `cargo test -p stab-cli m10` covers `stab analyze_errors`, the legacy `--analyze_errors` alias, measurement-flip output, unconditional and conditional correlated-error output, Pauli-target `OBSERVABLE_INCLUDE` output, `H`, `H_XY`, and `CX` Pauli-error propagation fixtures, `--decompose_errors` fallback output, `--block_decompose_from_introducing_remnant_edges`, `--ignore_decomposition_failures`, `--allow_gauge_detectors` on upstream CLI and `H_XY` gauge examples, default gauge rejection, gauge-observable rejection even with gauge detectors allowed, identity-noise no-op output, reset cutoff output, simple Pauli-error output, `DEPOLARIZE1`, `DEPOLARIZE2`, exact-solved `PAULI_CHANNEL_1`, bare and numeric-threshold `--approximate_disjoint_errors` for `PAULI_CHANNEL_1`, numeric-threshold `--approximate_disjoint_errors` for `PAULI_CHANNEL_2`, `HERALDED_ERASE`, `ELSE_CORRELATED_ERROR` fixtures, `--fold_loops` on a high-repeat fixture, and current flag parsing on supported circuits.
- `cargo test -p stab-bench m10_dem_benchmark_rows_have_stab_compare_runners` covers Stab-side M10 graphlike search, generated surface-code analyzer, error-decomposition utility, `analyze_errors --decompose_errors`, `.dem` parse, `.dem` print, and loop-folding analyzer benchmark runners. `just bench::baseline --only M10 --out target/benchmarks/m10-baseline --target-seconds 0.01 --cli-iterations 3` records a local pinned Stim v1.16.0 M10 baseline, and `just bench::compare --milestone M10 --baseline target/benchmarks/m10-baseline/baseline.json --strict` compares the Stab runners against that baseline with contract-only rows reported explicitly.

## Implementation Areas

- Added `DetectorErrorModel`, DEM instruction, target, repeat block, detector id, observable id, parser, canonical printer, detector counting, observable counting, and detector-shift helpers in `stab-core`.
- Added the first graphlike search internal types for observable masks, edges, nodes, graphs, canonical search states, and public shortest graphlike undetectable logical error search over direct DEM graph structures, including DEM flattening into graphlike edges and DEM error-instruction emission for search-state transitions.
- Added the first hypergraph search internal types and direct undetectable-logical-error algorithm for observable masks, hyperedges, nodes, graphs, and search states, including separator-aware target toggling, max-weight filtering, DEM flattening into hyperedges, detector-set BFS, path backtracking with duplicate-pair cancellation, and DEM error-instruction emission for search-state transitions.
- Added shortest and likeliest error SAT problem generation in WDIMACS form, including Stim-compatible XOR parity constraints, soft-clause weighting, half-probability omission, detector hard clauses, observable-flip hard clauses, and repeat flattening.
- Added matched-error value objects for coordinate-aware DEM targets, circuit targets, stack frames, circuit error locations, flipped measurements, and explained errors, including Stim-compatible display and fill helpers used by later error-matcher work.
- Added a staged error matcher that isolates individual candidate circuit errors, reuses the existing analyzer to derive each candidate's DEM symptoms, applies optional DEM filtering, and returns matched-error locations with detector and qubit coordinates for direct small-circuit cases, generated repetition-code `DEPOLARIZE1` noise, generated surface-code `DEPOLARIZE2` filter matches, and `PAULI_CHANNEL_2` component noise.
- Added a staged sparse reverse detector-frame tracker for symbolic detector/observable sensitivities across reverse circuit traversal, including representative Clifford undo, measurement/reset undo, detector and observable propagation, record feedback, anticommutation reporting, pair measurements, `MPAD`, and naive repeat traversal.
- Added a staged circuit-to-DEM analyzer for deterministic detector declarations, measurement-flip errors, unconditional and conditional correlated Pauli errors, identity-noise no-ops, reset cutoff of pending single-qubit noise, identical-symptom error merging, Pauli-target `OBSERVABLE_INCLUDE` logical sensitivity, and simple single-qubit `X_ERROR`, `Y_ERROR`, and `Z_ERROR` effects feeding measurement-record detectors and observables.
- Added single-qubit `DEPOLARIZE1` handling for probabilities up to `3/4` using Stim's independent per-channel probability conversion, with over-mixing rejection.
- Added two-qubit `DEPOLARIZE2` handling for probabilities up to `15/16`, using Stim's independent two-qubit Pauli-channel decomposition and identical-symptom XOR merging.
- Added exact-solved `PAULI_CHANNEL_1` handling for single-qubit channels that can be represented as independent errors without `--approximate_disjoint_errors`.
- Exposed the M10 independent/disjoint XYZ error-decomposition utility through typed probability triples for direct testing, benchmarking, and analyzer reuse.
- Added approximate `PAULI_CHANNEL_1` handling for remaining single-qubit channels under bare and numeric-threshold `--approximate_disjoint_errors`, including rejection when approximation is not explicitly enabled or when a component exceeds the threshold.
- Added approximate `PAULI_CHANNEL_2` handling for two-qubit channels under numeric-threshold `--approximate_disjoint_errors`, including per-channel disjoint component summation before independent error merging.
- Added pair-measurement analyzer handling for `MXX`, `MYY`, `MZZ`, and `MPP`, including pending Pauli-error parity updates, MPP result-flip probability handling, and matching backward gauge-tracker sensitivity for deterministic detector checks.
- Added `HERALDED_PAULI_CHANNEL_1` analyzer handling for single-component basis errors and thresholded disjoint multi-component errors.
- Added `HERALDED_ERASE` analyzer handling as thresholded disjoint heralded Pauli components with one herald measurement per target.
- Added scoped Clifford propagation for pending Pauli errors through single-qubit Clifford gates and `CX`, sweep-control no-op handling for `CX`, propagation of pending single-qubit `PAULI_CHANNEL_1` basis probabilities through single-qubit Clifford gates, cancellation of duplicate Pauli effects after propagation, and an explicit error instead of silent misanalysis when pending single-qubit Pauli channels would need to cross `CX`.
- Added `--decompose_errors` target-level decomposition paths that first try exact decomposition using known graphlike components, then optionally introduce a graphlike remnant edge, respect `--block_decompose_from_introducing_remnant_edges`, support `--ignore_decomposition_failures`, and reject simple undecomposable detector triples.
- Added a backward detector-sensitivity pass for the first `--allow_gauge_detectors` cases, including default rejection of non-deterministic detectors, rejection of non-deterministic measurement-record and Pauli-target observables even with gauge detectors allowed, and 50 percent gauge-error emission for detector-only gauge sets through `H`, `H_XY`, and `CX`.
- Added top-level `--fold_loops` handling for repeat blocks by analyzing one body, wrapping it in a DEM repeat block, and appending the body detector shift.
- Added `stab analyze_errors` CLI dispatch, including the legacy `--analyze_errors` alias and current staged flag parsing.
- Extended the oracle core fixture runner to support `core-dem-parse-print`.
- Implemented the current M10 exact and structural rows for `.dem` parse-print and basic `analyze_errors`.
- Added Stab-side benchmark compare runners for the M10 graphlike search, generated surface-code analyzer, error-decomposition utility, `analyze_errors --decompose_errors`, `.dem` parse, `.dem` print, and loop-folding analyzer rows.

## Current Evidence

| Requirement | Status | Evidence |
| --- | --- | --- |
| `.dem` parser and canonical printer | Partial | `DetectorErrorModel::from_dem_str`, `DetectorErrorModel::to_dem_string`, `just oracle::run --milestone M10 --exact` |
| DEM core types, repeats, coordinates, detector shifts, observables, separators, probability validation | Partial | `coverage-dem-dem-instruction`, `cargo test -p stab-core dem_instruction`, `cargo test -p stab-core dem` |
| Graphlike search structures and direct shortest-error algorithm | Partial | `coverage-search-graphlike-algo`, `coverage-search-graphlike-edge`, `coverage-search-graphlike-node`, `coverage-search-graphlike-graph`, `coverage-search-graphlike-search-state`, `cargo test -p stab-core graphlike`, `cargo test -p stab-core dem_shortest_graphlike_logical_error_api_matches_direct_dem`, `cargo test -p stab-bench m10_dem_benchmark_rows_have_stab_compare_runners`, `just oracle::run --milestone M10 --structural`, `just bench::compare --milestone M10` |
| Hypergraph search structures and direct undetectable-logical-error algorithm | Partial | `coverage-search-hyper-algo`, `coverage-search-hyper-edge`, `coverage-search-hyper-node`, `coverage-search-hyper-graph`, `coverage-search-hyper-search-state`, `cargo test -p stab-core hyper`, `just oracle::run --milestone M10 --structural`; generated circuit search parity remains future analyzer/QEC structural work |
| SAT WDIMACS problem generation | Implemented | `coverage-search-sat-wcnf`, `cargo test -p stab-core sat_problem`, `just oracle::run --milestone M10 --structural` |
| Matched-error value objects | Implemented | `coverage-simulators-matched-error`, `cargo test -p stab-core matched_error`, `just oracle::run --milestone M10 --structural` |
| ErrorMatcher staged subset | Implemented | `coverage-simulators-error-matcher`, `cargo test -p stab-core error_matcher`, `cargo test -p stab-core generated_repetition_code_data_depolarization`, `cargo test -p stab-core generated_surface_code_clifford_depolarization_filter`, `cargo test -p stab-core pauli_channel2_components_match_stim_error_matcher_order`, `just oracle::run --milestone M10 --structural`; generated surface-code repeat matching, heralded matching, and repeat-contained noise remain future work |
| Sparse reverse detector-frame tracker staged subset | Implemented | `coverage-simulators-sparse-rev-frame-tracker`, `cargo test -p stab-core sparse_rev_frame_tracker`, `just oracle::run --milestone M10 --structural`; optimized loop folding, all-unitary fuzzing, and error-matcher integration remain future work |
| `ErrorAnalyzer` staged structural coverage | Implemented | `coverage-simulators-error-analyzer`, `cargo test -p stab-core dem_analyzer`, `cargo test -p stab-core dem_analyzer_obs_include`, `cargo test -p stab-core dem_analyzer_rejects_unpaired_obs_include`, `cargo test -p stab-core dem_analyzer_mpp`, `cargo test -p stab-core dem_analyzer_quantum_controlled_pauli`, `cargo test -p stab-core dem_analyzer_ignores_sweep_controls`, `cargo test -p stab-core dem_analyzer_measurement_record_feedback`, `cargo test -p stab-core --test dem_analyzer_gauge`, `just oracle::run --milestone M10 --structural`; remaining upstream behavior is tracked by narrower partial rows and future generated-circuit parity |
| `stim analyze_errors` staged default behavior | Partial | `cargo test -p stab-cli m10`, `cargo test -p stab-bench m10_dem_benchmark_rows_have_stab_compare_runners`, `just oracle::run --milestone M10 --structural`, `just bench::compare --milestone M10` |
| `stim analyze_errors` unconditional correlated Pauli errors | Partial | `m10-analyze-errors-correlated-error`, `cargo test -p stab-core correlated_error`, `cargo test -p stab-cli correlated_error`, `just oracle::run --milestone M10 --exact`, `just oracle::record --check-clean` |
| `stim analyze_errors --decompose_errors` graphlike decomposition | Partial | `m10-analyze-errors-decompose-fallback`, `m10-analyze-errors-decompose-known-components`, `cargo test -p stab-core decompose`, `cargo test -p stab-cli decompose`, `cargo test -p stab-cli ignore_decomposition`, `cargo test -p stab-bench m10_dem_benchmark_rows_have_stab_compare_runners`, `just oracle::run --milestone M10 --exact`, `just bench::compare --milestone M10` |
| `stim analyze_errors --allow_gauge_detectors` | Partial | `m10-analyze-errors-allow-gauge-detectors`, `m10-analyze-errors-allow-gauge-detectors-hxy`, `m10-analyze-errors-multi-round-gauge`, `cargo test -p stab-core gauge`, `cargo test -p stab-core --test dem_analyzer_gauge`, `cargo test -p stab-cli gauge`, `just oracle::run --milestone M10 --exact`, `just oracle::record --check-clean` |
| `stim analyze_errors --approximate_disjoint_errors` conditional correlated Pauli errors | Partial | `m10-analyze-errors-else-correlated-error`, `cargo test -p stab-core else_correlated`, `cargo test -p stab-cli else_correlated`, `just oracle::run --milestone M10 --exact`, `just oracle::record --check-clean` |
| `stim analyze_errors` identity-noise no-ops | Partial | `m10-analyze-errors-identity-noise`, `cargo test -p stab-core identity_noise`, `cargo test -p stab-cli identity_noise`, `just oracle::run --milestone M10 --exact` |
| `stim analyze_errors` measurement-flip errors, noisy basis measurements, `MPAD`, pair measurements, duplicate detector-record parity, and identical-symptom merging | Partial | `m10-analyze-errors-measurement-flip`, `m10-analyze-errors-noisy-basis-measurements`, `m10-analyze-errors-mpad-pair-measurements`, `cargo test -p stab-core dem_analyzer_maps_measurement_flip_probability_to_error`, `cargo test -p stab-core dem_analyzer_merges_identical_error_symptoms`, `cargo test -p stab-core --test dem_analyzer_measurements`, `cargo test -p stab-cli measurement_flip`, `cargo test -p stab-cli noisy_basis_measurements`, `cargo test -p stab-cli mpad_pair_measurements`, `just oracle::run --milestone M10 --exact` |
| `stim analyze_errors` Pauli-target `OBSERVABLE_INCLUDE` | Partial | `m10-analyze-errors-obs-include-pauli`, `m10-analyze-errors-obs-include-boundaries`, `cargo test -p stab-core dem_analyzer_obs_include`, `cargo test -p stab-core dem_analyzer_rejects_unpaired_obs_include`, `cargo test -p stab-core --test dem_analyzer_observables`, `cargo test -p stab-cli obs_include_pauli`, `cargo test -p stab-cli obs_include_boundaries`, `just oracle::run --milestone M10 --exact` |
| `stim analyze_errors` reset cutoff behavior | Partial | `m10-analyze-errors-reset-clears-error`, `cargo test -p stab-core reset`, `cargo test -p stab-cli reset_clears_error`, `just oracle::run --milestone M10 --exact` |
| `stim analyze_errors` Clifford propagation | Partial | `m10-analyze-errors-h-propagates-pauli-error`, `m10-analyze-errors-hxy-propagates-pauli-error`, `m10-analyze-errors-cnot-propagates-pauli-error`, `cargo test -p stab-core propagates_pauli`, `cargo test -p stab-cli propagates_pauli`, `just oracle::run --milestone M10 --exact`, `just oracle::record --check-clean` |
| `stim analyze_errors` controlled-Pauli propagation | Partial | `m10-analyze-errors-controlled-pauli`, `m10-analyze-errors-composite-controlled-pauli`, `cargo test -p stab-core dem_analyzer_quantum_controlled_pauli`, `just oracle::run --milestone M10 --exact`, `just oracle::record --check-clean` |
| `stim analyze_errors` measurement-record feedback | Partial | `m10-analyze-errors-measurement-feedback`, `cargo test -p stab-core dem_analyzer_measurement_record_feedback`, `just oracle::run --milestone M10 --exact`, `just oracle::record --check-clean` |
| `stim analyze_errors` single-qubit `DEPOLARIZE1` | Partial | `m10-analyze-errors-depolarize1`, `coverage-util-bot-error-decomp`, `cargo test -p stab-core depolarize1`, `cargo test -p stab-core error_decomp`, `cargo test -p stab-core dem_error_decomposition_api_returns_typed_probability_triples`, `cargo test -p stab-cli depolarize1`, `cargo test -p stab-bench m10_dem_benchmark_rows_have_stab_compare_runners`, `just oracle::run --milestone M10 --exact`, `just bench::compare --milestone M10` |
| `stim analyze_errors` two-qubit `DEPOLARIZE2` | Partial | `m10-analyze-errors-depolarize2`, `cargo test -p stab-core depolarize2`, `cargo test -p stab-cli depolarize2`, `just oracle::run --milestone M10 --exact`, `just oracle::record --check-clean` |
| `stim analyze_errors` exact-solved single-qubit `PAULI_CHANNEL_1` | Partial | `m10-analyze-errors-exact-pauli-channel1`, `cargo test -p stab-core exact_solved_pauli_channel1`, `cargo test -p stab-cli exact_pauli_channel1`, `just oracle::run --milestone M10 --exact` |
| `stim analyze_errors --approximate_disjoint_errors` | Partial | `m10-analyze-errors-approx-pauli-channel1`, `m10-analyze-errors-approx-pauli-channel1-threshold`, `m10-analyze-errors-approx-pauli-channel2-threshold`, `cargo test -p stab-core threshold`, `cargo test -p stab-core pauli_channel1`, `cargo test -p stab-core pauli_channel2`, `cargo test -p stab-cli numeric_threshold`, `cargo test -p stab-cli pauli_channel2`, `just oracle::run --milestone M10 --exact` |
| `stim analyze_errors --approximate_disjoint_errors` heralded erasure | Partial | `m10-analyze-errors-heralded-erase`, `m10-analyze-errors-heralded-erase-conditional`, `cargo test -p stab-core heralded_erase`, `cargo test -p stab-core --test dem_analyzer_heralded`, `cargo test -p stab-cli heralded_erase`, `cargo test -p stab-cli heralded_erase_conditional`, `just oracle::run --milestone M10 --exact` |
| `stim analyze_errors --approximate_disjoint_errors` heralded Pauli channel | Partial | `m10-analyze-errors-heralded-pauli-channel1`, `coverage-util-top-circuit-to-dem`, `cargo test -p stab-core circuit_to_dem`, `cargo test -p stab-core --test dem_analyzer_heralded`, `cargo test -p stab-cli heralded_pauli_channel1`, `just oracle::run --milestone M10 --exact`, `just oracle::run --milestone M10 --structural` |
| Structural DEM comparators for generated QEC circuits and generated error matching | Partial | `coverage-simulators-generated-qec-dem`, `cargo test -p stab-core generated_qec_dem`, `just oracle::run --milestone M10 --structural`; broader generated QEC DEM coverage and generated surface-code repeat error-matcher parity remain future analyzer/search work |
| Loop folding without flattening high-repeat circuits | Partial | `m10-analyze-errors-fold-repeat`, `cargo test -p stab-core dem_analyzer_fold`, `just bench::compare --milestone M10` |
| M10 benchmark reporting | Implemented | `cargo test -p stab-bench m10_dem_benchmark_rows_have_stab_compare_runners`, `just bench::baseline --only M10 --out target/benchmarks/m10-baseline --target-seconds 0.01 --cli-iterations 3`, and `just bench::compare --milestone M10 --baseline target/benchmarks/m10-baseline/baseline.json --strict`; broader scale comparability remains future work for generated QEC and M12 performance hardening |

## Verification Commands

- `cargo fmt --check --all`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `just oracle::run --milestone M10 --exact`
- `just oracle::run --milestone M10 --structural`
- `just bench::compare --milestone M10`
